ممتاز. نوافق على Phase 7C.1 ونعتبرها مغلقة رسمياً.

لا تضف GET /api/reports/definitions/:id كميزة منفردة الآن.

الآن ابدأ Phase 8A فقط:
Final QA / API & UI Consistency Audit.

ممنوع:
- لا تضف ميزات جديدة كبيرة
- لا تضف integrations
- لا تضف AI جديد
- لا تضف automation
- لا تضف export
- لا تضف scheduled reports
- لا تغيّر business logic إلا لإصلاح خلل واضح
- لا تعمل migrations جديدة إلا إذا كان هناك mismatch حرج

الهدف:
فحص الاتساق النهائي بين الـ API والـ UI بعد كل المراحل، وإصلاح النواقص الصغيرة فقط التي تمنع الاستخدام الطبيعي.

Scope:

1. API Consistency Audit

افحص كل modules:
- auth
- workspaces/settings
- contacts
- conversations/messages
- tickets
- tasks
- followups
- opportunities
- orders
- payments
- debts
- knowledge
- ai
- analytics
- reports
- audit logs

لكل module:
- هل list موجود؟
- هل get by id موجود إذا تحتاجه الواجهة؟
- هل create/update/status موجود؟
- هل routes محمية بـ requireSession و requirePermission؟
- هل workspace-scoped؟
- هل رسائل الخطأ عربية؟

إذا وجدت endpoint ناقصاً تحتاجه الواجهة، أصلحه كـ consistency patch فقط.

مثال:
- إذا Reports edit/preview يحتاج GET /api/reports/definitions/:id:
  أضفه بشرط:
  - requireSession
  - requirePermission("reports:read")
  - workspace-scoped
  - 404 عربي إذا غير موجود
  - لا audit لأنه read-only
  - اختبار RBAC + workspace isolation

2. UI Consistency Audit

افحص الصفحات:
- Dashboard
- Contacts
- Contact Profile
- Inbox
- Tickets
- Tasks
- Followups
- Opportunities
- Orders
- Payments
- Debts
- Knowledge
- Agents
- Analytics
- Reports
- Settings
- Audit Logs

لكل صفحة تحقق من:
- RTL
- loading state
- empty state
- error state
- retry
- permission denied
- no English text for user
- no dead buttons
- disabled buttons have Arabic reason
- session expired behavior
- mobile layout لا يوجد كسر واضح

3. Reports API Patch إذا كان مطلوباً

إذا أضفت:
GET /api/reports/definitions/:id

اختبر:
- owner GET existing definition → 200
- viewer GET existing definition → 200 إذا لديه reports:read
- agent GET حسب seed → اذكر النتيجة
- WS2 GET definition من WS1 → 404
- UUID غير موجود → 404 عربي
- بدون session → 401
- بدون reports:read → 403

4. Smoke Tests

شغّل أو اختبر سريعاً:
- register
- login
- /api/auth/me
- healthz
- readyz
- contacts list
- conversations list
- orders list
- payments list
- debts list
- knowledge bases list
- ai provider status
- analytics overview
- reports definitions
- reports generated
- audit logs

5. Typecheck

شغّل:
- pnpm run typecheck:libs
- pnpm --filter @workspace/api-server run typecheck
- pnpm --filter @workspace/web run typecheck

6. Security Regression Check

أعد تأكيد:
- لا req.body.workspaceId
- لا req.query.workspaceId
- لا secrets hardcoded
- لا routes مالية بدون permission
- لا AI auto-send
- لا payment gateway
- لا external APIs جديدة

7. Report Back

بعد التنفيذ أعطني:
1. الملفات التي تغيرت
2. النواقص التي وجدتها
3. هل أضفت GET /api/reports/definitions/:id أم لا ولماذا
4. نتائج typecheck
5. نتائج smoke tests
6. نتائج RBAC/workspace لأي endpoint جديد
7. أي نصوص إنجليزية أو أزرار ميتة وجدتها وأصلحتها
8. المخاطر المتبقية

ثم توقف وانتظر الموافقة.