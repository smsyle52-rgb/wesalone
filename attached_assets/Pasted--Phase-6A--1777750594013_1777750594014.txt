ممتاز. تقرير Phase 6A قوي جداً، لكن لا نغلق المرحلة رسمياً قبل إغلاق نقطتين فقط من المخاطر المتبقية.

لا تضف Dashboard widget.
لا تبدأ Phase 6B.
لا تضف export.
لا تضف scheduled reports.
لا تضف AI reports.
لا تضف أي ميزة جديدة.

نفّذ فقط:

Phase 6A.1 — Analytics Clarity + Real Workspace Isolation Verification

Scope:

1. توضيح معنى Overview في الواجهة والـ API

المشكلة:
بعض مقاييس overview هي حالة حالية أو نشاط اليوم، وليست كلها مرتبطة بـ date_from/date_to.

المطلوب:
- في AnalyticsPage داخل تبويب "نظرة عامة"، أضف ملاحظة عربية واضحة:
  "تعرض هذه الصفحة مزيجاً من الحالة الحالية ونشاط اليوم. المقاييس التفصيلية في التبويبات الأخرى تعتمد على النطاق الزمني المحدد."
- لا تغيّر الحسابات الآن.
- لا تضف charts.
- لا تضف metrics جديدة.

اختياري إن كان بسيطاً:
- في استجابة /api/analytics/overview أضف metadata:
  {
    "scopeNote": "current_and_today_snapshot",
    "usesDateRange": false
  }
أو ما يعادله، بشرط ألا يكسر الواجهة.

2. Real Workspace Isolation Test

أنشئ أو استخدم workspace ثاني حقيقي، وليس UUID وهمي فقط.

اختبر فعلياً:
- WS1 لديه analytics data.
- WS2 لا يرى أرقام WS1.
- WS2 GET /api/analytics/overview يرجع أرقام WS2 فقط.
- WS2 GET /api/analytics/finance يرجع أرقام WS2 فقط.
- WS2 GET /api/reports/generated لا يعرض تقارير WS1.
- WS2 GET /api/reports/generated/:id لتقرير من WS1 → 404 أو 403.
- workspace_id في body أو query يتم تجاهله.

3. Report Definition isArchived Risk

لا تغيّر schema الآن إلا إذا كان آمناً جداً.
فقط أضف TODO واضح:
"report_definitions.is_archived مخزّن كنص حالياً؛ يفضّل تحويله إلى boolean قبل الإنتاج."

4. Typecheck

شغّل:
- pnpm run typecheck:libs
- pnpm --filter @workspace/api-server run typecheck
- pnpm --filter @workspace/web run typecheck

5. ممنوع

- لا تضف Dashboard widget
- لا تضف scheduled reports
- لا تضف exports
- لا تضف PDF/Excel
- لا تضف AI-generated reports
- لا تضف BigQuery
- لا تضف أي feature جديدة

بعد التنفيذ أعطني:
1. الملفات التي تغيرت
2. نص ملاحظة overview التي ظهرت في UI
3. نتائج typecheck
4. نتائج اختبار workspace ثاني حقيقي
5. هل أضفت TODO لـ is_archived أم لا
6. أي مخاطر متبقية

ثم توقف وانتظر الموافقة.