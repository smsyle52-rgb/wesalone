ممتاز. لا تضف full-text search index الآن.
لا تضف GIN/tsvector.
لا تضف pg_trgm الآن.
لا تضف Gemini.
لا تضف AI agents.
لا تضف embeddings.
لا تضف Vertex AI.
لا تضف أي optimization جديد.

قبل إغلاق Phase 5A رسمياً، نحتاج Phase 5A Closure Verification Report كامل، لأن التقرير الحالي ممتاز للـ backend لكنه ناقص في UI وroutes والصلاحيات التفصيلية.

أكمل التقرير فقط، بدون كود جديد إلا إذا كان هناك خلل يمنع التحقق.

المطلوب:

1. Changed Files
اعرض كل الملفات التي تغيرت:
- db schema
- knowledge routes
- seed/permissions
- audit
- KnowledgePage
- navigation
- أي components جديدة
- replit.md

2. Database
اعرض الجداول والأعمدة الفعلية:
- knowledge_bases
- knowledge_sources
- knowledge_documents
- knowledge_chunks
- faq_entries
- embeddings_index_reference

3. Routes + Permissions
اعرض كل routes والصلاحية المطلوبة:

Knowledge Bases:
- GET /api/knowledge/bases
- POST /api/knowledge/bases
- GET /api/knowledge/bases/:id
- PATCH /api/knowledge/bases/:id
- archive/delete

Sources:
- GET /api/knowledge/bases/:id/sources
- POST /api/knowledge/bases/:id/sources
- PATCH /api/knowledge/sources/:sourceId
- archive/delete

Documents:
- GET /api/knowledge/bases/:id/documents
- POST /api/knowledge/bases/:id/documents
- GET /api/knowledge/documents/:documentId
- PATCH /api/knowledge/documents/:documentId
- archive/delete
- POST /api/knowledge/documents/:documentId/rechunk

Chunks:
- GET /api/knowledge/documents/:documentId/chunks

FAQ:
- GET /api/knowledge/bases/:id/faqs
- POST /api/knowledge/bases/:id/faqs
- PATCH /api/knowledge/faqs/:faqId
- archive/delete

Search:
- GET /api/knowledge/search?q=...

4. Typecheck
اعرض:
- pnpm run typecheck:libs
- pnpm --filter @workspace/api-server run typecheck
- pnpm --filter @workspace/web run typecheck

5. RBAC
اعرض النتائج:
- owner POST base → 201
- manager POST base → 201
- agent GET bases → 200
- agent POST base → 403
- viewer GET bases → 200
- viewer POST base → 403
- accountant GET bases → 403
- accountant POST base → 403

6. Workspace Isolation
اعرض:
- WS2 GET base من WS1 → 404 أو 403
- WS2 GET document من WS1 → 404 أو 403
- WS2 GET chunks لوثيقة من WS1 → 404 أو 403
- WS2 search لا يرجع محتوى WS1
- workspace_id في body يتم تجاهله

7. Chunking
اعرض:
- إنشاء وثيقة قصيرة → chunk واحد
- إنشاء وثيقة طويلة → عدة chunks
- chunk_index يبدأ من 0
- rechunk يستبدل chunks القديمة
- embedding_status ليس embedded
- embeddings_index_reference status = not_configured

8. Search
اعرض:
- البحث يجد FAQ
- البحث يجد document/chunk
- query أقل من حرفين → 400 QUERY_TOO_SHORT
- search لا يرجع نتائج workspace آخر
- البحث الحالي ilike فقط، لا full-text، لا embeddings

9. Audit
اعرض أمثلة:
- knowledge_base_create
- knowledge_base_update
- knowledge_base_archive
- knowledge_source_create
- knowledge_document_create
- knowledge_document_rechunk
- faq_create
- faq_update
- faq_archive

10. UI Verification
تحقق من KnowledgePage:
- موجودة في navigation باسم "المعرفة"
- RTL
- loading state
- empty state
- error state
- retry
- permission denied
- قائمة قواعد المعرفة
- إنشاء قاعدة معرفة
- فتح قاعدة معرفة
- تبويبات: المصادر / الوثائق / الأسئلة الشائعة / المقاطع / الإعدادات
- إنشاء وثيقة نصية
- عرض chunks
- إعادة التقسيم
- إضافة FAQ
- بحث نصي
- أزرار file/url disabled مع سبب عربي:
  "سيتم تفعيل رفع الملفات والروابط في مرحلة لاحقة"
- لا نصوص إنجليزية للمستخدم

11. Safety
أكد:
- لا Gemini
- لا AI calls
- لا embeddings provider
- لا Vertex AI
- لا OCR
- لا external URL fetch
- لا secrets
- لا integrations
- لا file parsing معقد

12. Remaining Risks
اذكر بصراحة:
- هل توجد routes بدون requirePermission؟
- هل يوجد workspaceId يُقرأ من body؟
- هل UI كامل أم backend فقط؟
- هل يوجد أي AI/embedding code؟
- هل البحث الحالي ilike فقط؟
- هل توجد migration issue بسبب drizzle push؟
- هل seed sync fix أثر على أدوار قديمة بشكل مقصود؟

ممنوع:
- لا تضف full-text search
- لا تضف Gemini
- لا تضف AI agents
- لا تضف embeddings
- لا تضف Vertex AI
- لا تضف file upload
- لا تضف URL fetch
- لا تضف أي feature جديدة

بعد التقرير توقف وانتظر الموافقة.