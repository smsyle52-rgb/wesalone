ممتاز. نوافق على Phase 8A.2 ونعتبرها مغلقة رسمياً.

لا تضف forgot password الآن.
لا تضف email reset.
لا تضف OTP.
لا تضف 2FA.
لا تضف email provider.
لا تضف SMS.
لا تضف أي ميزة جديدة.

الآن ابدأ Phase 9A فقط:
Cloud Staging Deployment Preparation.

الهدف:
تجهيز المشروع للنشر على staging بيئة قريبة من الإنتاج، بدون تنفيذ نشر فعلي إن لم تكن بيانات Cloud متوفرة.

ممنوع:
- لا تضف features جديدة
- لا تضف integrations
- لا تضف email provider
- لا تضف payment gateway
- لا تضف WhatsApp API
- لا تضف Automation
- لا تضف Voice
- لا تضف تغييرات business logic
- لا تغيّر schema إلا إذا كان هناك blocker واضح

Scope:

1. Staging Environment Checklist

جهّز قائمة متطلبات staging:

- DATABASE_URL لـ Cloud SQL staging
- SESSION_SECRET قوي
- NODE_ENV=production أو staging حسب التصميم
- CORS_ORIGIN
- GEMINI_API_KEY optional
- ALLOWED_ORIGINS
- PORT
- Storage env placeholders إن وجدت

لا تضع أي secrets حقيقية في الكود أو replit.md.

2. Build Verification

تحقق من أو أضف scripts واضحة:

- build للـ backend
- build للـ frontend
- start production
- typecheck
- db:migrate
- health check

اختبر محلياً إن أمكن:
- pnpm run typecheck
- pnpm --filter @workspace/web build إن موجود
- pnpm --filter @workspace/api-server build إن موجود

إذا build scripts غير موجودة، أضفها بحذر بدون تغيير منطق التطبيق.

3. Production Start Path

راجع كيف يبدأ التطبيق في production:
- هل يخدم API فقط؟
- هل Vite dev فقط؟
- هل frontend يحتاج build/static hosting؟
- هل API يخدم static frontend أم منفصل؟

وثّق الوضع الحالي بوضوح:
- Replit dev
- staging المقترح
- Cloud Run لاحقاً

لا تغيّر البنية الكبيرة الآن إلا إذا كان fix بسيطاً.

4. Migration Staging Plan

وثّق خطوات staging:

- إنشاء Cloud SQL staging DB فارغة
- ضبط DATABASE_URL
- تشغيل:
  pnpm --filter @workspace/db run migrate
- تشغيل التطبيق
- فحص:
  GET /api/healthz
  GET /api/readyz
- اختبار register/login
- smoke tests للوحدات الأساسية

5. Smoke Test Script

إن كان مناسباً، أضف script بسيط أو وثيقة أوامر لـ smoke tests:

- healthz
- readyz
- register
- login
- auth/me
- contacts list
- conversations list
- orders list
- payments list
- debts list
- knowledge bases
- ai provider status
- analytics overview
- reports definitions

لا تضف framework كبير.
يكفي bash script إن كان بسيطاً.

6. Security / Env Checklist

تأكد من:
- لا secrets في repo
- لا GEMINI_API_KEY hardcoded
- لا SESSION_SECRET hardcoded
- CORS production ليس wildcard
- secure cookies في production
- rate limits موجودة
- request id موجود
- readyz لا يكشف secrets

7. Replit / Cloud Notes

حدّث replit.md بقسم:
Phase 9A — Cloud Staging Deployment Preparation

يشمل:
- Staging env variables
- Build commands
- Migration commands
- Smoke test commands
- Known limitations
- What remains before production

8. Tests Required

شغّل:
- pnpm run typecheck:libs
- pnpm --filter @workspace/api-server run typecheck
- pnpm --filter @workspace/web run typecheck

إن كانت build scripts موجودة:
- pnpm --filter @workspace/api-server run build
- pnpm --filter @workspace/web run build

Health:
- GET /api/healthz → 200
- GET /api/readyz → 200

Security scan:
- grep GEMINI_API_KEY
- grep SESSION_SECRET
- grep password
- تأكد لا secrets hardcoded

9. Report Back

بعد التنفيذ أعطني:
1. الملفات التي تغيرت
2. هل أضفت أو عدّلت build scripts؟
3. نتائج typecheck
4. نتائج build إن وُجد
5. نتائج healthz/readyz
6. staging env checklist
7. migration staging plan
8. smoke test plan أو script
9. security scan results
10. ما الذي ما زال مطلوباً قبل production الحقيقي

ثم توقف وانتظر الموافقة.