ممتاز. نوافق على Phase 5A ونعتبرها مغلقة رسمياً.

لا تضف full-text search الآن.
لا تضف GIN/tsvector.
لا تضف pg_trgm.
لا تضف embeddings provider الآن.
لا تضف Vertex AI Vector Search الآن.

الآن ابدأ Phase 5B فقط:
Gemini AI Agents Safe Layer.

ممنوع في هذه المرحلة:
- لا ترسل رسائل تلقائياً للعميل
- لا تؤكد مدفوعات
- لا تغيّر حالات مالية
- لا تحذف بيانات
- لا تغيّر صلاحيات
- لا تفعّل WhatsApp API
- لا تضف Automation
- لا تضف Voice
- لا تضف external integrations
- لا تضف embeddings provider فعلي
- لا تضف Vector Search
- لا تضف full-text search optimization

Phase 5B Scope:

1. الهدف

بناء طبقة وكلاء ذكاء اصطناعي آمنة فوق المنصة، باستخدام Gemini لاحقاً عبر adapter، لكن مع حواجز صارمة.

في هذه المرحلة:
- AI يقترح فقط
- الإنسان يعتمد
- كل AI run يُسجّل
- كل استخدام يُحسب
- لا auto-send
- لا financial mutation
- لا delete
- لا permissions mutation

2. Database / Schema

أضف أو ثبّت الجداول:

ai_agents:
- id
- workspace_id
- name
- type: support | sales | followup | summarizer | classifier | reports | collections
- status: active | disabled
- default_model: gemini_flash | gemini_flash_lite | gemini_pro | mock
- dialect: standard_arabic | yemeni_light | yemeni_business
- tone
- created_by
- created_at
- updated_at

ai_agent_versions:
- id
- workspace_id
- agent_id
- version_number
- status: draft | active | archived
- instructions_snapshot jsonb
- tools_snapshot jsonb
- model_config jsonb
- created_by
- created_at

ai_agent_instructions:
- id
- workspace_id
- agent_id
- role_prompt
- business_rules
- forbidden_actions
- escalation_rules
- created_at
- updated_at

ai_agent_tools:
- id
- workspace_id
- agent_id
- tool_key
- is_enabled
- requires_approval
- config jsonb
- created_at
- updated_at

ai_agent_channels:
- id
- workspace_id
- agent_id
- channel_account_id nullable
- mode: disabled | suggest_only | draft_only
- created_at
- updated_at

ai_runs:
- id
- workspace_id
- agent_id nullable
- task_type: summarize | classify | draft_reply | extract | suggest_action | report
- input_type: conversation | message | contact | ticket | order | payment | knowledge_search | manual
- input_ref_id nullable
- status: queued | running | succeeded | failed | blocked
- model
- provider: gemini | mock
- prompt_tokens nullable
- completion_tokens nullable
- total_tokens nullable
- estimated_cost nullable
- safety_status: ok | blocked | needs_approval
- error_message nullable
- created_by
- created_at
- completed_at

ai_messages:
- id
- workspace_id
- ai_run_id
- role: system | developer | user | assistant | tool
- content
- metadata jsonb
- created_at

ai_extractions:
- id
- workspace_id
- ai_run_id
- extraction_type
- result_json jsonb
- confidence numeric nullable
- created_at

ai_usage:
- id
- workspace_id
- date
- model
- provider
- task_type
- total_runs
- total_tokens
- estimated_cost
- created_at

ai_feedback:
- id
- workspace_id
- ai_run_id
- rating: positive | negative | neutral
- comment nullable
- created_by
- created_at

ai_safety_events:
- id
- workspace_id
- ai_run_id nullable
- event_type
- severity: low | medium | high | critical
- blocked_action
- reason
- payload jsonb
- created_by nullable
- created_at

approval_requests:
- id
- workspace_id
- source_type: ai_run | workflow | manual
- source_id
- action_type
- payload jsonb
- status: pending | approved | rejected | cancelled
- requested_by
- approved_by nullable
- rejected_by nullable
- reason nullable
- created_at
- resolved_at nullable

3. AI Adapter

أنشئ AI provider adapter:

- mock provider يعمل دائماً بدون API key
- gemini provider جاهز لكن لا يفشل النظام إذا GEMINI_API_KEY غير موجود
- إذا لا يوجد GEMINI_API_KEY:
  - يستخدم mock provider
  - يظهر في UI: "وضع تجريبي — لم يتم ربط Gemini بعد"

لا تضع API key في الكود.
استخدم environment variable فقط:
- GEMINI_API_KEY
أو لاحقاً Secret Manager.

4. Safe AI Tasks

أضف endpoints أو services للمهام التالية فقط:

- summarize conversation
- classify message/conversation
- draft reply
- extract entities
- suggest ticket/task/followup/opportunity/order
- knowledge search assisted summary بدون embeddings

ممنوع:
- send_message
- confirm_payment
- reject_payment
- update_payment_status
- delete_data
- change_role
- activate_integration
- auto_create_external_webhook

5. Backend APIs

AI Agents:
- GET /api/ai/agents
- POST /api/ai/agents
- GET /api/ai/agents/:id
- PATCH /api/ai/agents/:id
- POST /api/ai/agents/:id/versions
- PATCH /api/ai/agents/:id/instructions
- GET /api/ai/agents/:id/tools
- PATCH /api/ai/agents/:id/tools

AI Runs:
- GET /api/ai/runs
- GET /api/ai/runs/:id
- POST /api/ai/runs/summarize-conversation
- POST /api/ai/runs/classify-conversation
- POST /api/ai/runs/draft-reply
- POST /api/ai/runs/extract
- POST /api/ai/runs/suggest-actions

Approvals:
- GET /api/approvals
- POST /api/approvals/:id/approve
- POST /api/approvals/:id/reject

كل API يجب أن:
- يستخدم requireSession
- يستخدم requirePermission
- يكون workspace-scoped
- لا يقبل workspace_id من body/query
- يسجل ai_run
- يسجل ai_messages
- يسجل ai_usage
- يسجل ai_safety_events عند الحظر
- يرجع رسائل عربية

6. Permissions

أضف:
- ai:read
- ai:use
- ai:configure
- ai:approve
- ai:view_usage
- ai:view_safety
- approvals:read
- approvals:approve
- approvals:reject

أدوار:
- owner/manager: كل شيء
- agent: ai:use + ai:read حسب seed
- sales/support: ai:use إن وجد
- accountant: لا ai financial actions
- viewer: read فقط أو لا حسب seed، اذكر القرار

7. Safety Middleware

أضف AI safety guard.

يمنع أي action_type أو tool_key يحتوي أو يساوي:
- payment.confirm
- payment.reject
- payment.update_status
- debt.write_off
- role.change
- user.invite
- integration.activate
- message.auto_send
- data.delete
- order.cancel بدون approval

عند الحظر:
- ai_run.status = blocked
- ai_run.safety_status = blocked
- ai_safety_events row
- رسالة عربية:
  "تم منع هذا الإجراء لأنه يتطلب اعتماداً بشرياً أو غير مسموح للذكاء الاصطناعي"

8. Human Approval

AI يمكنه إنشاء approval_request فقط لهذه الاقتراحات:
- create_ticket
- create_task
- create_followup
- create_opportunity
- create_order_draft
- draft_reply

لكن لا ينفذها مباشرة.

approval approve في هذه المرحلة:
- لا تنفذ كل actions فعلياً إذا سيعقّد النظام
- يمكن فقط تغيير status إلى approved وتسجيل audit
- أو تنفيذ actions الآمنة فقط إذا كانت واضحة

الأهم:
لا ترسل رسالة للعميل.
لا تغيّر ماليات.

9. Knowledge Integration

في draft_reply أو summarize:
- يمكن استخدام بحث knowledge الحالي ilike
- لا embeddings
- لا Gemini RAG متقدم
- لا citations معقدة الآن
- فقط أضف مصادر المعرفة المستخدمة في metadata إن وجدت

10. UI

أضف صفحة:
"الوكلاء"

يجب أن تحتوي:
- قائمة الوكلاء
- إنشاء وكيل
- نوع الوكيل
- اللهجة: فصحى / يمني خفيف / يمني تجاري
- التعليمات
- الأدوات المسموحة
- حالة الوكيل active/disabled
- زر اختبار الوكيل
- سجل تشغيلات AI
- usage summary
- safety events
- approval requests

في Inbox / Conversation:
- زر "تلخيص المحادثة"
- زر "اقتراح رد"
- زر "تصنيف"
- الرد المقترح يظهر كمسودة فقط
- زر "استخدام الرد" يضعه في صندوق الكتابة فقط، لا يرسله تلقائياً
- زر "إرسال" يبقى يدوي من المستخدم

في Knowledge:
- لا تضف embeddings UI
- فقط اعرض أن البحث الحالي نصي

UI states:
- RTL
- loading
- empty
- error
- retry
- permission denied
- رسائل عربية
- لا نصوص إنجليزية للمستخدم

11. Audit

Audit actions:
- ai_agent_create
- ai_agent_update
- ai_agent_disable
- ai_run_create
- ai_run_blocked
- ai_approval_requested
- ai_approval_approved
- ai_approval_rejected
- ai_feedback_create

12. Usage Limits

لا تبنِ billing كامل الآن.
لكن:
- سجّل usage في ai_usage
- أظهر عداد بسيط:
  - runs اليوم
  - tokens إن وجدت
  - provider = mock أو gemini
- إذا mock فلا tokens حقيقية.

13. Tests Required

Typecheck:
- pnpm run typecheck:libs
- pnpm --filter @workspace/api-server run typecheck
- pnpm --filter @workspace/web run typecheck

RBAC:
- viewer POST /api/ai/runs/draft-reply → 403 أو حسب seed
- agent POST /api/ai/runs/draft-reply → 201 إذا ai:use
- accountant POST /api/ai/runs/draft-reply → 403 إذا لا ai:use
- viewer POST /api/ai/agents → 403
- owner POST /api/ai/agents → 201
- agent PATCH /api/ai/agents/:id → 403
- owner PATCH /api/ai/agents/:id → 200

Workspace isolation:
- AI agent من workspace آخر → 404
- AI run من workspace آخر → 404
- conversation من workspace آخر في summarize → 404 أو 403
- knowledge من workspace آخر لا يظهر في context

Safety:
- attempt tool payment.confirm → blocked
- attempt message.auto_send → blocked
- attempt role.change → blocked
- blocked action creates ai_safety_event
- blocked action does not mutate target entity

AI run:
- summarize conversation creates ai_run + ai_messages
- draft reply creates ai_run + ai_messages
- classify creates ai_extraction
- suggest_actions creates approval_request for safe suggestions only
- mock provider works without GEMINI_API_KEY
- gemini provider not required for tests

Human approval:
- create approval request
- approve changes status only أو executes safe action حسب implementation
- reject requires reason أو حسب design
- audit logs created

UI:
- Agents page RTL
- create agent works
- test agent works with mock
- Inbox summarize/draft buttons work
- draft reply is not sent automatically
- use draft only fills text box
- no payment/role/integration action exists in UI

Safety negative:
- no auto-send
- no payment confirm
- no financial mutation
- no delete
- no permission mutation
- no external integration activation

بعد التنفيذ أعطني:
1. الملفات التي تغيرت
2. الجداول/الأعمدة الجديدة
3. routes والصلاحيات
4. نتائج typecheck
5. نتائج RBAC
6. نتائج workspace isolation
7. نتائج safety guard
8. نتائج AI runs
9. نتائج approvals
10. نتائج knowledge integration
11. نتائج UI
12. أي مخاطر متبقية

ثم توقف وانتظر الموافقة.