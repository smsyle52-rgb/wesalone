# WESAL ONE — الحالة الحيّة
آخر تحديث: 6 يوليو 2026 (Claude — **عميل ثانٍ (مجوهرات المصعبي) عالق بلا قناة** — لكن هذه المرة إنستغرام+ماسنجر لا واتساب: `exchangeCodeForToken` تستلم 200 من ميتا بلا `access_token` (على الأغلب code مُستهلَك/رفض صلاحية)، فيرجع 409 — الكود كان يُسقط رسالة خطأ ميتا الفعلية بصمت. أُضيف تسجيل دقيق (بلا أي توكن) ليُعرف السبب الحقيقي في المرة القادمة — لم يُسترجَع هذا العميل يدوياً بعد (لا معرّفات ميتا حقيقية بحوزتنا، بعكس عبدالكريم). انظر جلسة (24). + جلسات الليلة السابقة (17–23): تصعيد، فوترة/خطة مجانية، `inventory_products.status`، `send_product_media`، مهلة onboarding، احتواء ضغط Cloud Run (توسيع ثم تراجع مقصود لحماية الإشعارات الحيّة)، فرع Codex منفصل يحتاج مراجعة، شارة "معتمد من Meta"، تفاعلات واتساب تُعلّق الوارد — كلها مُصلَحة ومدفوعة. **قرار مالك معلّق:** إضافة ملخص الباقات كأسئلة شائعة لمعرفة وكيل "وصال ون" — بانتظار إضافة المالك يدوياً.)

## 📌 جلسة 6 يوليو 2026 (24) — عميل ثانٍ عالق (إنستغرام+ماسنجر لا واتساب): 409 صامت من ميتا (Claude)

**السياق:** المالك سأل عن حالة `mujawahirat111almaseabi@gmail.com` (مجوهرات المصعبي) — "نجح ربط لكن لم نحفظ أصوله". نفس نمط عبدالكريم (جلسة 21): وكيل نشط، **صفر قنوات**.

**الفرق الجوهري عن حالة عبدالكريم:** هذا العميل لم يجرّب واتساب إطلاقاً — سجلات الإنتاج تُظهر محاولاته الثلاث عبر `embedded-signup/instagram-messenger/complete` تحديداً (على مدى ٤٥ دقيقة: 22:31، 23:13، 23:15)، فإصلاح مهلة الـ٩٠ ثانية (جلسة 21، خاص بواتساب فقط) **لا ينطبق على مساره أصلاً**. كل المحاولات الثلاث رجعت `409`، والرابعة `502`.

**السبب:** تتبّعت الكود إلى `exchangeCodeForToken()` — الفرع الذي يُرجع `409` (`meta_token_exchange_unavailable`) يعني: ميتا ردّت 200 (نجاح HTTP) لكن **بلا حقل `access_token`** في الجسم — العلامة الكلاسيكية لكود مصادقة (code) مُستهلَك مسبقاً أو رفض صلاحية من ميتا لهذا النشاط تحديداً. المشكلة: **الكود لم يكن يُسجّل رسالة الخطأ الفعلية من ميتا في هذا الفرع إطلاقاً** — نخمّن بدل أن نعرف.

### مُغلق ✅
| الإصلاح | الملف |
|---|---|
| تسجيل `payload.error` الفعلي من ميتا عند غياب `access_token` (بلا أي توكن في السجل) | `artifacts/api-server/src/modules/integrations/integrations.routes.ts` (`exchangeCodeForToken`) |

**لم يُنفَّذ:** استرجاع يدوي لهذا العميل تحديداً — بعكس عبدالكريم، لا نملك معرّفات ميتا حقيقية (waba_id/page_id) لحسابه بعد؛ يحتاج إما إعادة محاولة منه الآن (سيظهر السبب الحقيقي في السجلات هذه المرة)، أو معرّفات حقيقية من Meta Business Manager لاسترجاع يدوي مثل عبدالكريم.

**التحقق:** `tsc` نظيف ✅، 351/351 اختبار ناجح ✅، بناء production نظيف ✅. تغيير تسجيل بحت — لا سلوك جديد لاختباره حياً.

---

## 📌 جلسة 6 يوليو 2026 (23) — تفاعلات واتساب تُعامَل كمرفق وهمي فتُعلّق الوارد (Claude)

**الطلب:** المالك أبلغ أن صندوق الوارد "يعلّق" وأن محادثات سابقة "اختفت"، وأن بيانات غير المقروء/التأخر "كلها كذب" — ووصفه بأنه "أخطر منعطف".

**التحقيق:** سجلات الإنتاج أظهرت طلب جلب نفس المرفق (لنفس الرسالة) يتكرر على فترات غير منتظمة لمدة ٧ دقائق، برمز 401 ثم 404. فحص مباشر لتلك الرسالة في قاعدة البيانات (Cloud Shell) كشف: `attachments: [{"type": "reaction", "caption": null, "provider": "whatsapp", "mime_type": null}]` — **لا ملف حقيقي وراء هذا المرفق إطلاقاً**.

**السبب الجذري:** `extractMedia()` في `meta.routes.ts` تتعامل مع أي `message.type` غير "text" كوسائط قابلة للتحميل بلا استثناء — بما فيها **تفاعل واتساب** (👍 على رسالة سابقة، `message.type === "reaction"`)، الذي شكله الحقيقي `{message_id, emoji}` لا `{id, mime_type, caption}` كباقي أنواع الوسائط. النتيجة: كل تفاعل عميل يُخزَّن كمرفق فارغ، والواجهة تحاول جلبه فتفشل بحلقة متكررة — هذا التعليق المحدَّد كان في هذه المحادثة تحديداً.

**فحوصات إضافية (لطمأنة، لا إصلاح):**
- صفر ازدواج رسائل حقيقي (`provider_message_id` مكرر) خلال 24 ساعة عبر كل مساحات العمل — حماية `ON CONFLICT DO NOTHING` (P0 سابقة) تعمل فعلاً؛ عدّاد "غير مقروء" لا يتضخّم من التكرار.
- منطق `GET /conversations` (القائمة الرئيسية) سليم — ترتيب حسب آخر رسالة، صفحات عادية، لا فلتر يُخفي محادثات قديمة. لم أجد دليلاً مباشراً على فقدان بيانات حقيقي؛ الأرجح أن "الاختفاء" كان أثراً لنفس التعليق (واجهة عالقة تبدو غير مستجيبة)، لا فقداناً فعلياً.

### مُغلق ✅
| الإصلاح | الملف |
|---|---|
| تفاعل واتساب الآن نص فقط (`[تفاعل: 👍]` أو "أزال تفاعله") — **بلا أي مرفق إطلاقاً**، فلا محاولة جلب ملف غير موجود | `artifacts/api-server/src/modules/webhooks/meta.routes.ts` (`extractMedia`, دالة جديدة `extractReactionText`) |
| اختبار عقد جديد (C-WA-7) يوثّق شكل حمولة التفاعل الحقيقي من واتساب (`reaction.emoji`، بلا `id`/`mime_type`) | `artifacts/api-server/src/__tests__/meta-webhook-contract.spec.ts` |

**التحقق:** `tsc` نظيف ✅، **351/351 اختبار ناجح** (39 اختبار عقد ميتا، بينها الاختبار الجديد) ✅، بناء production نظيف ✅. لا يمكن اختبار حي عبر المعاينة (يحتاج حمولة webhook حقيقية من ميتا) — الثقة مبنية على تطابق دقيق بين شكل البيانات الحقيقي المُستخرَج من الإنتاج والكود المُصلَح.

---

## 📌 جلسة 6 يوليو 2026 (22) — احتواء الضغط: توسيع Cloud Run + اكتشاف فرع Codex منفصل + شارة Meta (Claude)

**الطلب:** المالك أبلغ أن التطبيق "يعلّق وبطيء" تحت ضغط رسائل العملاء، والهدف "احتواء الضغط".

**السبب المؤكَّد:** كلا خدمتي الإنتاج على Cloud Run — `khadamatak-staging` (التطبيق) و`khadamatak-outbox-worker` (يعالج الرسائل الصادرة وحلقة تشغيل الوكيل الذكي `runAgentRunner`) — كانتا مُعدَّتين بـ`autoscaling.knative.dev/maxScale=1`: **نسخة واحدة فقط مهما زاد الضغط**، بمعالج واحد و512Mi ذاكرة، تزامن حتى 80 طلباً. مع ردود ذكاء اصطناعي (استدعاءات نموذج بطيئة نسبياً) هذا يفسّر البطء المحسوس تحت الضغط بدقة.

**محاولة أولى (لاحقاً تراجعت):** رفعت `max-instances` لكلا الخدمتين (20/10) عبر `gcloud run services update` مباشرة. تحقّقت أولاً أن `claimOutboxEvents()`/`claimDomainEvents()` في `outbox-worker/src/index.ts` تستخدم `UPDATE ... WHERE id IN (SELECT ... FOR UPDATE SKIP LOCKED)` — آمن تماماً لمنع أي نسختين من معالجة نفس الحدث مرتين.

**لماذا تراجعت — اكتشاف حرج:** فحصت `cloudbuild.yaml`/`cloudbuild.worker.yaml` فوجدت `--max-instances=1` **مفروضاً صراحة في كل نشر** — أي نشر تالٍ (حتى لتغيير غير مرتبط، كشارة Meta) كان سيُلغي رفعي تلقائياً. الأهم: تتبّعت السبب فوجدت أن الإشعارات الحيّة (رسالة جديدة، تنبيه تصعيد بشري) تعمل عبر `new EventEmitter()` **داخل العملية فقط** (`artifacts/api-server/src/lib/events.ts:25`) — **بلا أي توزيع (Pub/Sub) بين نسخ متعددة**. رفع عدد النسخ كان سيُسكت هذه الإشعارات بصمت لأي عميل تهبط طلباته (SSE) على نسخة غير التي عالجت الحدث. وجدت تأكيداً إضافياً في خطة مهارة أخرى (`docs/architecture/chatwoot-parity/sonnet-task-graph.yaml:262-263`): `max-instances` يُرفع فقط بعد تفعيل `REALTIME_PUBSUB` — علم غير موجود في الكود إطلاقاً بعد (قيد أمان مقصود من البداية، لا إغفال).

### مُغلق ✅
| القرار | الحالة |
|---|---|
| عرضت المفاضلة كاملة على المالك (احتواء بطء الآن مقابل خطر إسكات إشعارات حيّة بصمت) | — |
| **قرار المالك:** إبقاء `max-instances=1` مؤقتاً على كلا الخدمتين | النشر التالي (`ac2c434f`) أعاد `maxScale` إلى 1 تلقائياً بالفعل — تأكَّد بالفحص المباشر بعده |
| قفل استشاري لكل محادثة (`pg_advisory_xact_lock`) لحماية التسلسل عند تفعيل تعدد النسخ مستقبلاً | مصمَّم، غير مُنفَّذ — يُربط بمهمة `REALTIME_PUBSUB` نفسها لاحقاً |

**الخلاصة:** حل بطء الرسائل الجذري يحتاج بناء Pub/Sub حقيقي (Redis أو Cloud Pub/Sub) لتوزيع الإشعارات الحيّة أولاً — لا حل مستعجل يخاطر بكسر ميزة تعمل الآن لصالح تسريع أخرى. هذه مهمة منفصلة مستقبلية.

**اكتشاف مهم أثناء الجلسة:** المالك شارك تقرير عمل من **Codex** (وكيل آخر) يعمل على فرع منفصل `feat/gcp-production-stability` في نسخة مختلفة من نفس المستودع (`khadamatak-github-publish-20260507163016`) — تصليب مشابه تماماً بالهدف (health checks، graceful shutdown، تجمّع اتصالات Postgres، telemetry، سياسات إسناد محادثات). **لا خطر فوري:** فرع منفصل عن main (لا `origin/main`)، وكل تعديلاته غير محفوظة كالتزامات بعد (working tree فقط) — لكنه متأخر ٤٦ التزامة عن `origin/main` الحالي. فُحص أهم تقاطعين محتملين مع عمل الليلة:
- `agent-tools.ts`: تعديلات Codex في `executeHandoff` فقط (دورة حياة محادثة موحّدة خلف علم `UNIFIED_LIFECYCLE`) — لا تلمس `loadExecutableAgentTools` (إصلاح `send_product_media`، جلسة 20). لا تعارض.
- `migrate-phase345.sql`: 40 سطراً إضافة بحتة (عمود تتبّع تسليم + جدول `assignment_policies` جديد) في منطقة مختلفة تماماً عن تعديلات الليلة الثلاثة. لا تعارض.

الفرع أكبر من ذلك بكثير (35 ملفاً، منها `cloudbuild.yaml` نفسه و`package.json`/تبعيات جديدة) — **يحتاج مراجعة كاملة مخصَّصة قبل أي دمج، لم تُنفَّذ بعد.** المالك اختار "راجعه قبل أي دمج" — لا يُدفع أو يُدمَج بدون تلك المراجعة.

**طلب إضافي منفصل:** إضافة شارة "معتمد من Meta" (أيقونة `FaMeta` + نص) في أسفل الصفحة التسويقية. اقترحتُ ابتداءً صياغة أدق ("مزوّد تقني معتمد من Meta") لتفادي ادّعاء شراكة تسويقية عامة قد لا تنطبق حرفياً — المالك أكّد صراحةً (مع تفسير أن الوصول لـEmbedded Signup هو اعتماد Tech Provider فعلي من Meta) أنه يريد الصياغة المطلقة "معتمد من Meta" تحديداً، فنُفِّذت كما طلب.

### مُغلق ✅
| الإصلاح | الملف |
|---|---|
| شارة "معتمد من Meta" في أسفل الصفحة التسويقية العامة | `artifacts/web/src/components/marketing/WesalSourceMarketingPage.tsx` |

**التحقق:** الصفحة (هذا الملف بعلامة `@ts-nocheck` — لا يخضع لـtsc) اختُبرت حياً في المعاينة: الشارة تظهر بنص وأيقونة صحيحين، أبعاد حقيقية (غير مخفية)، بلا أخطاء console. بناء production نظيف.

---

## 📌 جلسة 6 يوليو 2026 (21) — عميل حقيقي فشل ربط واتساب 44 دقيقة: مهلة Meta قصيرة لرحلة أول مرة + استرجاع يدوي (Claude)

**السياق:** المالك أبلغ أن عميلاً مهماً (شركة أرسلان للصرافة — "شركة صرافة عملاقة وموظفين يتوقعون وصال ون قادرة تخدمهم") نجح — حسب ظنّه — بربط واتساب، لكن لوحته لا تظهر. فحص قاعدة البيانات مباشرة أظهر: وكيل نشط ("ارسلان") لكن **صفر قنوات مربوطة إطلاقاً**، رغم أن سجلات الإنتاج تُظهر عشرات استدعاءات `embedded-signup/config` (تحميل/إعادة تحميل الصفحة) على مدى 44 دقيقة — **بلا استدعاء `/complete` واحد**.

**السبب الجذري الأرجح:** `waitForCapturedSignupInfo` (`OnboardingPage.tsx`) تنتظر postMessage من نافذة Meta يحمل `waba_id`/`phone_number_id` بعد أن يُغلق `FB.login` النافذة بالكود. المهلة كانت 20 ثانية فقط — كافية لتأخر شبكة بسيط (سبب الإصلاح الأصلي 3 يوليو الذي رفعها من 5 إلى 20)، لكن **غير كافية لتاجر أول مرة** يحتاج فعلياً يمر بخطوات اختيار/إنشاء حساب واتساب للأعمال، تسجيل رقم هاتف، والتحقق منه عبر OTP — وهذه خطوات تستغرق دقائق حقيقية لا ثوانٍ. هذا يفسّر بدقة كل الأدلة: محاولات متكررة، لا نجاح أبداً.

**الاسترجاع اليدوي الفوري (نفس نمط الليلة السابقة):** اكتُشفت المعرّفات الحقيقية عبر Graph API (تأكيد الرقم +967 774 025 718 قبل أي كتابة)، تأكَّد أن التطبيق لم يكن مشتركاً بأحداث WABA إطلاقاً (نفس عطل الليلة السابقة) فاشتُرك، ورُبطت القناة بوكيله. **اكتشاف إضافي أثناء الاسترجاع:** `resolveOnboardingCompletion` يتطلب دليل اتصال حقيقي (`credentials_secret_ref` أو أحد أعمدة `external_*`) — وبما أن الاسترجاع اليدوي يترك `credentials_secret_ref` فارغاً عمداً (نعتمد على `META_SYSTEM_USER_TOKEN` النظامي)، لم تُملأ أعمدة `external_phone_id`/`external_business_id` في أول إدراج، فبقي onboarding غير مكتمل رغم أن القناة تعمل فعلياً. أُصلح لهذا العميل، وطُبّق نفس الإصلاح استباقياً على عميلي الليلة السابقة (وكالة مهران جميدة، أبو شوقي) بنسخ المعرّفات من `provider_config` المخزَّن أصلاً.

### مُغلق ✅
| الإصلاح | الملف |
|---|---|
| مهلة `waitForCapturedSignupInfo` من 20 إلى 90 ثانية + رسالة توضيحية أثناء الانتظار (`isAwaitingMetaData`) تطمئن التاجر أن الانتظار طبيعي ومرتبط بإكمال خطوات Meta | `artifacts/web/src/pages/OnboardingPage.tsx` |
| رسالة الفشل عند انتهاء المهلة أوضح وتوجّه لإعادة المحاولة بدل رسالة تقنية جافة | نفس الملف |
| استرجاع يدوي: قناة واتساب + اشتراك `subscribed_apps` + ربط بالوكيل لعميل "أرسلان للصرافة" | إنتاج مباشر (Cloud Shell، ليس كود) |
| إصلاح فجوة أدلة onboarding (`external_phone_id`/`external_business_id`) لهذا العميل + عميلي الليلة السابقة | إنتاج مباشر (Cloud Shell) |

**التحقق:** `tsc` نظيف ✅، بناء production نظيف ✅. **لم يُختبَر حياً عبر نافذة Meta فعلية** — يتطلب حساب واتساب للأعمال حقيقياً وتفاعلاً بشرياً مع نافذة منبثقة، غير قابل للمحاكاة في المعاينة الآلية؛ الثقة مبنية على تحليل الكود المباشر لسبب الفشل (تطابق دقيق مع الأدلة: محاولات متكررة، صفر نجاح) لا اختباراً حياً.

---

## 📌 جلسة 5 يوليو 2026 (20) — لا وكيل أرسل صورة منتج قط: send_product_media معطّلة افتراضياً للجميع (Claude)

**الطلب:** "الوكيل لم يرسل صور من المنتجات."

**السبب الجذري:** `executeSendProductMedia` (`agent-tools.ts`) أداة مبنية بالكامل — تجد المنتج، تبني caption بالاسم والسعر، وتوجّه حدث outbox الصحيح حسب القناة (واتساب/إنستغرام/ماسنجر) مع حل المستلم الصحيح. لكن `loadExecutableAgentTools` لا يتيح أي أداة للنموذج إلا إن وُجد لها صف صريح `isEnabled=true` في `ai_agent_tools` — و`POST /agents` (إنشاء الوكيل) **لا يزرع أي صف إطلاقاً** لأي أداة عند الإنشاء. فحص شامل لـ`artifacts/web/src` أكّد: **لا توجد أي واجهة في المنتج كله** لتفعيل/تعطيل أي أداة من الأربع (`create_order`, `log_payment_claim`, `schedule_followup`, `send_product_media`) — الاستثناء الوحيد `handoff_to_human` المُفعَّلة قسراً من الكود نفسه (جلسة 17). النتيجة: **كل وكيل على المنصة، منذ إطلاق هذه الأداة، غير قادر على إرسال صورة منتج واحدة قط** — بصرف النظر عن أي طلب عميل مهما كان صريحاً.

### مُغلق ✅
| الإصلاح | الملف |
|---|---|
| `send_product_media` تُعامَل الآن كقدرة أساسية افتراضية (نفس معاملة `handoff_to_human` تماماً) — تُضاف تلقائياً للنموذج إلا إن وُجد لها صف صريح `isEnabled=false` (تعطيل يدوي مستقبلي محترَم إن ظهرت واجهة). لا حاجة لـbackfill/migration — الإصلاح على مستوى الكود فيسري فوراً على كل الوكلاء الحاليين والجدد بمجرد النشر. | `artifacts/api-server/src/lib/agent-tools.ts` (`loadExecutableAgentTools`) |

**ملاحظة مؤجَّلة (قرار مالك، خارج نطاق الطلب الحالي):** نفس العطل بنيوياً يمسّ `create_order` و`log_payment_claim` و`schedule_followup` — معطّلة فعلياً للجميع أيضاً، بلا أي واجهة تفعيل. لم تُلمس عمداً (مخاطرها المالية/التشغيلية أعلى من إرسال صورة معلوماتية بحتة — تحتاج قرار مالك صريح قبل تفعيلها تلقائياً للجميع).

**التحقق:** `tsc` نظيف ✅، **350/350 اختبار ناجح** (لا كسر لاختبارات `agent-tool-declarations.spec.ts` الموجودة، تختبر دالة مختلفة أدنى مستوى) ✅، بناء production نظيف ✅. لم يُختبَر حياً عبر محادثة حقيقية (يحتاج قناة ميتا فعلية) — لكن المسار نفسه (`executeSendProductMedia` + توجيه outbox) موجود ومكتمل منذ قبل هذه الجلسة؛ الفجوة الوحيدة كانت إتاحة الأداة للنموذج، وهي الآن مُصلَحة مباشرة.

---

## 📌 جلسة 5 يوليو 2026 (19) — عطل نشط: عمود inventory_products.status مفقود من الإنتاج (Claude)

**السياق:** المالك سأل "عدة عملاء يتوقفون عند ربط القناة، ايش الحل؟". فحص سجلات الإنتاج (Cloud Logging، `httpRequest.status=402`) أكّد: 3 عملاء حقيقيين منفصلين (2، 4، 5 يوليو) اصطدموا تحديداً بعطل `plan_limit_reached` على `instagram-messenger/complete` — **بالضبط** عطل جلسة (18) أعلاه (حد قنوات المجاني)، المُصلَح والمدفوع بالفعل. لا حاجة لإصلاح إضافي لسؤال «ربط القناة» تحديداً.

**اكتشاف عرضي أثناء البحث (أخطر وأعجل):** نفس فحص السجلات كشف مئات الأخطر خلال الساعات الأخيرة: `column "status" of relation "inventory_products" does not exist` — على استعلامات INSERT و SELECT حقيقية من عدة تجار (تأكَّد على الأقل 3 workspaces مختلفة)، وأيضاً فشل صامت متكرر في `loadProductCatalogContext` (السجل: `"loadProductCatalogContext failed — continuing without catalog context"`).

**السبب الجذري:** `lib/db/src/schema/products.ts` يعرّف `status: text("status").notNull().default("active")` على `inventoryProductsTable` منذ دمج الكوميرس، ونفس الدمج أضاف فعلياً 4 أعمدة أخرى مفقودة (`barcode`, `cost`, `images`, `low_stock_threshold`) إلى `scripts/migrate-phase345.sql` — **لكن نسي `status` تحديداً**، رغم أن التعليق الملاصق يحذّر صراحة: "هذه الأعمدة MUST exist in prod... وإلا /products تُرجع 500". هذا يعني: كل تاجر أضاف منتجات، ثم عطلت صفحة منتجاته (500 على العرض والإضافة)، **وأي رسالة رد من الوكيل لعملائه افتقدت كتالوج الأسعار بصمت** — نفس فئة "اختراع/رفض الأسعار" التي عالجناها طوال الليلة، لكن هذه المرة السبب تقني بحت (عمود مفقود) لا حدّ في الـprompt.

### مُغلق ✅
| الإصلاح | الملف |
|---|---|
| إضافة عمود `status` (نفس نمط الأعمدة الأربعة الأخرى — `DO $$ ... EXCEPTION WHEN duplicate_column`) + الفهرس `idx_inv_products_status` المطابق للـDrizzle schema | `scripts/migrate-phase345.sql` |

**التحقق:** لا تغيير كود TypeScript (سكيمة الكود صحيحة أصلاً؛ الفجوة كانت في ملف الهجرة فقط). **دُفع** (`70d75a9`) ونجح كلا الـbuild (staging+worker) — وبما أن خطوة `migrate-database` في `cloudbuild.yaml` تُشغّل هذا الملف بـ`ON_ERROR_STOP=1`، فنجاح الـbuild دليل قاطع أن العمود والمؤشر صارا في قاعدة بيانات الإنتاج فعلياً؛ أي خطأ SQL كان سيُفشل الـbuild فوراً. لا أخطاء `inventory_products` جديدة في السجلات منذ اكتمال النشر.

---

## 📌 جلسة 4 يوليو 2026 (18) — الخطة المجانية تمنع فعلياً ربط إنستغرام+ماسنجر (Claude)

**الطلب:** المالك طلب تفعيل ربط قنوات إنستغرام وماسنجر للخطة المجانية، مع إبقائها ضمن حد نقاط الخطة المجانية (1000 نقطة) بلا تغيير.

**السبب الجذري:** لا يوجد أي منع على مستوى نوع القناة في الكود — القيد الوحيد هو عدد القنوات الإجمالي (`limits.channels`). لكن مسار توصيل إنستغرام+ماسنجر معاً (`POST /meta/embedded-signup/instagram-messenger/complete`) يكتشف كل صفحات فيسبوك وحسابات إنستغرام المتاحة من حساب Meta المستخدَم، وينشئ قناة لكل واحدة منها **في نفس الاستدعاء** (`requestedChannelCount = pages.length + instagramAccounts.length`) — فصفحة فيسبوك واحدة مرتبطة بحساب إنستغرام واحد (الإعداد المعتاد) تعني طلب قناتين دفعة واحدة. الخطة المجانية كانت `channels:1`، فالفحص `current + requestedChannelCount > limit` (`0+2 > 1`) يرفض العملية دائماً بـ402 — **الإكمال كان مستحيلاً فعلياً على أي حساب مجاني**، بصرف النظر عن أي شيء آخر في الإعداد.

### مُغلق ✅
| الإصلاح | الملف |
|---|---|
| حد قنوات الخطة المجانية `1 → 2` (فقط؛ باقي الحدود بلا تغيير، `monthly_points` بقيت 1000) | `artifacts/api-server/src/lib/seed.ts` (`SYSTEM_PLANS`) |
| نفس القيمة حرفياً في الكتلة المطابقة (الملف يُعاد تطبيقه عند كل دفع) | `scripts/migrate-phase345.sql` (Billing patch C) |
| تحديث نص تسويقي كان يذكر «قناة واحدة» للمجاني في الصفحة العامة | `artifacts/web/src/components/marketing/WesalSourceMarketingPage.tsx` |

**التحقق:** `tsc` نظيف (api-server + web) ✅، **350/350 اختبار ناجح** ✅، بناء production نظيف لكليهما ✅. لم يُختبَر حياً عبر Meta OAuth فعلي (يحتاج حساب Meta حقيقي) — لكن منطق الفحص (`checkLimit`) نفسه مُختبَر ومباشر؛ رفع الرقم من 1 إلى 2 يكفي حسابياً لإزالة الحجب المؤكَّد من الكود.

---

## 📌 جلسة 4 يوليو 2026 (17) — عطل تصعيد: مفردة «صعّد» لم تكن تُطابق حارس «الادعاء=تنفيذ» (Claude)

**كيف اكتُشف:** المالك أبلغ أن آخر محادثتين انتهتا بفشل ذريع — لا سعر، لا تحويل بشري، لا إقناع. فحص نص المحادثة الحقيقية (استفسار عن الخدمة انتهى بسؤال شراكة/استثمار) كشف أن العميل انتظر رداً من «الإدارة» ولم يصله أحد قط.

**السبب الجذري:** الوكيل قال حرفياً 4 مرات «سأقوم بتصعيد طلبك للإدارة... وسأصعد كلا الأمرين للإدارة». حارس `HANDOFF_PROMISE_PATTERNS` (`agent-escalation.ts`، أُنشئ 3 يوليو من حادثة مشابهة بمفردة «حوّل») يطابق فقط عائلة «حوّل/نقل» — لا يعرف مفردة «صعّد/تصعيد» إطلاقاً. النتيجة: `shouldEscalate` بقي `false`، `agentStatus` لم يتحوّل لـ`human`، لا إشعار وصل للتاجر، والعميل انتظر مسؤولاً لن يأتي أبداً. تحقّقتُ أن مسار التصعيد نفسه (تحديث الحالة + الإشعار + بث الحدث في `internal.routes.ts`) يعمل تماماً — العطل حصراً في الزناد (المطابقة النصية) لا في التنفيذ.

**ملاحظتان أضيق نطاقاً من نفس المحادثة — لم تُصلَحا، تحتاجان قرار المالك (قلب منتج):**
1. **الأسعار خلف تسجيل الدخول:** الوكيل وجّه العميل لصفحة `wesal.one/billing` لمعرفة السعر، لكنها تتطلب تسجيل دخول — والعميل رفض التسجيل قبل معرفة السعر (طلب صراحة تمكين تصفّح الصفحات العامة بما فيها الأسعار قبل التسجيل). هذا خارج نطاق حارس الأسعار (يعمل كما صُمِّم: يمنع الاختراع لا محتوى الصفحة).
2. **رفض استفسار الشراكة/الاستثمار بجفاء:** حين سأل العميل عن الاستثمار كشريك، ردّ الوكيل «لا توجد برامج شراكة حالياً» وأغلق الموضوع بدل تصعيده بحماس لصاحب العمل مباشرة (فرصة تجارية لا شكوى).

### مُغلق ✅
| الإصلاح | الملف |
|---|---|
| أُضيفت عائلة أنماط «صعّد» الكاملة (ساصعد، بتصعيد طلبك/المحادثه، قمت بتصعيد، تم تصعيد/التصعيد، سيتم تصعيد، صعدت/صعدنا طلبك) — موازية تماماً لبنية عائلة «حوّل» الموجودة | `artifacts/api-server/src/lib/agent-escalation.ts` |
| 3 اختبارات جديدة بالنص الحرفي من المحادثة الحقيقية (`agent-escalation.spec.ts`) | نفس الوحدة |

**التحقق:** `tsc` نظيف ✅، **333/333 اختبار ناجح** (30 اختبار تصعيد بما فيها 3 جديدة) ✅، بناء production نظيف ✅. **قرار المالك:** ادفع هذا الإصلاح فوراً منفصلاً عن الملاحظتين أعلاه (لا علاقة له بقراري الأسعار والشراكة).

---

## 📌 جلسة 4 يوليو 2026 (16) — صفحة إدارية: متابعة التسجيلات الجديدة (خوارزميات مراقبة الضيوف) (Claude)

**السياق:** طلب المالك في جلسة 15 "خوارزميات تراقب الضيوف الجدد" بقي مؤجلاً بانتظار تحديد الشكل. القرار المُنفَّذ: صفحة إدارية تفاعلية تُغني عن فحص SQL يدوي كان يتكرر طوال الليلة لكل عميل جديد على حدة.

**البناء:**
- `GET /admin/signups?days=N` (endpoint جديد، محمي بـ`requirePlatformAdmin`): يجلب كل مساحات العمل المسجَّلة خلال آخر N يوم (افتراضي 14، حتى 60)، ولكل واحدة يستدعي `getWorkspaceOnboardingStatus()` الموجودة مسبقاً (نفس المنطق المُختبَر في `onboarding-status.spec.ts`) ليحسب: هل أكمل الإعداد، هل أنشأ وكيلاً، هل ربط قناة، وأين توقف فعلياً (`stuckAt`: تأكيد البريد ← وكيل ← قناة بالترتيب).
- `AdminSignupsPage.tsx` (صفحة جديدة `/admin/signups`، نفس صلاحية platform admin): جدول بكل تسجيل جديد مع شارات (بريد/وكيل/قناة) وتمييز لوني للصفوف غير المكتملة، واختيار مدى الأيام (7/14/30/60).
- ربط في `App.tsx` (route) و`Layout.tsx` (رابط تنقّل ضمن مجموعة "wesal-admin").

### مُغلق ✅
| الإصلاح | الملف |
|---|---|
| Endpoint جديد `GET /admin/signups` — تجميع حالة كل تسجيل جديد (بريد/وكيل/قناة/توقف عند) بإعادة استخدام `getWorkspaceOnboardingStatus()` | `artifacts/api-server/src/modules/admin/admin.routes.ts` |
| صفحة إدارية جديدة تعرض الجدول والفلاتر | `artifacts/web/src/pages/AdminSignupsPage.tsx` (جديد) |
| Route + رابط تنقّل | `artifacts/web/src/App.tsx`, `artifacts/web/src/components/Layout.tsx` |

**التحقق:** `tsc` نظيف (api-server + web) ✅، بناء production لـweb نظيف ✅. اختبار حي محلي: `GET /api/admin/signups?days=30` أعاد `200` وبيانات صحيحة لـ٧ مساحات عمل تجريبية حقيقية؛ لقطة شجرة إمكانية الوصول (accessibility tree) للصفحة أكدت عرض الجدول والشارات وتسميات "توقّف عند" بدقة لكل الحالات السبع.

---

## 📌 جلسة 4 يوليو 2026 (13) — عطل منتجي جذري: كل تجربة مجانية تبدأ برصيد صفر نقاط (Claude)

**كيف اكتُشف:** بعد استرجاع قناة العميل يدوياً (جلسة 12) وتفعيل اشتراك webhook، وصلت رسالة العميل الأولى فعلياً وسُجِّلت بقاعدة البيانات — لكن الوكيل لم يرد. فحص `ai_runs.error_message` أظهر `"AI points exhausted"`.

**السبب الجذري:** `registerWorkspace()` في `auth.service.ts` تُنشئ `subscriptions` بخطة "free" (تجربة ١٤ يوماً) لكنها **لا تمنح أي نقاط فعلية إطلاقاً**. منح النقاط الشهرية (`createGrant` بنوع `monthly_subscription`) موجود فقط في `subscription-payments.ts`، ويُستدعى حصراً عند تأكيد **دفع فعلي** لخطة مدفوعة — وهذا المسار يرفض صراحة الخطط المجانية (`slug === "free"` → `PLAN_NOT_PAYABLE`). النتيجة: **كل حساب تجريبي جديد يبدأ برصيد ٠ نقاط دائماً، بلا استثناء**، ويتعطل وكيله بصمت على أول رسالة حقيقية — تماماً كما حصل مع هذا العميل. هذا على الأغلب يفسّر جزءاً حقيقياً من ضعف التفاعل مع الإعلان الذي شغّله المالك (نطاق أوسع من مجرد هذا العميل).

**طلب المالك:** "صلح المشكلة فوراً 1000 نقطة تجربة".

### مُغلق ✅
| الإصلاح | الملف |
|---|---|
| `registerWorkspace()` تمنح الآن ١٠٠٠ نقطة تلقائياً (`grantType: monthly_subscription`, `sourceType: trial_signup`, تنتهي مع `trialEndsAt`, `idempotencyKey` فريد لكل مساحة عمل) عند التسجيل بخطة "free" | `artifacts/api-server/src/modules/auth/auth.service.ts` |
| **عطل مكتشَف أثناء التنفيذ:** `createGrant(opts, externalTx)` تقبل transaction خارجية لكن `getOrCreateWallet()` بداخلها تستخدم `db` المستقل دائماً — فشل بـ`foreign key violation` لأن صف مساحة العمل لم يُلتزَم (commit) بعد ضمن نفس transaction التسجيل. **الحل:** نُقل استدعاء `createGrant` خارج transaction التسجيل بالكامل (يُستدعى بعد نجاح الالتزام، بدون تمرير tx) بدل إصلاح الدالة المشتركة (أقل خطورة على مسارات فوترة أخرى تستخدمها). | نفس الملف |

**التحقق:** تسجيل حساب تجريبي محلي جديد فعلي (`test.points2.*@wesalqa.test`) — نجح بـ`201` (بعد أن فشل أول مرة بـ`500` بسبب عطل الـtransaction أعلاه)، وتأكيد مباشر من `point_grants`: `1000000000 micro-points`، `grant_type=monthly_subscription`، `expires_at` مطابق للـ١٤ يوماً، `source_type=trial_signup`. `tsc` ✅، بناء كامل ✅.

---

## 📌 جلسة 4 يوليو 2026 (12) — تطبيقنا غير مشترك في أحداث WABA + استعادة رسائل العميل (Claude)

**كيف اكتُشف:** بعد استرجاع القناة يدوياً (جلسة 11)، أرسل العميل عدة رسائل تجريبية ولم يصل أي منها لخادمنا (صفر استدعاء `webhooks/meta` في كل الفحوصات). نظرية أولى خاطئة: افترضت أن السبب "وضع التعايش يحتاج تأكيداً من داخل تطبيق واتساب للأعمال" — تبيّن لاحقاً أنها غير صحيحة.

**السبب الفعلي:** فحص مباشر عبر Graph API (`GET /{waba_id}/subscribed_apps` باستخدام `META_SYSTEM_USER_TOKEN`) أظهر `{"data":[]}` — **تطبيقنا غير مشترك إطلاقاً** في أحداث حساب واتساب هذا. حقل `webhook_configuration` الذي رأيته سابقاً في استجابة `/phone_numbers` يعني فقط أن رابط webhook مُهيَّأ على مستوى التطبيق عموماً — لا يعني أن هذا الـWABA تحديداً مُشترك لإرسال أحداثه له. الاسترجاع اليدوي للقناة (جلسة 11) أنشأ صف `channel_accounts` لكنه لم يُنفّذ خطوة `POST /{waba_id}/subscribed_apps` التي يقوم بها مسار `/embedded-signup/complete` الطبيعي عادة.

**الإصلاح:** استدعاء مباشر (نفّذه المالك بنفسه عبر Cloud Shell، بعد رفض المصنّف الآلي تنفيذي المباشر لهذا الفعل مرتين حتى بعد موافقة صريحة): `POST /{waba_id}/subscribed_apps` → `{"success":true}`.

**التحقق:** خلال دقائق، رسالة العميل الحقيقية ("السلام عليكم") وصلت وسُجِّلت في `messages` (`inbound`، ٠٠:٣١)، و`domain_events` أظهر `message.received` بـ`processed_at` مطابق — الاستقبال يعمل تماماً الآن. (محاولة الرد فشلت لسبب منفصل تماماً — انظر جلسة 13).

**ملاحظة مهمة:** أول محاولة تسجيل مباشر لرقم العميل عبر `POST /{phone_number_id}/register` (لتفعيل إرسال Cloud API) رجعت **"Register endpoint is not available for SMB businesses"** — هذه ليست عطلاً، بل تأكيد أن الرقم في وضع تعايش (coexistence) مع تطبيق واتساب للأعمال، وهذا الوضع لا يستخدم/يحتاج هذه النقطة أصلاً. الإرسال الفعلي عبر `POST /{phone_number_id}/messages` نجح لاحقاً وتأكَّد بتجربة حقيقية (رسالة وصلت فعلياً لجوال المالك).

---

## 📌 جلسة 4 يوليو 2026 (15) — ربط تلقائي بين القناة والوكيل عند الإنشاء (Claude)

**السياق:** المالك اختبر onboarding الجديد (خطوتان) بنفسه ووجد أن الوكيل لا يرتبط تلقائياً بالقناة بعد اكتمال الخطوتين — يحتاج المستخدم يذهب يدوياً للإعدادات ← القنوات ويختار الوكيل (تماماً كما فعلنا يدوياً الليلة لعميلي جلسة 11/12). طلب: ربط تلقائي فوري، بلا أي إعداد إضافي.

**السبب:** كل مسارات إنشاء `channel_accounts` (`upsertMetaChannelAccount` في `integrations.routes.ts` لمسار Meta embedded-signup، و`POST /channels/accounts` في `channels.routes.ts` للقنوات اليدوية) لم تكن تضبط `default_agent_id` إطلاقاً عند الإنشاء — القناة تُنشأ وتعمل (استقبال/إرسال) لكن بلا وكيل مسؤول، فلا رد تلقائي أبداً حتى تعيين يدوي.

### مُغلق ✅
| الإصلاح | الملف |
|---|---|
| بعد إنشاء/تحديث القناة، إن كانت `default_agent_id` فارغة: يُختار آخر وكيل نشط في نفس الـworkspace (`ORDER BY updated_at DESC, created_at DESC LIMIT 1`) ويُضبط تلقائياً. لا يتجاوز اختياراً سابقاً للتاجر (الشرط `if (!account.defaultAgentId)`). | `artifacts/api-server/src/modules/integrations/integrations.routes.ts` (`upsertMetaChannelAccount`) |
| نفس المنطق بالضبط لمسار القناة اليدوية | `artifacts/api-server/src/modules/channels/channels.routes.ts` (`POST /accounts`) |

**التحقق:** `tsc` نظيف ✅، **330/330 اختبار ناجح** (كامل مجموعة api-server) ✅. اختبار حيّ محلي كامل: تسجيل → تأكيد بريد → إنشاء وكيل → إنشاء قناة يدوية عبر `POST /channels/accounts` → تأكيد أن `channel.defaultAgentId` يطابق معرّف الوكيل تماماً بلا أي خطوة يدوية. (مسار Meta لم يُختبر حياً — لا صلاحيات Meta محلياً — لكنه يستخدم نفس الكود المُختبَر بالضبط.)

**طلب إضافي مؤجَّل (يحتاج تحديد نطاق):** المالك طلب أيضاً "خوارزميات تراقب الضيوف الجدد" — رؤية سريعة لحالة كل عميل جديد (سجّل؟ أنشأ وكيلاً؟ ربط قناة؟ أين توقف؟) بدل تكرار فحص يدوي عبر SQL كما حصل الليلة لعدة عملاء. لم يُبنَ بعد — يحتاج قراراً: صفحة إدارية جديدة أم تنبيه استباقي أم تقرير دوري؟

---

## 📌 جلسة 4 يوليو 2026 (14) — قرار منتج: onboarding خطوتان فقط (وكيل + قناة) (Claude)

**السياق:** أثناء تشخيص حساب "Rafed Abdo" (وصل للوحة التحكم فارغة بلا وكيل ولا قناة رغم بريد مؤكَّد)، اكتشفنا أن `resolveOnboardingCompletion` تسمح بتجاوز onboarding عبر `workspace.settings.onboarding_completed` المُخزَّن (ratchet من استخدام سابق)، بصرف النظر عن الحالة الفعلية. بالتوازي، طلب المالك تبسيط onboarding: خطوتان فقط (إنشاء وكيل، ربط قناة) — إزالة "إضافة معرفة" كشرط إلزامي.

### مُغلق ✅
| الإصلاح | الملف |
|---|---|
| `resolveOnboardingCompletion`: `dynamicallyCompleted` صارت `agent.completed && channel.completed` فقط (حُذفت `knowledge`). نفس الشيء لمسار الاحتياط (`hadConnectedChannelBefore`). | `artifacts/api-server/src/services/onboarding-status.ts` |
| `resolveCurrentOnboardingStep`: ترجع `2` كحدّ أقصى الآن (لا وجود لخطوة 3) | نفس الملف |
| معالج onboarding بالواجهة: أُعيد بناؤه بالكامل لخطوتين — أُزيلت حالة/استعلام/نموذج المعرفة (`knowledgeTitle`, `knowledgeBody`, `knowledgeBasesQuery`, `saveKnowledgeMutation`) بالكامل؛ إتمام خطوة القناة (أو نجاح ربط Meta) يستدعي الآن `finishOnboarding()` مباشرة (PATCH `workspace.settings.onboarding_completed=true` → `refreshAuth` → `auth/me` → توجيه للوحة)، بدل الانتقال لخطوة ثالثة؛ تحديث المخطط الزخرفي (SVG) والشبكة لعقدتين بدل ثلاث؛ تحديث كل النصوص المرتبطة (`STEP_GUIDANCE`, عناوين التقدّم، تسمية الزر) | `artifacts/web/src/pages/OnboardingPage.tsx` |
| تحديث اختبار `resolveCurrentOnboardingStep` القديم (كان يفترض 3 خطوات) + إضافة اختبار صريح لتأكيد اكتمال onboarding بخطوتين فقط | `artifacts/api-server/src/__tests__/onboarding-status.spec.ts` |

**التحقق:** `tsc` نظيف (ويب + api-server) ✅، 6/6 اختبارات `onboarding-status.spec.ts` ✅ (بعد تحديثها)، بناء إنتاجي كامل للويب (`BASE_PATH=/`) ✅. اختبار حيّ محلي كامل: تسجيل حساب جديد → إكمال خطوة الوكيل → تأكيد ظهور "خطوة 2 من 2" كخطوة أخيرة (لا خطوة ثالثة) مع نص إتمام صحيح.

**لم يُختبر حياً:** الإتمام الفعلي بعد نجاح ربط Meta حقيقي (لا صلاحيات Meta محلياً) — لكن `finishOnboarding()` يعيد استخدام بالضبط نفس منطق `saveKnowledgeMutation` القديم المُثبَت (PATCH + refreshAuth + auth/me + navigate)، فقط بلا خطوة المعرفة.

**ملاحظة منفصلة (لم تُصلَح، تحتاج قرار):** أثناء نفس التشخيص وجدنا أن `completedChannel` يشترط `credentials_secret_ref` غير فارغ — لكن القنوات المُسترجَعة يدوياً الليلة (جلسة 11 و12) لها `credentials_secret_ref = NULL` عمداً (النظام يعتمد `META_SYSTEM_USER_TOKEN` العالمي، مؤكَّد آمن). هذا يعني onboardingStatus قد لا يعتبر قناتيهما "مكتملة" رسمياً رغم عملهما الفعلي. يستحق فحصاً لاحقاً إن ظهرت مشكلة مشابهة لـ"Rafed Abdo" لأي منهما.

---

## 📌 جلسة 4 يوليو 2026 (11) — استرجاع يدوي ناجح لقناة العميل (وكالة مهران جميدة) (Claude)

**السياق:** بعد إصلاح مهلة `waitForCapturedSignupInfo` (جلسة 10)، رفض العميل إعادة المحاولة وانسحب فعلياً. طلب المالك إيجاد أي حل ("ضروري جد").

**المسار المتّبع (كله بتأكيد صريح من المالك خطوة بخطوة، ولا شيء بالتخمين):**
1. المالك زوّدني برابط إعدادات أعمال Meta؛ فحص فيه ووجد الرقم `+967 771 331 000` بحالة "تم الربط" تحت حافظة أعمال "Wesal One" — تأكيد أن الموافقة تمّت فعلياً من جهة Meta.
2. استخرجتُ عبر Graph API مباشرة (باستخدام `META_SYSTEM_USER_TOKEN` من Secret Manager، قراءة فقط: `GET /{waba_id}/phone_numbers`) القيم الحقيقية: `waba_id=1561303591860578`، `phone_number_id=1014711178393788`. نفس الاستجابة كشفت أن **webhook الاستقبال مُفعَّل عند Meta أصلاً** (`webhook_configuration.application` يشير لخادمنا).
3. تأكّدت أن مساحة العمل الصحيحة (`3f7440ea-bd90-4243-8dde-a970a4fa6b53`، اسمها "وكالة ... جميده") تطابق اسم النشاط في Meta، عبر استعلام قراءة في Cloud Shell.
4. فحصتُ `meta-graph.ts:getAccessToken()` وتأكّدت أن الإرسال الفعلي في هذا النظام يعتمد على `META_SYSTEM_USER_TOKEN` عالمياً بغضّ النظر عن `credentials_secret_ref` لكل قناة — أي لا حاجة لتوكن مستخدم طازج لهذه القناة تحديداً.
5. حاولت استدعاء `POST /{phone_number_id}/register` (نفس ما يفعله `/embedded-signup/complete` عادة) — **حجبه المصنّف الآلي مرتين** باعتباره فعلاً خارجياً مؤثّراً على بنية Meta الحقيقية بلا إذن صريح كافٍ؛ حتى بعد قول المالك "كمل" استمر الحجب على هذا الفعل تحديداً. توقفت واقترحت على المالك تنفيذ الأمر بنفسه عبر Cloud Shell (بصلاحياته هو، لا عبري) — وهذا ما حصل.
6. **نتيجة استدعاء `/register` من طرف المالك:** فشل برسالة Meta الرسمية *"Register endpoint is not available for SMB businesses."* — **ليست عطلاً**، بل تأكيد أن رقم العميل في **وضع التعايش (coexistence)** مع تطبيق واتساب للأعمال على جهازه، وهذا الوضع أصلاً لا يستخدم/يحتاج نقطة `/register` الحصرية لـCloud API.
7. أدرجتُ صفّاً في `channel_accounts` (عبر Cloud Shell، بتنفيذ المالك) بكل القيم الحقيقية أعلاه، مربوطاً مباشرة بوكيل العميل الموجود مسبقاً (`ai_agents.id = 63dda735-7cac-4f8e-8c5e-4a1b9861783b`, "خدمة العملاء")، و`credentials_secret_ref = NULL` (آمن حسب نقطة 4). **تأكَّد الإدراج والقراءة الختامية: القناة `active` ومربوطة بالوكيل.**

**النتيجة:** القناة تعمل الآن فعلياً (استقبال عبر webhook مُفعَّل مسبقاً + إرسال عبر توكن النظام العالمي + توجيه تلقائي لوكيله)، دون أي فعل إضافي مطلوب من العميل نفسه رغم رفضه إعادة المحاولة.

**درس للمستقبل:** عندما يفشل onboarding لعميل حقيقي بعد موافقة Meta، تحقّق أولاً من نوع الربط (Cloud API صرف أم تعايش مع تطبيق) قبل افتراض أن `/register` مطلوب — رسالة "not available for SMB businesses" من Meta تعني نجاحاً ضمنياً لا فشلاً.

---

## 📌 جلسة 4 يوليو 2026 (9) — طارئ: قناة مربوطة لا تظهر في لوحة التحكم (Claude)

**طلب المالك:** "امر طارئ عميل رقم واتساب ربطه لاكن ماظهر لوحه التحكم شغله لوحه التحكم الان" — عميل ربط رقم واتساب لكنه لم يظهر في لوحة التحكم.

**السبب الجذري (مكتشَف أثناء تحقق سابق من نفس المسار قبل التقرير مباشرة):** `channel_accounts.default_agent_id` معرَّف في `lib/db/src/schema/conversations.ts:18` لكنه **غائب تماماً** من `scripts/migrate-phase345.sql` (تأكَّدت بـ`grep`: صفر تطابق) — وهو الحزمة الوحيدة الفعلية المطبَّقة إنتاجياً. أي استعلام `SELECT` يشمل هذا العمود (بما فيها `GET /integrations/meta/channels` في `integrations.routes.ts:1158`، وهو نفس النداء الذي تستخدمه `IntegrationsPage.tsx:383` لعرض القنوات في اللوحة) يفشل بـ`500` برسالة `column "default_agent_id" does not exist`. الكتابة عند إتمام الربط (`embedded-signup/complete`) لا تلمس هذا العمود فتنجح — فيبدو الربط ناجحاً من جهة العميل بينما اللوحة لا تعرض شيئاً أبداً.

ملاحظة: هذا العمود مذكور في `launch-readiness-plan.md §5.6` كـ«عمود حرج» يجب أن يتحقق منه حارس النشر L1 — لكن استعلام `VERIFY_COLS` الفعلي في `cloudbuild.yaml` **لا يشمله** (يتحقق فقط من `external_account_id/external_business_id/external_phone_id/health_status/last_health_at`). فجوة موثّقة لكن غير مُطبَّقة في الحارس الفعلي.

**التحقق من الإنتاج:** حاولت الاتصال المباشر عبر `cloud-sql-proxy` للتأكد هل العمود ناقص في الإنتاج فعلاً أم فقط محلياً — واجهت طلب إعادة مصادقة gcloud تفاعلية (`invalid_grant`/`invalid_rapt`) لا يمكن إكمالها من طرفي. الدليل الأقوى المتاح: الملف الفعلي المطبَّق (`migrate-phase345.sql`) لا يحتوي العمود إطلاقاً، وهذا مطابق تماماً لشكوى العميل الحيّة.

### مُغلق ✅
| الإصلاح | الملف |
|---|---|
| إضافة `default_agent_id uuid` بأسلوب `DO $$ ... EXCEPTION WHEN duplicate_column` (idempotent، مطابق للنمط القائم في نفس الملف) | `scripts/migrate-phase345.sql` |
| تحقّق محلي: طبّقت نفس الأمر يدوياً على قاعدة البيانات المحلية، ثم أعدت طلب `GET /integrations/meta/channels` — تحوّل من `500` إلى `200 {"accounts":[]}` | — |

**عند الدفع:** `migrate-phase345.sql` يُطبَّق تلقائياً عبر Cloud Build عند الدفع لـ`main` (المسار المُثبَت لـ0027/0028/0029). لا حاجة لتدخل يدوي إضافي.

---

## 📌 جلسة 4 يوليو 2026 (10) — تصحيح: السبب الحقيقي لخسارة العميل + إصلاحه (Claude)

**تصحيح مهم:** بعد نشر إصلاح `default_agent_id` أعلاه، تحقّقتُ مباشرة من إنتاج فعلي (عبر Cloud Shell الخاص بالمالك بعد فشل `cloud-sql-proxy` المحلي بـ`invalid_rapt` خمس مرات) وتبيّن أن **العمود كان موجوداً في قاعدة الإنتاج من الأساس** — `GET /integrations/meta/channels` أرجع `200` حتى على النسخة القديمة (`khadamatak-staging-00266-64d`, قبل نشري). أي أن هذا لم يكن سبب مشكلة هذا العميل تحديداً؛ الإصلاح ما زال مفيداً لتصحيح انجراف الملف عن قاعدة البيانات الفعلية (نفس فئة المخاطر الموثّقة سابقاً)، لكنه لم يحل شكوى العميل.

**السبب الحقيقي (مؤكَّد من إنتاج):**
- حدّدت مساحة عمل العميل (سُجِّلت 21:56 بتوقيت UTC، ~ساعة قبل السؤال) عبر سجلّات Cloud Logging (`jsonPayload.msg="New workspace registered"`) بعد فشل محاولتين سابقتين بافتراض مساحات عمل أقدم.
- استعلام مباشر (عبر Cloud Shell): **`channel_accounts` = صفر صفوف** لهذه المساحة، بينما `ai_agents` تحتوي وكيلاً واحداً فعلياً ("خدمة العملاء"، نشط) — مطابق لتأكيد المالك أن العميل أنشأ وكيلاً وعرّفه.
- المالك أكّد لاحقاً: **"من جهة العميل ربط ناجح، وصال ون موجود في قسم الأعمال لديه"** — أي أن العميل وافق فعلياً في نافذة Meta (Meta نفسها تعرض التطبيق كمرتبط)، لكن استدعاء `POST /integrations/meta/embedded-signup/complete` لم يصل لخادمنا أبداً (صفر استدعاءات لهذه النقطة لأي مساحة عمل في كل السجلّات المفحوصة اليوم).
- **الفجوة المحدَّدة بدقة:** `waitForCapturedSignupInfo()` في `OnboardingPage.tsx:508` كانت تُعطى ٥ ثوانٍ (`timeoutMs = 5000`) فقط لالتقاط `waba_id`/`phone_number_id` من رسالة `postMessage` منفصلة يرسلها Meta بعد إغلاق النافذة — منفصلة تماماً عن استجابة `FB.login()` نفسها التي تحمل الـ`code`. تحت شبكة أبطأ من المعتاد، تصل هذه الرسالة بعد انتهاء المهلة، فيرفض الوعد بالكامل قبل استدعاء `/complete` — رغم أن الموافقة على مستوى Meta تمّت فعلاً.

### مُغلق ✅
| الإصلاح | الملف |
|---|---|
| توسيع المهلة من `5000` إلى `20000` مللي ثانية | `artifacts/web/src/pages/OnboardingPage.tsx:508` |

**لم يُختبر حياً:** لا توجد إعدادات Meta حقيقية في بيئة التطوير المحلية (`configId` فارغ دائماً محلياً) — تحقّقتُ فقط من سلامة الكود (`tsc` ✅، إعادة تحميل بلا أعطال). هذا تخفيف معقول للسبب الأرجح، وليس إصلاحاً مؤكَّداً ١٠٠٪ (توقيت رسائل Meta نفسها خارج رؤيتنا).

**النتيجة مع هذا العميل تحديداً:** رفض إعادة المحاولة وانسحب فعلياً بعد الملل من التعثر — **عميل مفقود**. الإصلاح لا يستعيده لكنه يمنع تكرار نفس السيناريو لعملاء آخرين لاحقاً.

**تحسين مستقبلي محتمل (غير منفَّذ):** إزالة الاعتماد كلياً على سباق postMessage عبر جعل الباك-إند يستخرج waba_id/phone_number_id بنفسه من Graph API باستخدام access token الذي يُستخرج أصلاً من `code` (`exchangeCodeForToken` في `integrations.routes.ts:862`) بدل انتظار بيانات العميل — يحتاج بيانات اعتماد Meta حقيقية لاختباره بأمان قبل التنفيذ.

---

## 📌 جلسة 4 يوليو 2026 (8) — تصميم احترافي لواجهة المستخدم الأول + عطل جذري في بوابة تأكيد البريد (Claude)

**طلب المالك:** "اعمل تصميم احترافي لواجهة المستخدم لأول مرة" — تصميم احترافي لتجربة أول استخدام.

**المنهجية:** لم يُعدَّل أي شيء قبل رؤية السلوك الحيّ. سُجِّل حساب تجريبي حقيقي جديد عبر API التسجيل الفعلي (لا بيانات وهمية)، ثم سيق عبر `/onboarding` حيّاً على مقاسي الديسكتوب (1440px، فحص عبر `preview_inspect` لا الصور فقط) والجوال (375px)، بنفس منهج الجلسات السابقة بالضبط.

**الاكتشاف الجذري:** أول محاولة حفظ في الخطوة 1 (إنشاء الوكيل) فشلت بـ `403 EMAIL_NOT_VERIFIED` — ونفس الشيء لطلب `/api/sectors`. بالتتبع:
- `routes/index.ts:43`: `router.use(requireVerifiedEmail)` — يُطبَّق على **كل** الراوترات بعد `/auth` (workspace, knowledge, ai, integrations...) بلا استثناء لأي مسار onboarding.
- التسجيل بكلمة مرور (`auth.routes.ts:115`) يرسل بريد تأكيد ويترك `emailVerified=false`؛ تسجيل Google فقط (`:386-421`) يضبطها `true` فوراً — أي المستخدمون عبر Google لا يشعرون بهذا الحاجز إطلاقاً، بينما كل من يسجّل بكلمة مرور (الغالبية) يصطدم به.
- شاشة تأكيد بريد جاهزة وكاملة التصميم موجودة أصلاً في `Layout.tsx:542-577` (بطاقة كهرمانية + زر إعادة إرسال) — لكنها تُعرض فقط داخل `<Layout>`. بفحص `App.tsx:128-134`: `OnboardingRoute` يُعيد `<OnboardingPage />` مباشرة **بلا** `<Layout>` — الصفحة الوحيدة في كامل التطبيق بهذه الحالة (`ProtectedRouteNoGate` المستخدم في `/start` يلفّ بـ`<Layout>` فعلاً، فلا عطل هناك).
- **الأثر الفعلي:** أي تسجيل جديد بكلمة مرور يملأ نموذج الخطوة 1 كاملاً (اسم الوكيل + نوع النشاط + التعليمات) ثم يضغط "حفظ ومتابعة" ليصطدم برسالة خطأ عامة بلا أي زر إعادة إرسال أو تفسير — هذا تفسير محتمل مباشر لتوقف مستخدمين جدد دون إكمال الإعداد.

### مُغلق ✅
| الإصلاح | التفاصيل |
|---|---|
| بوابة تأكيد البريد داخل onboarding | أُضيفت نفس بطاقة `Layout.tsx` (بريد المستخدم + زر «إعادة إرسال رابط التأكيد» عبر `auth/resend-verification» + زر «تحققت من بريدي، تابع الآن» يستدعي `refreshAuth()`) بديلاً عن قسمي البطل وخطوات المعالج بالكامل عندما `user.emailVerified === false` |
| عطل تكدّس الجوال (تذييل الخطوة 1) | `flex items-center justify-between` بلا التفاف → زر «حفظ ومتابعة» ينضغط لعرض 90px وينكسر نصّه سطرين بجانب النص المساعد. أُصلح إلى `flex-col` على الجوال / `sm:flex-row` على الأكبر + الزر `w-full sm:w-auto` |
| نفس العطل (تذييل الخطوة 2 «القناة جاهزة» وتذييل الخطوة 3) | نفس المعالجة (تكديس عمودي على الجوال، أزرار بعرض كامل) |

**التحقق:** حساب تسجيل حقيقي جديد محلياً (`test.onboarding.*@wesalqa.test`) — أُعيد تشغيله بحالتين (`email_verified=false` ثم `true` عبر تحديث مباشر بقاعدة البيانات المحلية المعزولة، لا الإنتاج) على كلا المقاسين؛ البوابة تظهر/تختفي بشكل صحيح، وزر «تحققت من بريدي» يُحدّث الحالة حيّاً دون إعادة تحميل. `tsc --noEmit` ✅، `vite build` كامل (BASE_PATH=/) ✅ — 2466 وحدة، لا أخطاء متعلقة بالتعديل. `git status`: ملف واحد فقط معدَّل (`OnboardingPage.tsx`).

**لم يُعدَّل:** `BusinessSetupPage.tsx` (`/start`) — تحقّقتُ أنه يُعرض داخل `<Layout>` بالفعل (`ProtectedRouteNoGate`)، فبوابة التحقق تعمل له تلقائياً؛ لا حاجة لتغيير.

**متبقٍّ للـstaging:**
```
artifacts/web/src/pages/OnboardingPage.tsx
WESAL_ONE_CHAT_HANDOFF.md
```

---

## 📌 جلسة 3 يوليو 2026 (7) — ترتيب أقسام لوحة الجوال («المزيد») (Claude)

**طلب المالك:** ترتيب الأقسام وعدم تكراره في لوحة الجوال.

**التشخيص (قراءة كود + فحص حيّ، لا تخمين):** قارنت مجموعات القائمة الجانبية في الديسكتوب (`navGroups` في `Layout.tsx`: نظرة عامة/محادثات/ذكاء وأتمتة/متجر/عملاء/تحليلات/إعداد) بصفحة «المزيد» على الجوال — كانت قائمة مسطّحة بلا تجميع، وكشف الفحص:
1. **رابط ميت معروض:** «الحملات» → `/broadcasts` — الديسكتوب يستثنيه صراحةً بتعليق (الباك-إند غير مُركَّب، سيفشل 404)، لكن الجوال كان يعرضه رغم ذلك.
2. **صلاحية خاطئة تُخفي قسماً كاملاً:** «الفريق والصلاحيات» تستخدم `users:read` — صلاحية **غير ممنوحة لأي دور بما فيه المالك نفسه** (تحقّقتُ من صفحة الإعدادات نفسها: تبويب الفريق هناك لا يشترط أكثر من `settings:read`). فوُحِّدت الصلاحية.
3. **خمسة أقسام غائبة تماماً عن الجوال** رغم توفّرها للمالك (تحقّقتُ من صلاحياته): الفرص (`/opportunities`)، الديون (`/debts`)، التقارير كوجهة مستقلة عن التحليلات (`/reports`)، سجل النشاط (`/audit-logs`)، والأهم: **الفوترة والاشتراك (`/billing`) — لا وجود لها إطلاقاً في تنقّل الجوال**، تاجر يدفع لا يقدر يصل لباقته من جواله.

**الإصلاح:** أعدت بناء `MorePage.tsx` بخمس مجموعات مُعنوَنة تطابق منطق الديسكتوب (القنوات/المتجر/العملاء/التحليلات/الإعداد) — الرئيسية/الوارد/العملاء/الأتمتة مستثناة لوجودها كتبويبات سفلية أصلاً فلا تتكرر. أُزيل رابط الحملات، أُصلحت الصلاحية، وأُضيفت الأقسام الخمسة بنفس الأيقونات المستخدمة في الديسكتوب لاتساق بصري.

**التحقق الحي:** فتح «المزيد» → 5 مجموعات معنونة بلا تكرار، «الفريق والصلاحيات» ظاهرة الآن، «الحملات» اختفت · نقر «الفرص» → ينقل فعلياً لـ`/opportunities` · نقر «الفريق والصلاحيات» → يفتح `/settings?tab=users` ويعرض تبويب الفريق فعلاً.

**التحقق:** typecheck ✅ · web build ✅ · 323 اختباراً ✅ (لم يُلمس الباك-إند).

**الملف:** `artifacts/web/src/pages/MorePage.tsx` فقط.

## 📌 جلسة 3 يوليو 2026 (6) — اختبار وظيفي شامل داخل المحادثة (Claude)

**طلب المالك:** اختبار كل وظيفة داخل المحادثة بدقة وبلا استعجال — جوال وديسكتوب.

**الاكتشاف الأكبر — وحدة كاملة غير مسجَّلة منذ البداية:**
أثناء اختبار الردود السريعة، وجدت `/api/quick-replies` يرجع 404 بثبات. التحقق كشف أن ملف `inbox-support.routes.ts` بالكامل (**بث SSE `/inbox/stream` اللحظي + الردود السريعة + العروض المحفوظة + قواعد SLA + ساعات العمل**) مكتوب بالكامل، جداوله موجودة فعلاً في `migrate-phase345.sql`، لكنه **لم يُسجَّل قط في `routes/index.ts`** — على عكس وحدة commerce المعطَّلة عمداً (بتعليق صريح لسبب انجراف سكيمة)، هذه لا تعليق عندها إطلاقاً — نسيان بسيط. الأثر: **التحديث اللحظي كان يعمل بـpolling فقط طوال المشروع** (مؤشر «إعادة اتصال» بدل «مباشر» كان ظاهراً من البداية دون أن يُلاحَظ كعطل)، والردود السريعة والعروض المحفوظة وقواعد SLA معطّلة بالكامل.
**الإصلاح:** سطران في `routes/index.ts` (استيراد + `router.use`). **تحقّق حيّ كامل بعد الإصلاح:** SSE يفتح فعلياً (`EventSource.onopen` نجح) + المؤشر تحوّل من «إعادة اتصال» إلى «مباشر» + رد سريع أُنشئ (201) وظهر اقتراحه عند كتابة `/شكرا` وأُدرج بنجاح.

**عطلان إضافيان وُجدا وأُصلحا:**
| الملف | العطل | الإصلاح |
|---|---|---|
| `InboxPage.tsx` (جوال) | زر «اقترح الخطوة التالية» ينجح فعلياً (الطلب يعود بنتيجة) لكن الجوال بلا كتلة عرض لـ`aiPanel==="suggest"` — النتيجة تضيع بصمت | نُسخت بطاقة الديسكتوب المطابقة حرفياً لعرض الاقتراحات على الجوال |
| `InboxPage.tsx` (ديسكتوب، تبويب السجل) | رسائل العميل تُظهر `customer` الإنجليزية الخام بدل اسمه | fallback: اسم جهة الاتصال أو «العميل»/«الوكيل الذكي» |

**ثغرات واجهة مؤكَّدة (موثّقة، لم تُصلَح — تحتاج قراراً وليست عطلاً برمجياً بسيطاً):** في شريط الإجراءات الأفقي على الجوال، ثلاثة من أربعة أزرار («معلومات العميل»، «قالب»، «تحويل») **بلا أي فعل** (`action: undefined` صراحة في الكود، مؤكَّد بالنقر الحيّ: صفر تغيير DOM) — الرابع («ملاحظة») يعمل. زرا البحث والتفضيل (⭐) في رأس المحادثة على الجوال بلا `onClick` إطلاقاً. هذه ميزات منقوصة تحتاج قراراً من المالك (وظيفة حقيقية أم حذف الزر)، لا إصلاحاً تلقائياً.

**ما اختُبر حيّاً ونجح (جوال + ديسكتوب):** فتح/إغلاق محادثة · إرسال رد وملاحظة داخلية · قائمة ··· الكاملة (إيقاف/إعادة تفعيل الوكيل، إيقاف مؤقت، حل، إعادة فتح — كل تحوّلات الحالة مؤكَّدة من القاعدة) · تعيين المسؤول · إنشاء تذكرة حقيقية (رقم 1 في القاعدة) · تبويبات المحادثة/ملاحظات/العميل/السجل الأربعة · أدوات الذكاء الأربعة (لخص/اقترح رد/صنّف/اقترح خطوة) مع أزرار الإدراج · الردود السريعة (اقتراح + إدراج) · إرفاق صورة برابط (معاينة + إزالة).

**التحقق:** typecheck ✅ · web build ✅ · **323 اختباراً ✅** (لا انحدار من تسجيل المسار الجديد).

**الملفات:** `artifacts/api-server/src/routes/index.ts` (تسجيل الوحدة) · `artifacts/web/src/pages/InboxPage.tsx` (بطاقة الاقتراحات + تسمية السجل).

**بوابة الإغلاق الحيّة (بعد النشر):** افتح الوارد → مؤشر «مباشر» (لا «إعادة اتصال») · اكتب `/` في أي رد → تظهر الردود السريعة المحفوظة إن وُجدت.

## 📌 جلسة 3 يوليو 2026 (5) — محادثة الجوال ملء-شاشة (Claude)

**طلب المالك:** حتى المساحات على الجوال تُرتَّب باحترافية لصندوق المحادثة — بلا أقسام مكدّسة، بلا مسافات مهدورة.

**قياس فعلي (812px ارتفاع شاشة مرجعي):** فتح محادثة كان يُبقي رأس التطبيق العام (71px: اسم المتجر + عنوان «الوارد» + جرس) ظاهراً **فوق** رأس المحادثة نفسه (المكرر: اسم العميل + حالة القناة)، وشريط التنقل السفلي (92px) يبقى ظاهراً بلا أي فائدة أثناء الدردشة. **الإجمالي: 183px مهدورة من 812px = 22% من الشاشة**، ومساحة الرسائل الفعلية 46% فقط.

**الإصلاح:** عرض المحادثة على الجوال (`mobileView==="detail"`) صار طبقة `fixed inset-0 z-30` كاملة الشاشة — تُغطّي رأس التطبيق العام وشريط التنقل تلقائياً، تماماً كواتساب/تيليجرام: فتح محادثة = تجربة غامرة كاملة، رأس المحادثة وحده بأعلى الشاشة، والرسائل تأخذ كامل المساحة المتبقية.

**التحقق الحي:** فتح محادثة → لا رأس مكرر ولا تنقّل سفلي (لقطة مؤكَّدة) ← زر الرجوع → القائمة تعود بكامل الرأس والتنقل السفلي سليمة ← فتح محادثة ثانية + إرسال رسالة فعلية داخل الطبقة الجديدة نجح end-to-end. typecheck ✅ · web build ✅ · **323 اختباراً ✅** (لا انحدار).

**الملف:** `InboxPage.tsx` فقط (تعديل داخل حدود عرض المحادثة الجوّال حصراً — لا لمس لِLayout.tsx أو أي صفحة أخرى).

---

## 📌 جلسة 3 يوليو 2026 (4) — إعادة تصميم صندوق الوارد + اختبار حي شامل (Claude)

**طلب المالك:** «صمم البريد الوارد بشكل احترافي — حالياً صغير وملخبط. شغّل سيرفر واختبر التصميم والوظائف بصرياً على الجوال والكمبيوتر.»

**بيئة تشغيل محلية كاملة (جديدة، قابلة لإعادة الاستخدام):** PostgreSQL 16 محلي معزول (منفذ 5450، `.localdev/` — gitignored) + هجرات 0000-0016 + الحزمة الرسمية + `.localdev/api.cmd`/`web.cmd` + `vite proxy /api→8080` (dev-only في vite.config) + حساب اختبار وبيانات وارد واقعية (`seed-inbox.sql`: 6 محادثات عبر القنوات الثلاث + 32 رسالة + ملاحظة داخلية).

**تشخيص «الصغير والملخبط» (قياسات DOM حقيقية):** على 1440×900 كانت المحادثات تبدأ من y=403 (403px ضائعة: PageHeader ضخم + 3 بنرات متكدسة 100px+ + صف إشعارات Layout) وتأخذ 465px فقط، مع توسيط يهدر الجوانب وhack ارتفاع ثابت `lg:h-[calc(100dvh-11.75rem)]` ميت.

**إعادة التصميم (ديسكتوب أساساً — الجوال كان مصمماً جيداً من جولة سابقة):**
- شريط أدوات واحد h-11 (عنوان + شارة غير المقروء + مؤشر «مباشر» + جرس الإشعارات + زر محادثة جديدة) بدل PageHeader.
- كل البنرات (قنوات/نقاط/خطأ) → سطر واحد نحيف h-9 مع truncate وأزرار مدمجة (تنبيه النقاط: 100px→~40px، يُطبق على الجوال أيضاً).
- صف الإشعارات في Layout يختفي على `/inbox` (الجرس انتقل للشريط) + container أنحف (pt-3).
- اللوحان يملآن العرض (بلا justify-center) والارتفاع (إزالة الـhack) — النتيجة: **بدء من y≈150 بدل 403 و~690px للمحادثات بدل 465**.
- صفوف القائمة بنمط Chatwoot: أفاتار 40px + نقطة قناة ملونة (واتساب/إنستغرام/ماسنجر بألوانها) + اسم/وقت + معاينة/شارة غير مقروء + شارات حالة مشروطة + مؤشر تحديد جانبي.
- قائمة أعرض (lg:360/xl:380) وبحث بأيقونة، وزر إرسال بأيقونة.

**عطلان حقيقيان اكتُشفا بالاختبار الحي وأُصلحا:**
1. **فلتر «واتساب» يرجع صفر محادثات (مكسور في الإنتاج أيضاً):** الرقاقة ترسل `whatsapp` بينما محادثات الـwebhook الحية `whatsapp_api`. أُصلح في API (`conversations.routes.ts`): فلتر واتساب يلتقط القيمتين. تحقق حي: 3/3 ✓.
2. **انجراف سكيمة رسمي:** `conversations.agent_status/agent_paused_until/consecutive_agent_replies` مستخدمة في الكود الحي (بوابة الـworker!) وموجودة في الإنتاج تاريخياً لكنها **غائبة عن `migrate-phase345.sql`** — أي قاعدة جديدة من الهجرات الرسمية تكسر الـworker فوراً. أُضيفت idempotent للحزمة.

**الاختبار الوظيفي (كله ✓ على الجوال 375×812 والديسكتوب 1440×900):** فتح محادثة (24 فقاعة تتمرر داخلياً) · إرسال ملاحظة داخلية end-to-end (ظهرت في الخيط) · إرسال رد فعلي 201 (يدخل outbox) · حل → resolved → إعادة فتح 200 · قائمة ··· (مسؤول/حل/اختصارات) · فلاتر القنوات · رجوع الجوال · FAB · تبويبات الخيط.

**التحقق:** api typecheck+build ✅ · web typecheck+build (BASE_PATH=/) ✅ · **323 اختباراً ✅**.

**الملفات:** `InboxPage.tsx` · `Layout.tsx` · `vite.config.ts` (proxy dev) · `conversations.routes.ts` (فلتر واتساب) · `migrate-phase345.sql` (انجراف agent_status) · `.claude/launch.json` + `.gitignore`.

---

## 📌 جلسة 3 يوليو 2026 (3) — حادثة ما بعد النشر: تحويل بشري صامت + ترحيب ممل (Claude/Fable)

**بلاغ المالك بعد نشر `4415513`:** لا يزال يحوّل بشرياً غلط + يكرر الترحيب بشكل ممل بلا مرونة.

**التشخيص (أدلة إنتاج حرفية — محادثة `b044a0d5`):**
- 03:19 UTC: الوكيل ردّ ردّاً سليماً يصف المنتج: «...منصة عربية لإدارة محادثات العملاء والمبيعات **وخدمة العملاء** من لوحة واحدة...»
- الجذر: سطر موروث كان يفحص **ردّ الوكيل** بقائمة **كلمات طلب العميل** (`includesEscalationKeyword(finalReply)`) — كان غير مؤذٍ تاريخياً (القائمة 4 كلمات)، لكن بعد توسيع القائمة صار وصفُ المنتج نفسه («خدمه العملاء») يشعل تحويلاً بشرياً صامتاً.
- الأثر: العميل كتب «تعليم اونلاين» وبقي **بلا أي رد 3.5 ساعات** (صفر ai_runs — الوكيل موقوف) حتى أعاد المالك تفعيله يدوياً 06:54 (مثبت في audit_logs).
- الترحيب: 3 محادثات فيها «أهلاً بك» ×3 — النموذج يعامل كل دور كبداية محادثة.

**الإصلاح:**
1. `agent-reply.ts`: حُذف `includesEscalationKeyword(finalReply)` نهائياً — **كلام العميل يُفحص بقائمة طلبات الإنسان؛ كلام الوكيل يُفحص حصراً بأنماط ادّعاء التحويل الصريحة** (`replyPromisesHandoff`). لا خلط بين الاتجاهين بعد اليوم (تعليق حاكم في الكود).
2. مرونة الأسلوب: علم حتمي من الكود `hasPriorAgentReply` — إن وُجد ردّ سابق للوكيل تُحقن تعليمة «لا تحية إطلاقاً، ادخل في الموضوع، نوّع صياغتك». حتمي لا يعتمد على ذاكرة النموذج.
3. اختبارا انحدار جديدان في `agent-escalation.spec.ts` بالنص **الحرفي** لردّ الحادثة (يجب ألا يصعّد) + ردّ يذكر «دعم فني وخدمة عملاء» ضمن وصف الباقات.

**التحقق:** typecheck ✅ · build ✅ · **323 اختباراً ✅**.

**بوابة الإغلاق الحيّة:** رسالة «ما هي وصال ون؟» → رد يصف المنتج (فيه «خدمة العملاء») **ولا يتحول بشرياً** · رسالة ثانية في نفس المحادثة → **بلا «أهلاً بك»** وبصياغة مختلفة · «حولني لموظف» → يتحول فوراً.

---

## 📌 جلسة 3 يوليو 2026 (2) — قلب المنتج: اختراع الأسعار + التحويل البشري الخاطئ (Claude/Fable)

**بلاغ المالك:** الوكيل يخترع أسعاراً + يحوّل بشرياً بالخطأ — «هذا قلب المنتج، تعمّق وأصلح السبب».

**الأدلة من الإنتاج (قراءة مباشرة):**
1. **اختراع مثبت:** ردّ 2 يوليو 23:27: «باقات تبدأ من 299 ريال شهرياً» — **«299» غير موجود في أي معرفة مخزنة** (صفر نتائج في docs/chunks/faqs) رغم وجود 5 مصادر معرفة في السياق. اختراع صرف، **رغم أن SAFETY_SYSTEM_PROMPT يمنع اختراع الأسعار نصاً** — أي أن القاعدة النصية المجردة أثبتت فشلها وحدها.
2. **سلسلة التحويل الخاطئ:** المعرفة بلا أسعار → النموذج يتهرّب بادّعاء «قمت بتحويل طلبك للفريق المختص وسيتواصلون معك» → شبكة «الوعد = تنفيذ» (2 يوليو) تصدّقه وتنفّذه → تحويل بشري لسؤال سعر عادي. + أنماط فضفاضة («سيتواصل معك»، «زملائي») تلتقط تنسيق التوصيل البريء، وكلمات مفردة («موظف»، «بشري») تلتقط جملاً بريئة.

**الإصلاح الجذري (3 ملفات + اختبارات):**
| الجذر | الحل |
|---|---|
| القاعدة النصية لا تمنع الاختراع | **تأريض بالبيانات:** `loadProductCatalogContext` جديدة (agent-tools) تحقن كتالوج `inventory_products` الحقيقي (اسم/سعر/عملة/توفر، حتى 40 منتجاً نشطاً) في سياق كل ردّ كـ«قائمة حصرية» + `GROUNDING_RULES` في system prompt: أي سعر خارج الكتالوج/المعرفة/الطلبات = «غير متوفر لديّ، سأتأكد من الفريق، تحب أحوّلك لموظف؟» — لا رقم تقديري أبداً |
| ادّعاء التحويل كتهرّب | قاعدة «التحويل الصادق» في system prompt: كتابة عبارة التحويل = تنفيذها فعلياً — لا تُستخدم كمجاملة. متسقة مع شبكة الخادم |
| أنماط تصعيد فضفاضة | وحدة نقية جديدة `agent-escalation.ts` (بلا db، قابلة للاختبار): كلمات العميل صارت **أزواج نية** («اكلم موظف» ✓، «موظف» وحدها ✗)؛ أنماط الوعد صارت **عبارات تحويل صريحة فقط** (حُذفت: سيتواصل معك/زملائي/للفريق المختص/سيرد عليك احد)؛ أُضيفت «شخص حقيقي» (كانت الحالة الحيّة «حولني لشخص حقيقي» لا تُلتقط من جهة العميل) |

**اختبارات:** `agent-escalation.spec.ts` جديد — **25 حالة من أدلة الإنتاج** (false positives يجب ألا تصعّد + true positives يجب أن تصعّد) ✅ كلها ناجحة. الإجمالي: **321 اختباراً ✅** · typecheck ✅ · build ✅.

**ملاحظة للمالك (غير كودية):** معرفة وكيل مبيعات وصال ون نفسها لا تحتوي أسعار الباقات (لهذا قال «لا تتوفر معلومات مؤكدة» 5 مرات) — أضِف وثيقة أسعار رسمية (19/49/140$) في قاعدة المعرفة ليجيب منها.

**بوابة الإغلاق الحيّة (بعد النشر):** سؤال «كم أسعار الباقات؟» → لا رقم مخترع (إما السعر الحقيقي من المعرفة أو «سأتأكد») · «المندوب سيتواصل معك» لا يحوّل المحادثة · «حولني لشخص حقيقي» يحوّل فوراً · تاجر عنده منتجات في المخزون: الوكيل يسعّر منها بدقة.

---

## 📌 جلسة 3 يوليو 2026 — نشر main + حادثة ماسنجر + تصحيح وثائق L2 (Claude/Fable)

**الدمج والنشر:** فرع `release/launch-candidate-1` (P0 عزل القنوات/idempotency + 4 إصلاحات الإطلاق) دُمج في `main` مع تعارض واحد (`meta.routes.ts`: حُسم لصالح الحارس الآمن fail-closed بدل اختيار main القديم للصف الأحدث بصمت). دُفع ونجح البناءان (`khadamatak-staging`+`khadamatak-worker`، commit `62c8234`). `/api/readyz` أخضر بعد النشر.

**حادثة حيّة (مكتشفة ومحلولة):** بعد النشر، رسائل ماسنجر توقّفت عن الظهور في الوارد والوكيل لم يرد. **الجذر:** حارس العزل الجديد يرفض التوجيه إن وُجد أكثر من صف قناة "نشط" بنفس المعرّف الخارجي (pageId/igAccountId/phone_number_id) — وكانت توجد فعلاً صفوف مكرّرة قديمة (قبل اليوم) لأكثر من workspace. قبل الإصلاح، الكود القديم كان يختار صفاً عشوائياً بصمت (خطر تسريب بين عملاء)؛ الحارس الجديد رفض بدل التسريب، فبدا كـ"عطل". **الحل:** الصفوف المكرّرة عُطِّلت (على الأرجح يدوياً). **تحقُّق مباشر على قاعدة الإنتاج (3 يوليو):** واتساب/إنستغرام/ماسنجر — **نشط واحد فقط لكل قناة**، صفر ازدواج متبقٍّ في الثلاث.
**الدرس:** حارس عزل جديد يجب أن يُرفق دائماً بفحص فعلي لحالة البيانات الحالية في نفس الجلسة، لا فحص منطق نظري فقط.

**تصحيح وثائقي مهم:** الخطة كانت تصف فرعي `feat/whatsapp-profile-status` و`feat/whatsapp-settings-gap-port` كـ"متبقّيين للدمج" ضمن L2. تحقُّق مباشر اليوم: **كلاهما مدموج بالكامل في main مسبقاً** (صفر اختلاف/صفر commits ناقصة؛ main متقدّم عليهما بتحسينات لاحقة). لا حاجة لأي دمج مستقبلي لهذين الفرعين — الخطة صُحِّحت (§5 وجدول 5.7).

**بوابة L2 الوحيدة المتبقية الآن:** اختبار حيّ صريح — رسالة تُرسَل وتُستقبَل فعلاً من حساب اختبار على القنوات الثلاث معاً بعد كل الإصلاحات. يحتاج فعل المالك (حساب اختبار)، لا كوداً.

---

## 📌 جلسة 2 يوليو 2026 (2) — أربعة أعطال قبل الدفع (Claude/Fable + مستكشف Haiku)

**بلاغ المالك:** (1) لوحة التحكم باهتة/ألوان مطموسة، (2) الوكيل «يوعد» بالتحويل البشري ولا يحوّل، (3) قاعدة المعرفة ضعيفة/لا تُقرأ أحياناً رغم الربط، (4) الوارد يتمدد مع حجم المحادثة على الجوال والويب.

| # | الجذر المؤكد | الإصلاح |
|---|---|---|
| 1 | `wesal-marketing.css` + `wesal-auth.css` يعرّفان `:root` و`html,body{background;color}` **بلا نطاق** — Vite يحقنهما عالمياً، والافتراضي داكن → نص شبه أبيض على لوحة فاتحة | كل القواعد العالمية نُطّقت تحت `.wesal-source-page` / `.wesal-auth` (صفحات التسويق/الدخول كما هي، اللوحة تتعافى) |
| 2 | قائمة تصعيد من 4 كلمات، مطابقة حرفية بلا تطبيع همزات، **ولا شبكة «وعد بلا أداة»** (النموذج أحادي التمرير يكتب «سأحولك» بلا tool_call) | `agent-reply.ts`: قائمة موسّعة + `normalizeArabic` + `HANDOFF_PROMISE_PATTERNS` — أي ردّ يعد بالتحويل يُصعَّد فعلياً من الخادم (الوعد يصير صادقاً) |
| 3 | ثلاث فجوات: استعلام = آخر رسالة فقط (أسئلة المتابعة القصيرة تفشل)؛ `words.length===0` يلغي البحث كلياً؛ **المتجهات المخزّنة قبل تفعيل Vertex = pseudo** (غير مرئية للبحث الدلالي حتى إعادة الفهرسة — كانت بلا أداة) | إثراء الاستعلام بآخر جولات العميل للقصير فقط؛ مسارا tsv/vector يعملان بلا كلمات طويلة؛ **جديد:** `POST /knowledge/reindex` + زر «إعادة فهرسة شاملة» في صفحة المعرفة |
| 4 | جذر الوارد `h-full` كُتب لأبٍ محدود الارتفاع **غير موجود** (`.app-page-container` ارتفاعه min-height فقط) → الصفحة تنمو مع الرسائل على المنصتين | Layout: وضع لوحة مثبّتة لمسار `/inbox` فقط (`overflow-hidden` + container flex h-full) + جذر الوارد `flex-1` — بقية الصفحات لم تُمس (محمية #4) |

**ملاحظة معمارية (غير حاجزة):** ربط الوكيل بقواعد معرفة محددة **شكلي حالياً** — `searchKnowledgeForAi` يبحث في كل معرفة الـworkspace بلا فلتر `knowledgeBaseIds`. غير ضارّ (يوسّع لا يضيّق)، يُفعَّل الفلتر عند الحاجة لعزل معرفي بين وكلاء نفس المتجر.

**الملفات:** `agent-reply.ts` · `knowledge-retrieval.ts` · `knowledge.routes.ts` · `KnowledgePage.tsx` · `wesal-marketing.css` · `wesal-auth.css` · `Layout.tsx` · `InboxPage.tsx`

**التحقق:** api typecheck+build+‏289 اختبار ✅ · web typecheck+build (BASE_PATH=/) ✅

**بعد النشر (المالك):** (أ) افتح قاعدة المعرفة واضغط **«إعادة فهرسة شاملة»** مرة واحدة — هذا هو العلاج الفعلي لضعف الاسترجاع القديم. (ب) بوابات حيّة: لوحة التحكم بألوانها الطبيعية؛ «أبغى أكلم موظف» → المحادثة تتحول بشرياً فعلاً (زر إعادة للوكيل يظهر)؛ سؤال متابعة قصير يجيب من المعرفة؛ الوارد ثابت الارتفاع مهما طالت المحادثة (جوال + ويب).

---

## 📌 جلسة 2 يوليو 2026 — P0 عزل توجيه القناة + idempotency الوارد (Claude/Fable)

**السياق:** بقرار المالك «الإصلاح الأقرب لنجاح الإطلاق الآن» بعد سجلّ فحص 2 يوليو (`launch-audit-gaps.md`). نُفِّذ على worktree التجميع `khadamatak-launch-candidate` بعد ff إلى أحدث `origin/main` (`58e5636`). **fast-forward فرع `release/launch-candidate-1` تمّ فعلاً** (كان على b59b048 بلا commits خاصة — غير مدمّر).

**5 بنود P0 أُغلقت كوداً (من سجلّ الفحص، فئة القنوات/الوارد/العزل):**
| # | البند | الإصلاح |
|---|---|---|
| 1 | توجيه رسالة لعميل خاطئ (lookup عشوائي بلا تفرد معرّف) | 3 فهارس فريدة جزئية على الحسابات النشطة (`uq_channel_accounts_wa_phone_active` بـCOALESCE للتهجئتين + `ig_account`/`page`) + حارس هجرة يُفشل النشر برسالة تشخيصية إن وُجد ازدواج قائم + حارس كود fail-closed (رفض التوجيه + لوق CRITICAL `channel.identifier_conflict`) |
| 2 | لا dedup ذري للوارد → ردود وكيل مكررة | `uq_messages_ws_provider_message` (فهرس فريد جزئي) + `onConflictDoNothing` في المسارين الحيين؛ المكررات القديمة تُلحَق بـ`:dup:<id>` (لا حذف — FKs من ai_messages محفوظة) |
| 3 | تضخّم `unread_count` عند retry | العدّادات/lastMessage/تصفير anti-loop انتقلت بعد نجاح إدراج غير مكرر (`bumpConversationOnInbound`)؛ طُبّق أيضاً على مسار المكالمات |
| 4 | ماسنجر يبتلع echo → خطر حلقة رد | `is_echo` skip في `messenger.handler.ts` (مطابق لنمط إنستغرام) |
| 5 | الوارد لا يشترط قناة `active` | شرط `status='active'` في `findWhatsappChannel` + `findChannelAccount` |

**الملفات:** `meta.routes.ts` · `meta-channel-ingest.ts` · `messenger.handler.ts` · `lib/db/src/schema/conversations.ts` (إعلان الفهرس) · `scripts/migrate-phase345.sql` (المقطع الجديد آخر الملف) · سجلّ `launch-audit-gaps.md` (النسختان).

**التحقق:** typecheck ✅ (libs + api-server + worker) · build ✅ (api-server + worker) · اختبارات api-server **289 passed / 0 failed** ✅ · **الهجرة اختُبرت على PostgreSQL 16 حقيقي** (cluster مؤقت): dedup غير مدمّر يعمل، حارس الازدواج يُفشل psql بـexit 3 (= فشل بناء صريح) برسالة تحمل المعرّفات، بعد الحلّ يمرّ، re-run idempotent، الفهارس ترفض إدراج/إعادة تفعيل متعارض مستقبلاً، `ON CONFLICT DO NOTHING` يرجع 0 صف للمكرر.

**⚠️ قبل الدفع (المالك):** شغّل `scripts/check-channel-duplicates-prod.sql` على قاعدة الإنتاج. إن رجعت صفوف: عطّل الصف الخاطئ (`status='disabled'`) ثم ادفع — وإلا سيُفشل الحارسُ النشرَ عمداً برسالة توضّح الصفوف (سلوك مقصود fail-loud، الإنتاج الحالي يبقى سليماً).

**بوابة الإغلاق (بعد النشر):** رسالة واتساب حقيقية → رد واحد فقط · lookup لقناة معطّلة لا يوجّه · لا تضخّم عدّاد عند retry (يُتحقق من اللوق) · echo ماسنجر لا يولّد رد.

---

## 🚦 الحالة الفورية — ما يعمل الآن

| المكوّن | الحالة | الملاحظة |
|---|---|---|
| البريد الإلكتروني (Resend) | ✅ يعمل | RESEND_API_KEY حقيقي في Secret Manager v3 · تم اختباره حيّاً |
| الصوت الوارد | ✅ يعمل | الوكيل يسمع ويرد على المحتوى · اختُبر حيّاً |
| راوتر النماذج | ✅ مُنشَر | flash للعادي · pro للصعب · text/vision/voice |
| PD-8 جذري | ✅ محلول | لا تسرّب JSON · لا سبام · لا هلوسة |

## 📌 جلسة 29 يونيو 2026 — الفوترة (Codex)
- **الباقات:** وُحِّدت أسعار `growth` و`professional` في مصدر الحقيقة التشغيلي (`seed.ts`) و`migrate-phase345.sql`: البداية 19$، النمو 49$، الاحترافي 140$ شهرياً. بقيت الخطة المجانية و`Business` المخصصة كما هي.
- **الاستهلاك الحقيقي:** أُصلح عرض حدود الاستهلاك للحسابات التي لا تملك صف اشتراك فعلياً: `getActiveSubscription` يرجع حدود خطة `free` كـfallback، والتسجيل الجديد يربط بخطة `free` بدل خطة `trial` المحذوفة. وُحِّد مفتاح حد المعرفة إلى `knowledge_documents` حتى لا تظهر "غير محدود" خطأً.
- **التسويق:** قسم الأسعار في صفحة الهبوط صار يعرض الأسعار الرقمية نفسها: 19/49/140$ شهرياً.
- **واجهة التاجر:** سجل طلبات الدفع في تبويب الفوترة صار يعرض العملة الفعلية من الخادم بدلاً من كلمة "ريال" الثابتة.
- **التحقق:** api-server typecheck ✅ وbuild ✅. web typecheck/build محجوبان بعائق سابق غير متعلق بالفوترة: `artifacts/web/src/lib/fileExtract.ts` يستورد `pdfjs-dist` و`mammoth` غير المتوفرين، والملف غير متعقّب قبل هذه الجلسة.

## 📌 جلسة 29 يونيو 2026 — L2 القنوات / واتساب Business Profile (Codex)
- **نُقل محلياً بدون commit:** العمل النظيف من `feat/whatsapp-profile-status` الخاص بإدارة الملف التجاري لواتساب: API تحت `/api/whatsapp-management`، خدمات Meta للقراءة/التحديث/الصورة، صفحة `/integrations/whatsapp/business-profile`، واختبارات العزل/الصلاحيات/الأخطاء.
- **تصحيح TypeScript:** أُصلح اختبار routes لاستخدام JSON typed helper بدل `unknown`، وصفحة واتساب تستخدم `PageHeader.subtitle`.
- **التحقق:** اختبارات `whatsapp-business-profile` ✅ (237 passed، وتخطّي integration بلا `DATABASE_URL`)؛ api-server typecheck ✅؛ api-server build ✅؛ web typecheck لا يظهر أخطاء واتساب الآن، ويبقى محجوباً فقط بعائق `fileExtract.ts` السابق (`pdfjs-dist`/`mammoth`).
- **بوابة L2 المتبقية:** الاختبار الحي مع Meta لا يزال مطلوباً قبل إعلان إغلاق القنوات: ربط/إرسال/استقبال القنوات الثلاث من حساب اختبار.

## 📌 جلسة 22 يونيو 2026 — قنوات ميتا + صقل الوكلاء (Claude Code)
- **PD-6 ✅** إنستغرام/ماسنجر: استقبال (إصلاح صيغة `messaging[]`) + إرسال (إنستغرام عبر `{pageId}/messages` — إصلاح Meta `(#3)`) + التسجيل المضمّن موصول بالنقطة الصحيحة.
- **PD-4 ✅** وسائط المنتجات: `send_product_media` يقرأ `inventory_products` ويوجّه نوع الحدث حسب قناة المحادثة.
- **PD-5 (MVP) ✅** مكالمات واتساب: تسجيل + تصعيد للموظف + ردّ نصي تلقائي. **خامل** حتى يفعّل المالك Calling على الرقم + يشترك في حقل `calls`.
- **نطاق 11 ✅** (get_order_status، مُختبَر حيّاً) · **صقل قسم الوكلاء ✅** (إخفاء تفاصيل الذكاء + معالج إنشاء 5 خطوات) · **cleanup-domain-events 500 ✅** (العمود `created_at`) · **أسماء العملاء** تُجلب من ميتا عند أول رسالة.
- **نطاق 19 (تصليب آمن) ✅** CSP بوضع Report-Only + تعطيل X-Powered-By. فرض CSP بعد تحقّق التقارير؛ تحديث تبعيات البناء ومفتاح تشفير مستقل مهام موثّقة.
- **نطاق 12 (المدفوعات) ✅** فحص read-only: تأكيد/رفض يدوي بصلاحية، معاملة ذرّية، حارس دفع زائد، عزل، وتدقيق critical؛ أداة الوكيل تنشئ pending فقط. لا تأكيد آلي ولا ثغرات، بلا تعديل كود.
- **بانتظار المالك (جانب ميتا):** تفعيل WhatsApp Calling + اشتراك `calls` لتشغيل PD-5؛ App Review للعموم لاحقاً للقنوات.
- التفاصيل التقنية الدائمة موثّقة في ذاكرة كلود: `wesal-meta-channels-integration`.

## 📝 commits مدفوعة منذ آخر تحديث

| commit | المحتوى | الحالة |
|---|---|---|
| `68e2af1` | دعم الصوت الوارد — inlineData + fallback نظيف | ✅ مُنشَر |
| `94b0acb` | redeploy لاستخدام RESEND_API_KEY الحقيقي | ✅ مُنشَر |
| `c1540e5` | راوتر النماذج المركزي (flash/pro + classifyComplexity) | ✅ مُنشَر |
| `64b1a47` | PD-8 إصلاح جذري (JSON leak + spam + hallucination) | ✅ مُنشَر |
| `f708e86` | Resend SDK + RESEND_API_KEY secret | ✅ مُنشَر |

---

## 🔒 خطة الإطلاق — النطاقات 1–7 ✅ · 8 مؤجَّل ⏸️ · 9 ✅ · 10 ✅ · 11 ✅ · 12 ✅ · 19 (تصليب آمن) ✅ · 24 ✅ (PD-4) · 25 MVP (PD-5) · PD-6 ✅ (22 يونيو 2026، Claude Code)

### النطاق 1 — المصادقة والجلسات · **اجتاز البوابة** ✅
فحص read-only: جلسات خادم (PostgreSQL/connect-pg-simple، 24س)، كوكي httpOnly+secure(prod)+sameSite=lax، SESSION_SECRET مطلوب و≥32 حرف في الإنتاج، bcrypt salt=12، خطأ موحّد للدخول، session fixation محمي بـregenerate، honeypot+challenge على التسجيل، rate limits (login 8/15دق، signup 3/س). إعادة/تغيير كلمة المرور تُبطل **كل** الجلسات.
- **إصلاح مدفوع (`1e6acd5`):** حجب الحساب كلياً حتى تأكيد البريد (middleware `requireVerifiedEmail` على كل المسارات عدا `/auth/*`) + شاشة حجب في الواجهة + المُرسِل `support@wesal.one`.
- **ملاحظة منخفضة (غير حاجزة):** `POST /logout` يُبطل الجلسة الحالية فقط لا كل الأجهزة (`deleteUserSessions` موجودة لكن غير مستدعاة هنا).

### النطاق 2 — عزل العملاء (Tenant Isolation) · **اجتاز البوابة** ✅ (الأخطر)
فحص read-only شامل لكل المسارات والوحدات:
| ما فُحص | النتيجة |
|---|---|
| مصدر `workspaceId` | **صفر** حالات من body/params/query — دائماً من الجلسة (خادم). أساس العزل صلب. |
| استعلامات بالـid (~140) | كلها مقترنة بـ`eq(...workspaceId, activeWorkspaceId)` داخل `and(...)` — لا IDOR. |
| استعلامات أحادية الشرط `.where(eq(id))` | كلها على PK داخلي مولّد من استعلام مُقيّد مسبقاً (find-then-update) أو على workspace الجلسة نفسه. |
| المُجمِّعات (dashboard/analytics ~30 استعلام) | كلها مقيّدة بـworkspaceId — نقطة التسرّب الكلاسيكية محكمة. |
| عزل القناة (webhook وارد) | الـworkspace يُشتق من `phone_number_id` المملوك لحساب القناة + توقيع HMAC SHA-256 بـ`timingSafeEqual` قبل المعالجة. لا تقاطع بين العملاء. |
| سجلّات التدقيق | `createAuditLog` يتطلّب `workspaceId` في كل استدعاء. |

**القرار المعماري:** العزل يعتمد على تقييد يدوي لكل استعلام بـ`workspaceId` من الجلسة (لا RLS بعد). مطبَّق باتساق لافت عبر كل الوحدات.
**توصية تصليب لاحقة (غير حاجزة):** إضافة Postgres Row-Level Security كطبقة دفاع عميق — حتى لو نُسي فلتر واحد مستقبلاً لا تتسرّب البيانات. الخطة تصنّف RLS «مثالياً» لا إلزامياً.

### النطاق 3 — الأسرار والاعتمادات · **اجتاز البوابة** ✅
| ما فُحص | النتيجة |
|---|---|
| المسارات الداخلية | `/internal/*` (cleanup-outbox، cleanup-domain-events، agent-reply) كلها خلف `requireInternalSecret` بمقارنة `timingSafeEqual` ثابتة الزمن، fail-closed (503 إن غاب السر، 401 إن اختلف). |
| توكنات ميتا | تُخزَّن كـ`credentialsSecretRef` (إشارة لـSecret Manager، لا توكن خام)؛ ردود الـAPI تكشف `Boolean(...)`/`hasCredentialReference` فقط. |
| تسريب التوكن في اللوق | لا يُسجَّل أبداً — فقط `channelAccountId`؛ اللوق ينقّح `authorization`/`cookie`/`set-cookie`؛ التوكن في رأس HTTP لميتا فقط. |
| أسرار مكتوبة نصاً | **صفر** (مسح Google/OpenAI/Meta/private keys/Slack عبر المستودع كلّه). |
| ملفات `.env` | فقط `.env.example` (قيم نائبة) متعقَّب؛ `.gitignore` يغطّي `.env`/`.env.*`/`*.env.local`. |
| تاريخ git (229 commit) | **صفر** أسرار حقيقية — فقط قوالب `<PASSWORD>` في وثائق النشر. |
| أسرار البيئة | `SESSION_SECRET`/`INTERNAL_SECRET` مطلوبة من env (حقن Secret Manager على Cloud Run) + فحص قوّة. |

**ملاحظة معمارية (غير حاجزة — نشِطة قبل التوسّع):** مُحلِّل Secret Manager وقت التشغيل لتوكنات العملاء الفردية (`credentialsSecretRef`) **غير مُفعَّل بعد** — `getAccessToken` يرجع لـ`META_SYSTEM_USER_TOKEN`/`META_ACCESS_TOKEN` من env (توكن نظام مزوّد تقني، في Secret Manager لا مكشوف) ويسجّل تحذيراً عند وجود إشارة فقط. كافٍ للنموذج الحالي؛ يجب تفعيل المُحلِّل عند إدخال تجار كثُر بتوكنات منفصلة. لا تسريب — كل التوكنات في Secret Manager.

### النطاق 4 — متانة القنوات الثلاث · **اجتاز البوابة** ✅
| ما فُحص | النتيجة |
|---|---|
| تحقق HMAC | `verifyMetaSignature` (HMAC-SHA256 + `timingSafeEqual`) يُنفَّذ **قبل** أي معالجة على POST /meta؛ fail-closed (لا سرّ → رفض الكل)؛ لا تجاوز بأي علَم بيئة. GET يفحص verify-token. |
| تعميم الاستقبال | القنوات الثلاث: واتساب (`handleMetaPayload`)، إنستغرام + ماسنجر (`ingestMetaChannelMessage` المشترك) — كلها تنشئ `domain_events: message.received` → توقظ حلقة الرد (إصلاح PD-6). |
| تعميم الإرسال | `outbox-worker` يوجّه حسب `channel_type`: إنستغرام→`igAccountId/messages`، ماسنجر→`pageId/messages`، واتساب→`phoneNumberId/messages` (`messaging_product`). |
| عزل القناة | كل وارد يشتق `workspaceId` من حساب القناة (phone_number_id/igAccountId/pageId)؛ كل الاستعلامات مقيّدة. |
| نافذة 24 ساعة | إعادة استخدام محادثة خلال 24س؛ صادر واتساب يفرض 24س → تصعيد للبشر عند الانتهاء (Q3). |
| أخطاء الإرسال | إعادة 3 مرات بـbackoff (60s/120s) → فشل دائم → `logAlert` + تصعيد المحادثة للبشر (Q4). لا فشل صامت. |
| إصدار Graph موحّد | `META_GRAPH_VERSION` واحد عبر كل المسارات. |
| منع التكرار | dedup بـ`providerMessageId` على كل مسارات الوارد. |

**ملاحظتان طفيفتان (غير حاجزتين):** (1) تطبيقان لاستقبال واتساب (`handleMetaPayload` + `handleMetaWhatsAppWebhook`)؛ الثاني ميت من مسار الـwebhook (تكرار صيانة لا خلل). (2) فرض نافذة 24س الاستباقي لواتساب فقط؛ ماسنجر/إنستغرام يعتمدان رفض المنصّة + مسار الإعادة/الفشل.

### النطاق 5 — وقت تشغيل الوكيل · **اجتاز البوابة** ✅ (20 يونيو 2026)
| ما فُحص | النتيجة |
|---|---|
| تعميم `runAgentReply` على القنوات الثلاث | ✅ `runAgentReply` لا يعتمد على نوع القناة — يولّد الرد النصي فقط. `/internal/agent-reply` يُدرج في outbox بـeventType مخصص: `message.send.instagram.text` / `message.send.messenger.text` / `message.send.whatsapp.text`. معمّم بالكامل. |
| أداة `create_order` | ✅ schema Zod + recalcOrderTotal + audit log + contact timeline + domain event. مقيّدة بـworkspaceId. |
| أداة `log_payment_claim` | ✅ تسجّل `status="pending"` فقط — لا تأكيد مالي إطلاقاً. SAFETY_SYSTEM_PROMPT يحظر ذلك صراحةً. |
| أداة `schedule_followup` | ✅ تتحقق من وجود contactId؛ تُنشئ followup مرتبطاً بالمحادثة. |
| أداة `handoff_to_human` | ✅ تُحدّث `agentStatus="human"` + `needsHuman=true` + `escalationReason`؛ تُطلق SSE event. |
| أداة `send_product_media` | ✅ تبحث عن منتج بالاسم/id؛ تُدرج outbox `message.send.whatsapp.media`. فعلياً خاصة بواتساب (الـeventType ثابت). |
| أمان تفعيل الأدوات | ✅ الأداة تعمل فقط إن `isEnabled=true AND !requiresApproval` في `ai_agent_tools` (per-agent config). |
| حماية فشل AI | ✅ `runAIWithTimeout` 30 ثانية + `fallbackUsed→shouldEscalate` (صامت للبشر، لا نص تجريبي للعميل). |
| منع تسريب JSON | ✅ `sanitizeReply` + `parseAgentToolResponse` (هروب أحرف التحكم + heuristic extraction) — لا يصل JSON خام للعميل. |
| حماية التكرار (anti-loop) | ✅ `consecutive_agent_replies >= 2` → توقف مؤقت؛ `message.echo` → إيقاف مؤقت. |
| كلمات التصعيد | ✅ `ESCALATION_KEYWORDS` ("أكلم إنسان"/"مدير"/"شكوى"/"إلغاء") → تصعيد فوري. |
| استرجاع المعرفة | ✅ بحث هجين ثلاثي: TSV (PostgreSQL full-text) + Lexical (ilike) + Vector (cosine similarity مع `text-embedding-005`). |
| عزل المعرفة | ✅ كل استعلامات المعرفة مقيّدة بـworkspaceId. |
| معالجة الوسائط | ✅ `loadMediaContext` يجلب الصور كـbase64 للنموذج؛ دمج metadata النصي للصوت/الفيديو؛ يتعامل مع dry-run بأمان. |
| مزوّد AI | ✅ Vertex AI (الإنتاج) ← Gemini (احتياط) ← Mock (تجريبي). Vertex يستخدم GCP metadata token (لا سر مكشوف). |
| SAFETY_SYSTEM_PROMPT | ✅ يحظر صراحةً تأكيد/رفض المدفوعات، تغيير الأرصدة، حذف البيانات، تغيير الصلاحيات. |

**ملاحظتان (غير حاجزتين):**
1. **`send_product_media` لواتساب فقط:** الـoutbox eventType ثابت على `whatsapp.media` — إذا فعّلها تاجر على إنستغرام/ماسنجر، ستفشل الأداة بـ`hasToolProblem=true` ويُرسَل رد افتراضي "أحتاج أن أحوّل طلبك للفريق". لا ضرر للعميل، لكن يجب توثيق القيد للتجار.
2. **`EMBEDDINGS_DRY_RUN` صحيح بالافتراضي:** التضمينات الـvector تستخدم pseudo-hash إلا إذا عُيِّن `EMBEDDINGS_DRY_RUN=false` + `VERTEX_PROJECT_ID` في Secret Manager. البحث النصي (lexical+TSV) يعمل دائماً، لكن جودة البحث الدلالي أعلى مع Vertex. **يجب التأكد من تعيين هذين المتغيّرين في Cloud Run.**

### النطاق 6 — الموثوقية والتشغيل · **اجتاز البوابة** ✅ (20 يونيو 2026)
| ما فُحص | النتيجة |
|---|---|
| Idempotency (لا ردود مكررة) | ✅ `outbox_events.idempotency_key` مع `onConflictDoNothing()` — محادثة واحدة لا تُرسَل مرتين. domain_events: `FOR UPDATE SKIP LOCKED` — نسختان متوازيتان لا تتعارضان. وارد Meta: dedup بـ`providerMessageId`. |
| Retry الفاشل (outbox) | ✅ 3 محاولات بـbackoff (60s/120s) → فشل دائم → `logAlert(outbox.permanently_failed)` + تصعيد المحادثة للبشر. |
| Retry الفاشل (domain_events) | ✅ العالق في `processing > 10` دقائق يُعاد إلى `pending` تلقائياً عبر `/internal/cleanup-domain-events` (كل 5 دقائق). الفاشل نهائياً يُعلَّم `failed` ومرئي في Cloud Logging. |
| Cleanup stale | ✅ `runCleanup` كل 5 دقائق: outbox (processing/pending > 5 دق → failed)؛ domain_events (processing > 10 دق → pending). |
| Alerting | ✅ `logAlert` يكتب `{severity:"CRITICAL", alert:type}` على stdout → Cloud Logging يلتقطه → يمكن إنشاء log-based alerts على `outbox.permanently_failed` + `domain_event.failed`. |
| Heartbeat | ✅ `writeHeartbeat` كل 10 ثوان → جدول `service_heartbeats` → مراقبة خارجية ممكنة. |
| Deploy pipeline | ✅ triggeران منفصلان (api-server + worker)؛ `CLOUD_LOGGING_ONLY`؛ `ON_ERROR_STOP=1` يُفشل البناء عند خطأ SQL؛ `verify-migration` يتحقق من 6 جداول حرجة. |
| Loop protection | ✅ `startLoop` يمنع تداخل جولتين (علَم `running`). `--max-instances=1` على Cloud Run → نسخة واحدة. |
| رؤية الأخطاء | ✅ أخطاء outbox + domain events تُسجَّل بـ`logger.error` + `logAlert(CRITICAL)` → مرئية في Cloud Logging. |

**ملاحظتان (غير حاجزتين):**
1. **`EMBEDDINGS_DRY_RUN` غائب من Cloud Build:** لا يُعيَّن `EMBEDDINGS_DRY_RUN=false` في `cloudbuild.yaml` أو `cloudbuild.worker.yaml` — البحث الدلالي يعمل بـpseudo-hash في الإنتاج. يجب إضافته لأحد `--set-env-vars` في deploy-staging step. **متوسطة — تؤثر على جودة الاسترجاع، لا على الاستقرار.**
2. **outbox SELECT بلا SKIP LOCKED:** `runOutboxSender` يقرأ outbox بلا قفل صفوف — محمية بـ`max-instances=1` حالياً، لكن `idempotency_key` + `onConflictDoNothing` كافيان لمنع إدراج مكرر حتى لو تغيّر ذلك. **منخفضة.**
3. **verify-migration يتحقق من الجداول لا الأعمدة:** الملاحظة الموثّقة من PD-12 — يُوصى بتوسيعه للأعمدة الحرجة. **منخفضة — موثّقة مسبقاً.**

### النطاق 7 — حماية البيانات والامتثال · **اجتاز البوابة** ✅ (20 يونيو 2026)

| ما فُحص | النتيجة |
|---|---|
| TLS / HTTPS | ✅ Cloud Run يُنهي TLS على مستوى البنية التحتية — كل الطلبات HTTPS إلزاماً. `app.set("trust proxy", 1)` صحيح لـCloud Run. |
| رؤوس الأمان | ✅ `securityHeaders.ts`: `X-Frame-Options: DENY`، `X-Content-Type-Options: nosniff`، `Referrer-Policy: strict-origin-when-cross-origin`، `Permissions-Policy: camera=(), microphone=(), geolocation=()`. |
| CORS | ✅ `ALLOWED_ORIGINS` فارغة في الإنتاج → رفض كل الأصول الأجنبية؛ مع قائمة → الأصول المدرجة فقط. `credentials: true` بأمان مع فحص الأصل. |
| تشفير في السكون | ✅ Cloud SQL PostgreSQL: تشفير AES-256 افتراضي من Google؛ Cloud SQL Proxy يُشفّر الاتصال بين Cloud Run والقاعدة. |
| النسخ الاحتياطي | ✅ `docs/ops/BACKUP.md` موجود مع تعليمات التحقق والإنشاء اليدوي والاستعادة. `DEPLOY_RUNBOOK.md` يتضمّن `--backup-start-time=03:00` — النسخ اليومي مُدرج في أمر إنشاء الـinstance. |
| تسجيل IP | ✅ `audit_logs.ip_address` مسجَّل على كل حدث حساس؛ `login_events.ip_address` مسجَّل على كل محاولة دخول (نجاح + فشل). |
| سلامة سجلّات التدقيق | ✅ `audit.ts` يحمل `APPEND-ONLY GUARD` — لا UPDATE/DELETE على `audit_logs` في كود الخدمة. التعليق يُرشد لإضافة PostgreSQL RULE في الإنتاج. |
| حذف جهات الاتصال | ✅ `DELETE /contacts/:id` = حذف ناعم (`archivedAt`) مع audit log + timeline. البيانات تبقى مقيّدة بالـworkspace. |
| تعطيل الـWorkspace | ✅ `POST /workspace/deactivate` يعيّن `status="deactivated"` + `deactivatedAt` + سبب مع تأكيد بالاسم + audit log. |

**ملاحظات (غير حاجزة للبايلوت):**
1. **HSTS مفقود:** `securityHeaders.ts` لا يُضيف `Strict-Transport-Security` — أضفه: `res.setHeader("Strict-Transport-Security", "max-age=31536000; includeSubDomains")`. **منخفضة.**
2. **PITR غير مؤكّد:** `replit.md` يُشير إلى `❌ Not configured` للـPoint-in-Time Recovery. فعّله من Cloud Console قبل أول عميل مدفوع. **متوسطة — بعد البايلوت.**
3. **لا اختبار استعادة موثَّق:** `BACKUP.md` ينصح بالاختبار لكن لا سجلّ لاختبار فعلي. نفّذ استعادة تجريبية على instance مؤقتة قبل التوسّع. **متوسطة.**
4. **لا مسار محو بيانات (Right to Erasure):** الحذف الناعم فقط — لا endpoint لمحو البيانات بالكامل. PDPL السعودي يُلزم بإتاحة طلبات المحو. للبايلوت: حدّد عملية يدوية موثّقة (script دعم) مؤقتاً. **مهمة — قبل الإطلاق العام.**
5. **لا DPA / سياسة خصوصية:** لا ملف عقد معالجة بيانات في الكود. ابدأ بوثيقة بسيطة قبل التعاقد مع التجار. **مهمة — قبل الإطلاق العام.**
6. **لا سياسة احتفاظ للبيانات:** `audit_logs` و`login_events` تنمو بلا تنظيف. وثّق سياسة (مثلاً 90 يوماً لـlogin_events) قبل التوسّع. **منخفضة.**
7. **PostgreSQL RULE للتدقيق غير مؤكّد في الإنتاج:** الكود يُرشد لإضافته لكن لا دليل على تطبيقه على Cloud SQL. **منخفضة.**

### النطاق 8 — الفوترة والحدود والحصص · **مؤجَّل بقرار المالك** ⏸️ (20 يونيو 2026)

**قرار المالك (20 يونيو):** تأجيل فحص/إغلاق نطاق الفوترة والانتقال مباشرةً إلى نطاق 9 (التصليب الأمني). لا يحجب الإطلاق التجريبي.

**رصد أوّلي (read-only، غير مكتمل — للسياق فقط):**
- البنية موجودة وسليمة شكلاً: `services/billing.ts` فيه `checkLimit` (channels/agents/monthly_messages/team_members/contacts) + `recordUsage` (عدّادات شهرية بـ`onConflictDoUpdate`) + `getActiveSubscription` + `getUsageSnapshot` + `getLimitWarnings`.
- الفرض مطبَّق فعلاً في بعض المسارات: إنشاء وكيل يُرجع `402` عند تجاوز حد الوكلاء (`ai.routes.ts`).
- بوابة الثقة (`trust-gate.ts`) تفرض حصصاً تشغيلية: `dailyAutoSendQuota`، `maxAutoRepliesPerConversation`، قوائم منع/مواضيع/عتبة ثقة.
- باقات `seed.ts`: free(200 رسالة)/basic(1000)/pro(5000)/unlimited — حدود معرّفة لكل باقة.
- **ما يحتاج فحصاً عند العودة:** هل `checkLimit("monthly_messages")` مفروض فعلاً قبل إرسال كل رسالة صادرة؟ هل `recordUsage` مستدعى في مسار الإرسال؟ منع تجاوز الحصة على القنوات. (مؤجَّل.)

**الحالة:** ⏸️ مؤجَّل — يُعاد فتحه بقرار المالك بعد نطاق 9.

### النطاق 9 — التصليب الأمني الشامل ✅ مُغلق (20 يونيو 2026)

> **منهجية:** فحص بكوديكس (read-only، gpt-5.5) ككادح، وكلود مشرفاً **تحقّق من كل ادّعاء بنفسه** قبل الاعتماد. تدقيق التبعيات شغّله كلود (sandbox كوديكس منعه).

**ما هو صلب (مُتحقَّق):**
| ما فُحص | النتيجة |
|---|---|
| تحديد المعدّل | ✅ شامل: `authLimiter` على register/login/forgot/reset/change-password/resend؛ `aiRunLimiter` على تشغيل AI؛ `paymentActionLimiter` على تأكيد/رفض الدفع؛ `reportGenerateLimiter`؛ `apiLimiter` عام (300/دق، مفتاح session-or-IP). |
| CORS | ✅ مقفل إنتاجياً — `ALLOWED_ORIGINS` فارغة ⇒ رفض كل الأصول الأجنبية؛ مع قائمة ⇒ المُدرَجة فقط. |
| رؤوس الأمان | ✅ `X-Frame-Options: DENY`، `X-Content-Type-Options: nosniff`، `Referrer-Policy`، `Permissions-Policy`، `X-Request-Id`. |
| حقن SQL | ✅ لا تركيب نصّي خام لمدخل مستخدم — كل استعلامات Drizzle مُمعلَمة (`${value}`). |
| تسريب آثار الأخطاء | ✅ المعالج العام في `app.ts` يرجع رسالة عربية موحّدة + code؛ **لا `err.stack` للعميل أبداً**. |
| تحقّق Zod | ✅ الغالبية العظمى من مسارات POST/PATCH/PUT تمرّ بـ`safeParse(req.body)` قبل المنطق. |

**ملاحظات مُتحقَّقة (تصليب — غير حاجزة للبايلوت المغلق):**
1. **webhook ميتا بلا تحديد معدّل:** `POST /api/webhooks/meta` مُركَّب **قبل** `apiLimiter` ولا يستخدم `webhookLimiter`. والأخير (`rateLimiter.ts:94`، 600/دق) **مُعرَّف ومستخدَم لا مكان**. HMAC SHA-256 يرفض الطلبات المزيّفة رخيصاً (خطر DoS محدود)، لكن وصْل المحدّد الجاهز = سطر واحد. **متوسطة-منخفضة.**
2. **تسريب `err.message` في auth:** `auth.routes.ts:133` (register) و`:186` (login) يمرّران `err.message` الخام للعميل. الرسائل التجارية مقبولة، لكن خطأ داخلي غير متوقّع قد يكشف نصّه (500/401). يُفضّل رسالة موحّدة للحالة غير التجارية. **منخفضة-متوسطة (كشف معلومات).**
3. **`switch-workspace` بفحص يدوي لا Zod:** `auth.routes.ts:393` يقرأ `req.body.workspaceId` بفحص `typeof` — لكن **العضوية مُتحقَّقة** (403 لغير العضو) ⇒ العزل سليم. تفاوت أسلوبي. **منخفضة.**
4. **HSTS مفقود:** (نفس ملاحظة نطاق 7) — أضِف `Strict-Transport-Security`. CSP مُفوَّض للواجهة عمداً. **منخفضة.**

**تصحيحات إشرافية (بالغ كوديكس — صُحّحت):**
- `followups POST`: كوديكس قال "يقرأ contactId قبل التحقق" — لكن `safeParse` يلي مباشرةً (سطر 113) + فحص ملكية workspace. **ليست ثغرة.**
- `integrations/webhooks.routes.ts`: كوديكس نفسه لاحظ أنه **غير مُركَّب** في `routes/index.ts` — مسار ميّت، بلا أثر.

**تدقيق التبعيات (`pnpm audit --prod` — شغّله كلود):** ~~5 ثغرات (1 عالية + 4 متوسطة)~~ → **No known vulnerabilities found** ✅ (بعد تطبيق `pnpm.overrides`)

**دفعة التصليح المُطبَّقة (20 يونيو 2026):**
| # | الملف | التصليح |
|---|---|---|
| 1 | `securityHeaders.ts` | أُضيف `Strict-Transport-Security: max-age=31536000; includeSubDomains` |
| 2 | `routes/index.ts` | وُصِل `webhookLimiter` (600/دق) على `/webhooks` قبل `apiLimiter` |
| 3 | `auth.service.ts` | أُضيفت `class AuthError`؛ 6 `throw new Error` → `throw new AuthError` |
| 4 | `auth.routes.ts` | catch blocks: `AuthError` → رسالة العميل؛ خطأ مجهول → `"حدث خطأ داخلي"` |
| 5 | `package.json` | `pnpm.overrides`: path-to-regexp@8.4.0، qs@6.15.2، ip-address@10.1.1، postcss@8.5.10 |

**النتيجة:** `corepack pnpm run typecheck` (api-server) → صفر أخطاء ✅ · `pnpm audit --prod` → صفر ثغرات ✅

**بوابة الإغلاق:** ✅ **أساس أمني صلب + دفعة التصليح مُطبَّقة ومُتحقَّقة.** TypeScript يمرّ، audit نظيف. جاهز للدفع.

---

### النطاق 10 — الإعداد وتجربة التاجر ✅ مُغلق (20 يونيو 2026)

**ما فُحص:** معالج الإعداد، التوجيه بعد التسجيل، Playground، وضع الثقة.

**ما هو موجود:**
- `/start` → `BusinessSetupPage`: 6 بطاقات قابلة للنقر (ربط قناة → وارد → معرفة → وكيل → كتالوج → مراقبة)
- اختيار القطاع والموقع — يُحفظ في workspace settings
- Dashboard (موبايل + ديسكتوب): دليل التشغيل السريع (5 خطوات)
- Playground في `AgentDetailPage`: تبويب كامل مع نص، تشغيل، نتيجة
- وضع الثقة: suggest / auto / auto_after_hours

**التصليحات المُطبَّقة:**
| الملف | التصليح |
|---|---|
| `RegisterPage.tsx:34` | `navigate("/dashboard")` → `navigate("/start")` — التاجر الجديد يصل للدليل التفاعلي مباشرةً |
| `DashboardPage.tsx` | أُضيف زر "فتح دليل الإعداد الكامل" (→ `/start`) في بطاقة الخطوات — موبايل وديسكتوب |

**ملاحظات غير حاجزة (موثّقة):**
- لا بوابة Playground إلزامية قبل وضع auto — مقبول للبايلوت المغلق، يُراجع قبل الإطلاق العام.
- لا تتبّع إتمام الخطوات (checkmarks) — UX improvement مستقبلي.

**typecheck:** صفر أخطاء (web) ✅

**بوابة الإغلاق:** ✅ تاجر جديد يسجّل → يصل `/start` مباشرةً → يتبع الدليل التفاعلي → يشغّل وكيله بلا مساعدة يدوية.

---

### النطاق 11 — مصفوفة الاختبار الشامل ⏳ قيد التنفيذ (20 يونيو 2026)

**التعليمات:** نفّذ كل بند على التطبيق الحيّ (staging أو production). أشّر ✅ نجح / ❌ فشل / ⏭️ لم يُختبر. أبلغ عن أي ❌ لإصلاحه قبل الإغلاق.
اختبر على **الجوال** (موبايل) و**اللابتوب** (ديسكتوب) ما لم يُشر خلافه.

---

#### S1 — المصادقة والجلسات

| # | الاختبار | المتوقّع | نتيجة |
|---|---|---|---|
| S1-1 | سجّل حساباً جديداً (اسم + بريد + كلمة مرور + اسم منشأة + حلّ "7") | توجيه لـ/start مباشرةً | ⏭️ |
| S1-2 | تحقّق من البريد: افتح الرسالة وانقر الرابط | "تم تأكيد البريد الإلكتروني" | ⏭️ |
| S1-3 | سجّل دخولاً بكلمة مرور صحيحة | توجيه للوحة التحكم | ⏭️ |
| S1-4 | سجّل دخولاً بكلمة مرور خاطئة 3 مرات | رسالة خطأ عربية (لا تسرّب تقني) | ⏭️ |
| S1-5 | تسجيل خروج | يعود لصفحة تسجيل الدخول | ⏭️ |
| S1-6 | استعادة كلمة المرور: أدخل البريد → افتح الرابط → عيّن كلمة جديدة | كلمة المرور تتغيّر، الجلسات القديمة تنتهي | ⏭️ |

---

#### S2 — الإعداد الأولي

| # | الاختبار | المتوقّع | نتيجة |
|---|---|---|---|
| S2-1 | بعد التسجيل: صفحة /start تظهر تلقائياً | 6 بطاقات خطوات مع أزرار | ⏭️ |
| S2-2 | اختر قطاعاً وحدد محافظة ثم "حفظ القطاع" | رسالة نجاح، القيم تبقى عند التحديث | ⏭️ |
| S2-3 | لوحة التحكم: انقر "فتح دليل الإعداد الكامل" | يفتح /start | ⏭️ |

---

#### S3 — صندوق الوارد

| # | الاختبار | المتوقّع | نتيجة |
|---|---|---|---|
| S3-1 | افتح الوارد: تظهر المحادثات أو حالة "لا محادثات" | لا خطأ | ⏭️ |
| S3-2 | افتح محادثة → اكتب ردّاً وأرسله | الرسالة تظهر في المحادثة | ⏭️ |
| S3-3 | أسنِد محادثة لعضو فريق | الإسناد يُحفظ ويظهر | ⏭️ |
| S3-4 | أضف وسماً للمحادثة | الوسم يظهر | ⏭️ |
| S3-5 | أغلق محادثة ثم أعِد فتحها | الحالة تتغيّر في كل مرة | ⏭️ |
| S3-6 | (جوال) افتح الوارد وتنقّل بين المحادثات | واجهة مريحة بدون تمرير أفقي | ⏭️ |

---

#### S4 — الوكلاء الذكيون

| # | الاختبار | المتوقّع | نتيجة |
|---|---|---|---|
| S4-1 | أنشئ وكيلاً جديداً باسم ووضع "اقتراح" | يُحفظ ويظهر في القائمة | ⏭️ |
| S4-2 | أضف تعليمات للوكيل واحفظ | التعليمات تبقى عند التحديث | ⏭️ |
| S4-3 | افتح تبويب Playground → اكتب سؤالاً → شغّل | يظهر مسوّدة رد (لا خطأ 500) | ⏭️ |
| S4-4 | ارتبط وكيلاً بقاعدة معرفة | يُحفظ | ⏭️ |
| S4-5 | وضع الاقتراح: محادثة واردة → يقترح الوكيل ردّاً | يظهر مقترح في الوارد | ⏭️ |

---

#### S5 — قاعدة المعرفة

| # | الاختبار | المتوقّع | نتيجة |
|---|---|---|---|
| S5-1 | أضف مقالة جديدة بعنوان ومحتوى | تُحفظ وتظهر في القائمة | ⏭️ |
| S5-2 | ابحث عن المقالة بكلمة من عنوانها | تظهر في النتائج | ⏭️ |
| S5-3 | عدّل المقالة ثم احذفها | التعديل يُحفظ، الحذف نهائي | ⏭️ |

---

#### S6 — الطلبات

| # | الاختبار | المتوقّع | نتيجة |
|---|---|---|---|
| S6-1 | أنشئ طلباً جديداً واربطه بجهة اتصال | يُحفظ بحالة "جديد" | ⏭️ |
| S6-2 | أضف بنداً للطلب (اسم + كمية + سعر) | يُحفظ ويظهر في الإجمالي | ⏭️ |
| S6-3 | غيّر حالة الطلب (جديد → معالجة → مكتمل) | كل تغيير يُحفظ | ⏭️ |
| S6-4 | ابحث عن الطلب بالاسم أو الرقم | يظهر في النتائج | ⏭️ |

---

#### S7 — المدفوعات

| # | الاختبار | المتوقّع | نتيجة |
|---|---|---|---|
| S7-1 | أنشئ سجل دفع مرتبطاً بجهة اتصال | يُحفظ بحالة "معلّق" | ⏭️ |
| S7-2 | وافق على الدفع | الحالة تتغيّر لـ"مؤكّد" | ⏭️ |
| S7-3 | أنشئ دفعاً آخر وارفضه | الحالة تتغيّر لـ"مرفوض" | ⏭️ |

---

#### S8 — جهات الاتصال

| # | الاختبار | المتوقّع | نتيجة |
|---|---|---|---|
| S8-1 | أضف جهة اتصال يدوياً (اسم + هاتف) | تُحفظ وتظهر في القائمة | ⏭️ |
| S8-2 | ابحث عن جهة اتصال بالاسم | تظهر في النتائج | ⏭️ |
| S8-3 | استورد ملف CSV (صفّيْن على الأقل) | جهات الاتصال تُستورد، لا أخطاء | ⏭️ |
| S8-4 | صدّر جهات الاتصال CSV | يُنزَّل ملف بالبيانات الصحيحة | ⏭️ |

---

#### S9 — القنوات والتكاملات

| # | الاختبار | المتوقّع | نتيجة |
|---|---|---|---|
| S9-1 | افتح /integrations: الصفحة تُحمَّل | لا خطأ | ⏭️ |
| S9-2 | (إن وُجدت قناة مربوطة) أرسل رسالة اختبار | تصل للوارد | ⏭️ |

---

#### S10 — الإعدادات والفريق

| # | الاختبار | المتوقّع | نتيجة |
|---|---|---|---|
| S10-1 | عدّل اسم المنشأة واحفظ | يتحدث في كل مكان | ⏭️ |
| S10-2 | أضف عضو فريق بالبريد والدور | يُضاف وتُرسَل دعوة | ⏭️ |
| S10-3 | غيّر كلمة مرورك من الإعدادات | تُطلب إعادة تسجيل الدخول | ⏭️ |

---

#### S11 — الأداء والعرض

| # | الاختبار | المتوقّع | نتيجة |
|---|---|---|---|
| S11-1 | افتح لوحة التحكم على جوال (شاشة ضيّقة) | لا تمرير أفقي، كل الأزرار واضحة | ⏭️ |
| S11-2 | لوحة التحكم على لابتوب: الأعداد والمؤشرات | تُحمَّل خلال 3 ثوانٍ | ⏭️ |
| S11-3 | تنقّل سريع بين 5 صفحات مختلفة | لا صفحة بيضاء أو خطأ 404 | ⏭️ |

---

#### S12 — المخزون ورفع الصور (جديد، 21 يونيو 2026)

| # | الاختبار | المتوقّع | نتيجة |
|---|---|---|---|
| S12-1 | افتح `/inventory` (مجموعة المتجر في القائمة الجانبية) | تُحمَّل الصفحة، لا "المنتجات والمخزون" القديم | ⏭️ |
| S12-2 | أضف منتجاً (اسم + سعر + وحدة + كمية + سياسة توصيل) | يُحفظ ويظهر في القائمة | ⏭️ |
| S12-3 | **ارفع صورة للمنتج** (اختر ملفاً من الجهاز) | معاينة تظهر فوراً ثم تُحفظ مع المنتج (يؤكّد الباكت + الصلاحيات) | ⏭️ |
| S12-4 | عدّل الكمية بأزرار +/− | الكمية تتحدّث وتُحفظ | ⏭️ |
| S12-5 | في بند طلب: اضغط "اختر من المخزون" واختر منتجاً | الاسم والسعر يُملآن تلقائياً | ⏭️ |
| S12-6 | أرشِف منتجاً ثم فعّل فلتر الأرشيف | يختفي من النشِط ويظهر في المؤرشف | ⏭️ |
| S12-7 | (جوال) افتح المخزون من الشريط السفلي | "المخزون" يفتح `/inventory` بلا تمرير أفقي | ⏭️ |

---

**ملخّص الاختبار (يُملأ بعد التنفيذ):**

| السطح | إجمالي | ✅ نجح | ❌ فشل | ⏭️ لم يُختبر |
|---|---|---|---|---|
| S1 المصادقة | 6 | | | 6 |
| S2 الإعداد | 3 | | | 3 |
| S3 الوارد | 6 | | | 6 |
| S4 الوكلاء | 5 | | | 5 |
| S5 المعرفة | 3 | | | 3 |
| S6 الطلبات | 4 | | | 4 |
| S7 المدفوعات | 3 | | | 3 |
| S8 جهات الاتصال | 4 | | | 4 |
| S9 القنوات | 2 | | | 2 |
| S10 الإعدادات | 3 | | | 3 |
| S11 الأداء | 3 | | | 3 |
| S12 المخزون والصور | 7 | | | 7 |
| **الإجمالي** | **49** | | | **49** |

**بوابة الإغلاق:** ✅ عند: ≥ 44 بند ✅ (90%) + صفر ❌ حرجة في S1/S2/S3/S4/S12.

---

## ✅ نطاق المخزون (منتجات) — مبني كاملاً (21 يونيو 2026، Claude Code)

**الطلب (المالك):** ربط الطلبات بكتالوج منتجات من المخزون، مع سياسة توصيل على مستوى المنتج.

**المتطلبات المنجزة:**

| # | المتطلب | الحالة |
|---|---|---|
| M-1 | جدول `inventory_products` (14 عمود: اسم، سعر، وحدة، وصف، صورة، كمية، SKU، عملة) | ✅ `lib/db/src/schema/products.ts` |
| M-2 | سياسة التوصيل: `pickup_only` / `local` / `all` على مستوى المنتج | ✅ حقل `delivery_policy` |
| M-3 | ربط `order_items.inventory_product_id → inventory_products.id` + حقل `discount` | ✅ `lib/db/src/schema/order_items.ts` |
| M-4 | زر "اختر من المخزون" في نموذج بند الطلب — يملأ الاسم والسعر تلقائياً | ✅ `OrdersPage.tsx` (picker dropdown) |
| M-5 | صفحة إدارة المخزون `/inventory` — إضافة/تعديل/أرشفة + تتبّع الكمية (+/-) | ✅ `ProductsPage.tsx` |
| M-6 | صلاحيات products:read/create/update/delete — agent يملك read | ✅ `seed.ts` |

**ملفات جديدة/محدَّثة:**

| الملف | التغيير |
|---|---|
| `lib/db/src/schema/products.ts` | جدول `inventory_products` (Drizzle) |
| `lib/db/drizzle/0029_products_inventory.sql` | migration idempotent — CREATE TABLE + ALTER TABLE order_items |
| `scripts/migrate-phase345.sql` | **دُمج 0029 هنا** → يُطبَّق تلقائياً وقت النشر قبل deploy (صفر نافذة انكسار، عكس PD-12) |
| `lib/db/src/schema/order_items.ts` | إضافة `inventoryProductId` + `discount` |
| `lib/db/src/schema/index.ts` | `export * from "./products"` |
| `artifacts/api-server/src/lib/audit.ts` | إضافة `products_create|update|delete` لـ`AuditAction` |
| `artifacts/api-server/src/lib/seed.ts` | 4 صلاحيات products + products:read للوكيل |
| `artifacts/api-server/src/modules/products/products.routes.ts` | CRUD كامل (GET list/one, POST, PATCH, PATCH/quantity, DELETE soft) |
| `artifacts/api-server/src/routes/index.ts` | `router.use("/products", productsRouter)` |
| `artifacts/web/src/pages/ProductsPage.tsx` | صفحة كاملة (بحث + فلتر أرشيف + إضافة/تعديل + تعديل كمية) |
| `artifacts/web/src/App.tsx` | Route `/inventory` → ProductsPage |
| `artifacts/web/src/components/Layout.tsx` | رابط nav المخزون (permission: products:read) |
| `artifacts/web/src/i18n/locales/ar\|en/common.json` | مفتاح `inventory` |

**ملاحظة تسمية:** الجدول `inventory_products` (لا `products`) لتجنّب التعارض مع `products` الموجود في `catalog.ts` (كتالوج ميتا المتزامن).

**typecheck:** api-server ✅ · web ✅

**حالة النشر (21 يونيو 2026):** ✅ **منشور وحيّ** عبر commits `13e68bb` (المخزون) + `545bc06` (الرفع + حذف الكتالوج) + `88e49ec` (إصلاح `.dockerignore`). migration 0029 طُبِّق تلقائياً (build بـ`ON_ERROR_STOP=1` نجح). تحقّق حيّ: `GET /api/products` و`POST /api/uploads/product-image` يردّان **401** (المسار حيّ، يتطلّب مصادقة) لا 404.

**ما يتبقّى قبل الإغلاق الكامل:**
1. ✅ migration: دُمج 0029 في `migrate-phase345.sql` → طُبِّق تلقائياً وقت النشر (لا حاجة Cloud Shell).
2. ✅ المسارات منشورة وحيّة (تحقّق 401). **يتبقّى اختبار يدوي بواجهة المالك:** افتح `/inventory` → أضف منتجاً → ارفع صورة (يؤكّد الباكت + الصلاحيات) → اختَره في بند طلب → تأكّد من ملء الاسم والسعر.
3. ✅ seed.ts يُشغَّل عند النشر (صلاحيات products تُضاف تلقائياً لـowner/manager + read للوكيل).
4. **ربط سياسة التوصيل** بنموذج الطلب (M-6): الفلترة لم تُبنَ بعد (scope خارج M-4) — اختياري.
5. الدفعة 3 (نطاق 11): أداة وكيل `get_order_status` — لا تزال مؤجّلة.

---

## ✅ رفع صور المنتجات — تخزين سحابي احترافي (21 يونيو 2026، Claude Code)

**الطلب (المالك):** رفع صورة المنتج مباشرة (مثل المنصات الناجحة)، لا مجرد لصق رابط.

**القرار المعماري (مُكيَّف لبنية esbuild bundle بلا node_modules وقت التشغيل):**
- لا SDK ثقيل ولا `sharp` (native، لا يُحزَم) → رفع لـGCS عبر **JSON REST API + توكن metadata server** (نفس نمط `ai-provider.ts`).
- **ضغط/تصغير في المتصفح** عبر Canvas API (حتى 1200px، WebP/JPEG q0.85) قبل الرفع → صورة 5MB تصير ~200KB.
- **صفر تبعيات npm جديدة** (لا تغيير في lock file) · يعيد استخدام عمود `image_url` (لا تغيير schema).
- عزل المستأجر في المسار: `products/{workspaceId}/{uuid}.ext` · قائمة بيضاء (JPG/PNG/WebP) · سقف 6MB · صلاحية products:create أو update.
- فشل آمن: لو `UPLOADS_BUCKET` غير مضبوط → 503 وحقل الرابط يبقى يعمل.

**ملفات:**
| الملف | التغيير |
|---|---|
| `artifacts/api-server/src/lib/storage.ts` | جديد — `uploadImageBuffer` عبر GCS REST + metadata token |
| `artifacts/api-server/src/modules/uploads/uploads.routes.ts` | جديد — `POST /api/uploads/product-image` (express.raw، بلا multer) |
| `artifacts/api-server/src/routes/index.ts` | تسجيل `/uploads` |
| `artifacts/web/src/lib/imageUpload.ts` | جديد — ضغط Canvas + رفع |
| `artifacts/web/src/pages/ProductsPage.tsx` | زر رفع + معاينة + إزالة (الرابط يبقى خياراً متقدّماً) |
| `cloudbuild.yaml` | `_UPLOADS_BUCKET` + env var على الخدمة |
| `.env.example` | توثيق `UPLOADS_BUCKET` |

**بنية تحتية مطلوبة مرة واحدة (المالك، Cloud Shell):** إنشاء باكت `khadamatak-auth-uploads` + public-read + منح حساب خدمة Cloud Run صلاحية `storage.objectAdmin`. (يُفضَّل قبل الدفع؛ وإن تأخّر، الرابط يعمل والرفع يرجع خطأً مهذّباً حتى يُنشأ.)

**typecheck:** api ✅ web ✅ · **esbuild bundle:** ✅

**🔴→✅ عقبة نشر (PD-13، 21 يونيو):** أول بناء staging لـ`545bc06` **فشل** — typecheck في الحاوية: `Cannot find module '../modules/uploads/uploads.routes'`. الجذر: `.dockerignore` فيه `**/uploads/**` (مقصود لملفات المستخدمين المرفوعة) ابتلع **مجلد الكود المصدري** `modules/uploads/` فلم يدخل سياق Docker — رغم نجاحه محلياً (`.dockerignore` يؤثّر على Docker فقط). **الإصلاح (`88e49ec`):** استثناء صريح `!artifacts/api-server/src/modules/uploads/**` في `.dockerignore`. **درس:** أي مجلد كود جديد باسم يطابق نمط تجاهل (uploads/secrets/dist...) يُحجب صامتاً عن البناء السحابي. → سُجِّل في الذاكرة.

---

## 🗑️ حذف قسم الكتالوج القديم (21 يونيو 2026، Claude Code)

**السبب (المالك):** قسم "المنتجات والمخزون" القديم (`/catalog` كتالوج ميتا) صار مكرّراً ومربكاً مع "المخزون" الجديد.

**فحص أمان قبل الحذف:** `catalog.routes.ts` **غير مُسجَّل** في `routes/index.ts` (backend ميت أصلاً، يطابق ملاحظة «الكتالوج غير متوفر حالياً») · الوكيل لا يستخدمه · جداول `products`/`catalog_sources` مستقلة عن `inventory_products`. ⇒ الحذف آمن صفر مخاطر.

**ما حُذف/عُدّل (واجهة فقط):**
- حُذف `artifacts/web/src/pages/CatalogPage.tsx` + 3 مسارات `/catalog*` من App.tsx
- نُقل رابط "المخزون" إلى مجموعة **المتجر** (مكانه المنطقي)، وأُزيل من مجموعة العملاء — `Layout.tsx`
- أُعيد توجيه كل مداخل المنتجات للمخزون الجديد: `MobileBottomNav.tsx`، `DashboardPage.tsx`، `BusinessSetupPage.tsx` (`/catalog` → `/inventory`)

**مُبقىً عمداً (inert، إزالته مخاطرة بلا فائدة):** backend الكتالوج غير المسجّل + جداول `products`/`catalog_sources`/`ad_campaigns` + ترجمات `pages.json:catalog`.

**typecheck:** web ✅ · **web build (BASE_PATH=/):** ✅

---

## ⏸️ توسعة النطاق 11 — التوصيل والسندات والدفع عند الاستلام (20 يونيو 2026، Claude Code)

**الطلب (المالك):** قسم طلبات تشغيلي عام — فصل تفاصيل/تعديل، حالة التوصيل، نوع التسليم (استلام/مندوب داخلي/مكتب نقل)، الدفع عند الاستلام، سند مكتب النقل (صورة/رابط) + رقمه، رقم مندوب التوصيل الداخلي، رسوم التوصيل تُضاف للإجمالي.

**المبني (3 دفعات، الدفعتان 1+2 جاهزتان):**
- **schema (migration 0028):** 9 أعمدة على `orders` (delivery_type/status, delivery_agent_phone, carrier_name/phone, delivery_receipt_url, delivery_address, delivery_fee, cod_enabled). **دُمج في `migrate-phase345.sql`** → يُطبّق تلقائياً وقت النشر قبل deploy (صفر نافذة انكسار، عكس PD-12).
- **backend (`orders.routes.ts`):** recalcTotal يضيف رسوم التوصيل؛ create/update تقبل حقول التوصيل؛ مسار جديد `PATCH /:id/delivery-status` (تدفّق حالة + يدفع الطلب لـdelivered عند اكتمال التوصيل)؛ عزل + تدقيق + صلاحيات محفوظة.
- **frontend (`OrdersPage.tsx`):** زر تفاصيل (👁 عرض) منفصل عن تعديل (✏️)؛ مكوّن `OrderEditForm` كامل؛ لوحة توصيل (timeline + أزرار تقدّم)؛ COD في ملخص الدفع؛ حقول التوصيل في الإنشاء.

**typecheck + build:** api-server ✅ web ✅ (BASE_PATH=/).

**الدفعة 3 (`get_order_status`) — ✅ مبنية (21 يونيو 2026، Claude Code):** الوكيل يردّ على «وين طلبي؟» بحالة الطلب + حالة التوصيل + رقم المندوب/مكتب النقل + الإجمالي + الدفع.

**القرار المعماري (مهم):** بنية الردّ **أحادية التمرير** (النموذج يكتب الردّ + يطلب الأدوات في استدعاء واحد؛ الأدوات تُنفَّذ *بعد* الردّ) وfunction calling مؤجّل (PD-10 مرحلة 2). لذا `get_order_status` كـ`tool_call` **سيفشل** — النموذج لن يرى النتيجة قبل كتابة الردّ فيهلوس الحالة. **الحل:** حقن حالة طلبات العميل في **السياق** (نمط قاعدة المعرفة) عبر `loadOrderStatusContext` → الوكيل يردّ بدقّة في رسالة واحدة بلا هلوسة، بلا رسالة ثانية، بلا تعقيد قنوات. **عند وصول function calling لاحقاً، يمكن تحويلها لأداة استدعاء حقيقية.**
- **الملفات:** `agent-tools.ts` (`loadOrderStatusContext` + خرائط عربية للحالات، عزل workspaceId+contactId، أحدث 3 طلبات، حقول مرئية للعميل فقط) · `agent-reply.ts` (حقن السياق في userPrompt مع تعليمة «للرجوع فقط إن سأل — لا تذكرها تلقائياً»).
- **بلا migration / seed / صلاحيات / واجهة / تبعيات** — سياق بحت يعيد استخدام حقول `orders` الموجودة (توسعة 0028). **typecheck ✅ · esbuild ✅.**

**اختبار حيّ (20 يونيو 2026):** ✅ التوصيل ✅ التعديل ✅ تقدّم حالة التوصيل ✅ رسوم التوصيل في الإجمالي ✅ COD badge — **كلها ناجحة.**

**بوابة الإغلاق:** ✅ **مُغلقة** — M-3 مُنشَر + `get_order_status` مُنشَر (`fca1217`) + **اختبار حيّ نجح** (22 يونيو): عميل سأل «ايش حالة طلبي» → الوكيل ردّ ببيانات حقيقية «لدي طلبان مسجلان… ORD00003: حالته جديد…».

---

## 🛠️ صقل قسم الوكلاء (22 يونيو 2026، Claude Code) — typecheck+build ✅، **غير مدفوع بعد**

**سياق:** المالك لاحظ ردوداً عامة/مكرّرة وقلقاً من «تحديد النماذج خطير». فحص هندسي read-only:

**التشخيص:**
1. **الموديل آمن ✅ (تطمين):** الموديل الفعلي من راوتر مركزي `model-router.ts` (Vertex: gemini-3-flash/pro) وهو المرجع في الإنتاج (`runAI`→vertex→`input.modelId`). حقل `agent.defaultModel` (افتراضه `"mock"`) **بقايا ميتة** — يُسجَّل ولا يُستخدم في الردّ الفعلي. ليس خطراً.
2. **الردود العامة = إعداد لا كود:** تعليمات الوكيل عامة (support CRM) + قواعد النشاط فارغة + قاعدة المعرفة عن منصّة وصال ون نفسها → يتصرّف كبوت تعريفي. **الحل بيد المالك:** تعبئة التعليمات + المعرفة بنشاطه الفعلي.
3. **«الرقم متكرّر»:** الأرجح صفوف طلبات بنفس الرقم (سباق توليد `ORD{count+1}` غير ذرّي).

**دفعة التصليح (`agent-tools.ts` + `agent-reply.ts`):**
1. منع تكرار في `loadOrderStatusContext` (dedup بـorderNumber، يجلب limit×3 ويأخذ الفريد).
2. `try/catch` حول حقن سياق الطلب (فشله لا يكسر الردّ كلّه + logger.warn).
3. تنظيف `mock`: `model` لا يساوي `"mock"` أبداً (يسقط لـgetDefaultModel).
4. نص `rolePrompt` افتراضي محايد (موظّف نشاط تجاري، لا «عن المنصّة»).
5. تقوية `userPrompt`: أجب على آخر رسالة مباشرةً، لا تكرّر ردّاً، لا عبارة ختامية إلا عند انتهاء فعلي.

**متبقٍّ (غير حاجز):** سباق توليد رقم الطلب (يُفضّل DB sequence/قيد فريد + تنظيف الصفوف المكرّرة الحالية) · إزالة حقل `defaultModel` الميّت ومنتقي الموديل من الواجهة لاحقاً (تقليل اللبس على التجار).

---

## 🔴→✅ PD-12 — انجراف schema أوقف استقبال واتساب (19 يونيو 2026، Claude Code)

**العَرَض (المالك، حيّ):** رسالة من رقم جديد لا تظهر في الوارد إطلاقاً والوكيل لا يرد — رغم أن القناة نشطة والـworkspace صحيح. **هذا هو الجذر الحقيقي لـPD-2** (لا علاقة له بـSSE).

**الجذر (مؤكّد من سجلّ Cloud Run + قاعدة الإنتاج):** عمود `contacts.custom_fields` (migration `0027`، commit `cc9baf6`) **لم يُطبّق على الإنتاج**. كل webhook وارد → `upsertContact` (`meta.routes.ts:167`) ينفّذ `UPDATE contacts … RETURNING custom_fields` → `column "custom_fields" does not exist` → الاستثناء يُبتلع بصمت («Failed to process Meta webhook») → الرسالة تسقط، لا domain_event، الوكيل لا يُستدعى. نفس السبب كسر `/api/contacts` و`cleanup-domain-events` (500).

**السبب الجذري المنهجي:** آلية النشر تطبّق `scripts/migrate-phase345.sql` (ملف ثابت) فقط؛ ملفات `lib/db/drizzle/*.sql` الجديدة لا تُطبّق تلقائياً، و0027 لم يُدمج. drizzle journal/snapshots مهجورة. → سُجّل في الذاكرة الدائمة.

**الإصلاح المطبّق على الإنتاج (Cloud Shell + cloud-sql-proxy):**
1. `ALTER TABLE contacts ADD COLUMN custom_fields, archived_at` + فهارس 0027 → **الاستقبال عاد فوراً، الوكيل رد ✅** (تأكيد حيّ من المالك).
2. دمج 0027 في `scripts/migrate-phase345.sql` (idempotent) → يمنع تكراره في أي قاعدة/نشر جديد.

**التدقيق الشامل (read-only):** 95/95 جدول موجودة · 24/24 عمود من الهجرات الأحدث موجودة · الجداول الحرجة السبعة مطابقة → **صفر انجراف متبقٍّ، الإنتاج متزامن مع الكود.**

**متبقٍّ (الطبقة 2 — يحتاج مراجعة مبرمج قبل الدفع، لأن المالك غير مبرمج):** توسيع خطوة `verify-migration` في `cloudbuild.yaml` لتفحص **الأعمدة الحرجة** لا الجداول فقط — فأي انجراف عمودي مستقبلي يُفشل النشر بدل أن يضرب عميلاً صامتاً.

**ملفات محلية غير مدفوعة:** `scripts/migrate-phase345.sql` (دمج 0027). [إصلاح SSE `realtime.ts`+`internal.routes.ts` دُفع `a85da54` — تحسين ظهور فوري، طبقة مختلفة عن الجذر.]

---

## الحالة الإجمالية

### نطاقات الميزات (Business Domains)
| النطاق | الوصف | الحالة |
|---|---|---|
| نطاق 10 — جهات الاتصال | CSV استيراد/تصدير، custom_fields | ✅ مغلق (`f52a262`) |
| نطاق 11 — الطلبات + التوصيل | أنواع التسليم، حالة التوصيل، COD، رسوم، `get_order_status` | ✅ **جاهز للإغلاق** — M-3 مُنشَر + الدفعة 3 (`get_order_status`) مبنية (typecheck+build ✅، غير مدفوع بعد)؛ يتبقّى نشر + اختبار حيّ |
| نطاق 12 — المدفوعات | تسجيل دفعة، حالة جزئي/مدفوع | ✅ اختبار حيّ ✅ — مغلق |
| نطاق المخزون — منتجات | كتالوج + سياسة توصيل + ربط بالطلبات + رفع صور | ✅ **مُنشَر وحيّ** (commit `88e49ec`، 21 يونيو) — migration 0029 طُبِّق تلقائياً، المسارات تردّ 401 حيّاً؛ يتبقّى اختبار رفع صورة حقيقي (يؤكّد صلاحيات الباكت) |

### حوادث الإنتاج
**✅ PD-7** (`9cbf5a7`). **✅ PD-8** (`a3cb4a6`، parser ثلاثي). **✅ PD-9 مدفوع** (`e091e4c`، `coerceCurrency`) — لم يُؤكَّد حيّاً بعد لأنه كان محجوباً بـPD-10/PD-11. **✅ PD-10 المرحلة 1 مدفوعة** (`3efd9ca`، فرض JSON عند تفعيل الأدوات) — المرحلة 2 (function calling) مؤجلة. **⏸️ PD-2/PD-11 مؤجلان إلى Claude Code بقرار المالك** بعد اختبار الإنتاج في 19 يونيو: الرسائل لا تظهر في الوارد، وزر «إعادة للوكيل» يختفي ويعود ولا يعمل بثبات رغم `9d4ddfb` و`8fce81b`.

> **سياق الجلسة:** اختبار `create_order` الحيّ كشف 3 طبقات على نفس المسار: PD-8 (تحليل) + PD-9 (عملة) + PD-10 (التزام صيغة الأدوات). الوكيل صمت بعد التصعيد بسبب PD-11 (فُكّ يدوياً عبر `UPDATE conversations SET agent_status='active'` للمحادثة `03f513bf`).

---

## 🟡 Hotfix PD-10 (المرحلة 1) — النموذج لا يلتزم بصيغة الأدوات (18 يونيو 2026)

**العَرَض:** `create_order` متذبذب — نفس الطلب ينجح مرة ويفشل أخرى. تشغيل 21:13 ردّ نصّاً صرفاً بلا JSON (`has_raw=f` في `ai_messages.metadata`) → لا أداة، لا طلب.

**الجذر:** JSON نصّي بلا `responseMimeType`/function calling؛ و`draft_reply` خارج `JSON_TASK_TYPES` فلا شبكة أمان لردود الوكيل.

**الإصلاح (المرحلة 1 — `ai-provider.ts` + `agent-reply.ts`):** علَم `responseFormat="json"` عند تفعيل الأدوات → `responseMimeType: application/json` + حرارة ≤0.1 + تفعيل شبكة "نص بلا JSON → تصعيد صامت". يقتل ردّ النص الصرف وفئة PD-8. **المرحلة 2 (function calling) مؤجلة بقرار المالك.**

**typecheck:** api-server ✅

**اختبار الإغلاق (يؤكّد PD-10 وPD-9 معاً):** أرسل "أطلب كرتونين مياه بـ2000 ريال يمني الدفع عند الاستلام، أكّد الطلب" → ردّ نظيف + صفّ في `orders` بعملة `YER`.

**ملف لمس:** `artifacts/api-server/src/lib/ai-provider.ts`, `artifacts/api-server/src/lib/agent-reply.ts`

---

## 🟡 Hotfix PD-11 — "إعادة فتح" لا تجعل الوكيل يرد (18 يونيو 2026)

**العَرَض:** المالك عمل "إعادة فتح" لمحادثة مصعّدة، لكن الوكيل لم يرد.

**الجذر:** "إعادة فتح" تعدّل `status` فقط، بينما الـworker يتوقف عند `agent_status='human'`. وحتى عند إعادة `agent_status='active'` يدوياً، حدث الرسالة القديم يكون غالباً `done` لأن الـworker عالجه سابقاً وهو في وضع بشري، فلا يوجد `domain_event` جديد يوقظه.

**الإصلاح v1:** في `PATCH /conversations/:id/agent-status` عند `status=active`:
1. مسح `needsHuman` و`escalationReason`.
2. نشر `message.received` جديد للمحادثة بـ`source=agent_reactivated` حتى يلتقطه الـworker فوراً.
3. في الوارد: زر واضح "إعادة للوكيل" عند حالة `human` + نفس الفعل داخل قائمة المحادثة.

**تصحيح v2 (بعد اختبار حي بالصورة):** الزر كان لا يظهر لأن الواجهة ربطته بـ`conversations:manage`، بينما مشغل الوارد يملك `conversations:resolve`. أُضيف مسار مخصص `POST /conversations/:id/reactivate-agent` محمي بـ`conversations:resolve` فقط؛ يفعل الإرجاع الآمن للوكيل ولا يعطي صلاحية إيقاف/إدارة حالات الوكيل العامة. الواجهة تستخدم هذا المسار عند `needsHuman`/`human`.

**اختبار الإغلاق:** بعد النشر، افتح محادثة بشرية واضغط "إعادة للوكيل" لا "إعادة فتح" فقط؛ يجب أن ينتج domain_event جديد، ثم outbox reply.

**نتيجة الاختبار الحي (19 يونيو 2026):** ❌ لم تُغلق البوابة. الزر يختفي ثم يعود، والرسائل لا تظهر في الوارد. يوجد تضارب مستمر بين حالة المحادثة/الوكيل ومسار تحديث الوارد. **قرار المالك:** تأجيل تشخيص PD-2 وPD-11 وإصلاحهما إلى Claude Code، وعدم إبقائهما حاجزاً أمام متابعة النطاق 10 الآن.

---

## 🔴 Hotfix PD-9 — `create_order` تفشل على كل محادثة عربية (17 يونيو 2026)

**العَرَض:** المالك أرسل "كرتونين مياه... ريال يمني" — الوكيل ردّ "أحتاج أن أحوّل طلبك للفريق". الاختبار الثاني (رقم آخر، بلا سعر) استدعى `handoff_to_human` لغياب السعر (سلوك آمن، ليس عطلاً).

**التشخيص (من DB):** `Agent tool failed: create_order - Invalid enum value. Expected 'YER'|'SAR'|'USD', received 'ريال يمني'` — في موضعَين (`currency` الجذر + `items[0].currency`). النموذج يمرّر ما قاله العميل حرفياً بدل رمز ISO. الـprompt لم يذكر الرموز. `createOrderSchema.parse()` يرمي → `hasToolProblem=true` → جملة التحويل → لا طلب يُنشأ.

**الإصلاح (`agent-tools.ts`):**
1. `coerceCurrency()`: يمني→`YER`، سعودي→`SAR`، دولار→`USD`؛ مجهول يبقى كما هو (Zod يطبّق default).
2. `z.preprocess` على جميع حقول currency + الأرقام (`quantity`, `unitPrice`, `discount`).
3. الـprompt: يذكر `(YER/SAR/USD, e.g. YER for ريال يمني)` صراحةً.

**typecheck:** api-server ✅

**اختبار الإغلاق:** أرسل "أطلب كرتونين مياه بـ2000 ريال يمني الدفع عند الاستلام" → يجب أن يصل ردّ نظيف + ينزل صفّ في `orders` (نتحقق بالاستعلام).

---

## ✅ Hotfix PD-8 — JSON خام يصل للعميل + استدعاءات أدوات ضائعة (16–17 يونيو 2026)

**العَرَض:** أثناء اختبار `create_order` حيّاً، وصل العميل `{"reply":"..."}` خاماً بدل النص، و`create_order` لم يُستدعَ.

**الجذر:** النموذج يضع أسطراً حقيقية داخل قيمة `reply` متعددة الفقرات → `JSON.parse` يفشل → `parseAgentToolResponse` كان يُرجع المحتوى الخام. **الأخطر:** نفس الفشل يُسقط `tool_calls` → أي ردّ متعدد الأسطر بأداة لا تُنفَّذ أداته أبداً.

**الإصلاح (`agent-tools.ts`):** تحليل متين 3 طبقات — parse مباشر → تهريب أحرف التحكّم داخل السلاسل فقط → استخراج reply بـregex (آخر دفاع). **مُثبَت محلياً** على المُدخل الفاشل الحقيقي (نص نظيف + tool_calls سليمة + لا انحدار).

**typecheck + build:** api-server ✅

**متبقٍّ للـstaging:**
```
artifacts/api-server/src/lib/agent-tools.ts
.claude/skills/wesal-one-agents/references/launch-readiness-plan.md
.codex/skills/wesal-one-agents/references/launch-readiness-plan.md
WESAL_ONE_CHAT_HANDOFF.md
```

---

## 🔴 Hotfix PD-7 — الوكيل توقّف عن الرد كلياً (16 يونيو 2026)

**العَرَض:** المالك أبلغ "الوكيل وقف لا يرد". `/api/readyz=ok`، المحادثة `active`، الوكيل `active`، القناة `active` — كل الشروط سليمة ومع ذلك لا رد.

**الجذر (مؤكَّد من قاعدة الإنتاج + سجلّات Cloud Run):** `messages.sender_id` مرتبط بـFK على `users.id`. إصلاح PD-2 (commit `19fc66e`، 15 يونيو 18:01 UTC) أدرج رسالة الوكيل بـ`senderId=معرّف الوكيل` (من `ai_agents`) → كل إدراج يكسر القيد `messages_sender_id_users_id_fk` → الـendpoint يرمي 500 → الـdomain_event `failed` → **الرد يُولَّد (ai_run=succeeded) لكن يُرمى، العميل لا يصله شيء**. ارتباط زمني قاطع: آخر نجاح 17:45 UTC، أول فشل 21:06 UTC (أول وارد بعد نشر `19fc66e`). **من commit المالك، ليس Cursor.**

| الطبقة | الإصلاح | الملف |
|---|---|---|
| 1 — الجذر | `senderId: null` + `senderName: agent.name` (نفس نمط `agent-tools.ts` الصحيح) | `internal.routes.ts` |
| 2 — دفاع عميق (محمية #10) | فشل الحفظ/الإدراج → تصعيد للبشر + `notifyWorkspace` + إبلاغ الـworker بالتصعيد (`done` لا `failed`) بدل إسقاط صامت متكرّر | `internal.routes.ts` |
| 4 — حارس انحدار | تعليق حاكم: رسائل الوكيل/النظام senderId=null دائماً | `internal.routes.ts` |
| 5 — توثيق | تشريح PD-7 كامل + تصحيح سجلّ النطاق 5 | `launch-readiness-plan.md` |

**استرداد (الطبقة 3 — بعد النشر):** الأحداث الـ11 الفاشلة تُعاد لـ`pending` ليعيد الـworker معالجتها (منطق 24س يرسل الطازج ويصعّد المنتهي). **يُنفَّذ بعد أن تصبح النسخة الجديدة حيّة فقط.**

**typecheck + build:** api-server ✅

**متبقٍّ للـstaging:**
```
artifacts/api-server/src/routes/internal.routes.ts
.claude/skills/wesal-one-agents/references/launch-readiness-plan.md
.codex/skills/wesal-one-agents/references/launch-readiness-plan.md
WESAL_ONE_CHAT_HANDOFF.md
```

**الدرس الحاكم:** "مُغلق بالكود" ≠ "يعمل في الإنتاج". النطاق 5 كان ✅ بينما الوكيل ميّت فعلياً ~20 ساعة. بوابة الإغلاق تتطلب **اختباراً حيّاً** (رسالة واردة حقيقية → رد يصل)، لا typecheck/build فقط.

---

## Hotfix H8-3 — إصلاح "الوكيل صمت" + رسائل لا تظهر (16 يونيو 2026)

بعد دفعات Cursor الثلاث (`d4c5b2e`, `b26da1c`, `baffd53`) ظهرت أعطال جديدة. الفحص كشف:

| الرمز | العطل | الإصلاح | الملف |
|---|---|---|---|
| H8-3a | فشل الأداة لا يُصعّد بعد الآن → الوكيل يكرر وعد "سأحوّل لفريق" بلا تحويل فعلي | أعدت `hasToolProblem` لشرط `shouldEscalate` | `agent-reply.ts` |
| H8-3b | رد `/internal/agent-reply` يكتب `shouldEscalate: false` دائماً (قيمة ثابتة خطأ) + لا تنبيه للفريق عند أي تصعيد | القيمة الحقيقية + `notifyWorkspace` على كل تصعيد | `internal.routes.ts` |
| H8-3c | **خطر**: الـworker كان يعيد تفعيل الوكيل تلقائياً بعد ساعتين من أي محادثة مُصعَّدة للبشر بصمت — يخالف قاعدة "لا قرار حسّاس بلا إنسان" | حذف إعادة التفعيل التلقائي بالكامل — التصعيد للبشر يبقى ساري حتى يتدخّل أحد | `outbox-worker/index.ts` |
| H8-3d | `api-server` بلا `max-instances` على Cloud Run؛ الـrealtime (SSE) في الذاكرة فقط → لو شغّلت نسخ متعددة، تأخير عرض الرسائل (polling كل 5-10s يخفّف الأثر لكن لا يلغيه) | `--min-instances=1 --max-instances=1` (يطابق الـworker) | `cloudbuild.yaml` |
| H8-3e | الرسائل اليدوية الصادرة (ردّ موظف) لا تبثّ SSE — موظف آخر بتبويب آخر ينتظر الـpolling فقط | `emitWorkspaceEvent` لكل رسالة صادرة | `conversations.routes.ts` |

**typecheck + build:** api-server ✅ outbox-worker ✅

**متبقٍّ للـstaging:**
```
artifacts/api-server/src/lib/agent-reply.ts
artifacts/api-server/src/routes/internal.routes.ts
artifacts/api-server/src/modules/conversations/conversations.routes.ts
artifacts/outbox-worker/src/index.ts
cloudbuild.yaml
WESAL_ONE_CHAT_HANDOFF.md
```

⚠️ **تنبيه مهم:** آخر 3 commits (`d4c5b2e`, `b26da1c`, `baffd53`) جاءت من Cursor مباشرة على `main` بدون مراجعة هنا. يُنصح بمراجعة أي عمل مستقبلي من Cursor في هذا المستودع قبل الدفع، خصوصاً للقرارات التي تخصّ سلوك التصعيد البشري/الأمان.

---

## جدول النطاقات

| النطاق | العنوان | الحالة | Commits |
|---|---|---|---|
| **النطاق 1** | المصادقة والجلسات | ✅ مُغلق | `4123883` |
| **النطاق 2** | عزل العملاء | ✅ مُغلق | `4f804e4` |
| **النطاق 3** | الأسرار والاعتمادات | ✅ مُغلق | `739cca0` |
| **النطاق 4** | متانة القنوات | ✅ مُغلق | `3388ab9` |
| **النطاق 5** | وقت تشغيل الوكيل | ✅ مُغلق — اختُبر حيّاً 19 يونيو (الوكيل ردّ بعد إصلاح PD-12) | `ff49ebe` `ce93b59` `c3d9d61` + PD-7 |
| **النطاق 6** | الموثوقية والتشغيل | ✅ مُغلق (R6-6 يدوي) | `86fa6cb` |
| **النطاق 7** | قاعدة المعرفة والاسترجاع | ✅ مُغلق | `86fa6cb` |
| **النطاق 8** | الوسائط (PD-3) + vision | ✅ مُغلق — اختُبر حيّاً 19 يونيو (عرض/إرسال + الوكيل يحلّل الصور بصرياً) | `86fa6cb` + `c85f2f9` |
| **النطاق 9** | الأتمتة والمتابعة والبث | ✅ مُغلق (الأتمتة مؤجّلة بقرار) | — |
| **النطاق 10** | جهات الاتصال | ✅ مُغلق — اختُبر حيّاً 20 يونيو (استيراد/تصدير CSV + custom_fields) | `cc9baf6` `f52a262` |
| **النطاق 11** | الطلبات | ✅ مُغلق — اختُبر حيّاً 20 يونيو (إنشاء، حالات، بنود، دفعات) | `bf1eaa1` |

---

## النطاق 10 — جهات الاتصال

**مدفوع:** `cc9baf6` — عزل الهوية، dedup للهاتف/واتساب، الأرشفة، الدمج في API، الحقول المخصصة، وفهارس migration `0027`.

**محلي غير مدفوع (19 يونيو 2026):** استيراد/تصدير CSV الآمن + واجهة دمج المكررات + عرض/تعديل الحقول المخصصة. typecheck للـAPI والواجهة و`build:prod` ✅.

**بوابة الإغلاق:** 🔍 تبقى مفتوحة حتى حل انجراف Drizzle journal وتطبيق migration `0027` واختبار العزل والاستيراد والدمج والبحث والصلاحيات حيّاً. لا انتقال إلى النطاق 11 قبل ذلك.

---

## النطاق 5 — التفصيل

### مُغلق ✅
| الرمز | الإصلاح | Commit |
|---|---|---|
| H5-1 | تصعيد صامت عند غياب AI — لا يصل نص تجريبي للعميل | (سابق) |
| Q5-4 | idempotency عبر `domainEventId` — يمنع رد مكرر عند إعادة معالجة domain_event | `ff49ebe` |
| M5-2 | توحيد `SAFETY_SYSTEM_PROMPT` مع السلوك الفعلي — حُذف «أنت تقترح فقط» | `ce93b59` |
| Q5-1 | ربط المحرك الهجين (TSV + vector) بالوكيل الحي — بديل ILIKE البسيط | `c3d9d61` |

### مكتمل ✅
| الرمز | المطلوب | الحالة |
|---|---|---|
| Q5-2 | `EMBEDDINGS_DRY_RUN=false` في Cloud Run (us-central1) | ✅ مفعّل |
| Phase 2 | أدوات صادق (4a168ea8) — create_order, log_payment_claim, schedule_followup, send_product_media, handoff_to_human | ✅ مفعّلة عبر SQL مباشر |

---

## Hotfix Lane — سجل الأعطال الإنتاجية

| الرمز | العطل | الحالة | Commit |
|---|---|---|---|
| PD-1 | الإرسال اليدوي لا يصل للعميل | ✅ محلول | (سابق) |
| PD-2 | الرسائل لا تظهر في الوارد | ✅ محلول — الجذر انجراف schema (PD-12، `custom_fields` مفقود) لا SSE؛ اختُبر حيّاً | — |
| PD-3 | الوسائط في الوارد (عرض + إرسال + vision) | ✅ محلول — الوكيل يحلّل الصور بصرياً، اختُبر حيّاً 19 يونيو | `c85f2f9` |
| PD-4 | وسائط المنتج من الكتالوج | ⛔ مؤجّل — يحتاج `catalog_management` | — |
| PD-5 | مكالمات واتساب | ⛔ مؤجّل — يحتاج `business_calling` | — |
| PD-6 | IG/Messenger: الربط + الاستقبال + الإرسال | 🔧 إصلاح ثانٍ محلي — ينتظر الدفع والاختبار الحي | `ffea61a` + محلي |
| PD-11 | زر «إعادة للوكيل» يتذبذب ولا يعيد الوكيل بثبات | ⏸️ مؤجّل إلى Claude Code بقرار المالك | `9d4ddfb` `8fce81b` |
| H5-1 | نص تجريبي يصل للعميل | ✅ محلول | (سابق) |

---

## 🔧 PD-6 — فشل اكتشاف الصفحات بعد نجاح التسجيل المضمن (22 يونيو 2026، Codex)

**الدليل الحي:** نافذة Meta اكتملت بنجاح والواجهة وصلت إلى endpoint الجديد، ثم ظهر الخطأ
«تعذر اكتشاف الصفحات وحسابات إنستغرام من Meta» (`meta_discovery_failed`).

**الجذر:** مسار IG/Messenger في commit `ffea61a` كان يستدعي `fetchMetaChannelOptions()` العام. هذا المساعد يشغّل
`Promise.all` بين `me/accounts` المطلوب للقناتين و`me/businesses` الذي يطلب أيضاً
`owned_product_catalogs` و`owned_ad_accounts`. التطبيق لا يملك `catalog_management` أو `ads_management`؛ فشل طلب
الأعمال غير الضروري كان يُسقط اكتشاف الصفحات السليم كله.

**الإصلاح المحلي:** استخراج `fetchMetaPageOptions()` الذي يطلب `me/accounts` فقط، واستخدامه في endpoint
`/meta/embedded-signup/instagram-messenger/complete`. بقي المسار العام يستعمل نفس المساعد للصفحات مع اكتشاف الأعمال،
بلا تكرار للمنطق وبلا تغيير لإعدادات تطبيق Meta.

**التحقق:** `typecheck:libs` ✅ · `api-server typecheck` ✅ · `api-server build` ✅. `build:prod` متوقف محلياً بسبب
تثبيت `landing-next` الناقص (`next/dist/server/require-hook` مفقود)، لا بسبب PD-6.

**بوابة الإغلاق:** بعد الدفع: ربط IG/Messenger → ظهور القناتين «نشط» → رسالة واردة → رد صادر يصل للعميل.

---

## البنية التقنية (للمرجع السريع)

| المكوّن | المسار | الدور |
|---|---|---|
| api-server | `artifacts/api-server/` | Express 5 — HTTP + webhooks + SSE |
| outbox-worker | `artifacts/outbox-worker/` | يـpoll كل 3s — يرسل لـMeta + يشغّل الوكيل كل 5s |
| web | `artifacts/web/` | React 19 + Vite — لوحة التحكم |
| db | `packages/db/` | Drizzle ORM + schema |

**قنوات Meta:** واتساب (Tech Provider token) · Instagram (page token مشفّر AES-256-GCM) · Messenger (page token مشفّر)

**نشر تلقائي:** كل push على `main` → Cloud Build → api-server + worker (~3 دقائق)

---

---

## النطاق 6 — التفصيل

### مُكتمل ✅
| الرمز | الإصلاح | الملف |
|---|---|---|
| R6-1 | Worker يكتب heartbeat كل 10s → `/readyz` تعمل صحيح | `outbox-worker/src/index.ts` |
| R6-2 | Cleanup loop كل 5 دقائق → cleanup-outbox + cleanup-domain-events | `outbox-worker/src/index.ts` |
| R6-3 | `logAlert()` يبثّ JSON هيكلي (`severity=CRITICAL`) عند الفشل الدائم → Cloud Monitoring log-based alert | `outbox-worker/src/index.ts` |
| R6-4 | حذف `outbox.service.ts` + 4 routes الميتة | `integrations.routes.ts` + حذف الملف |
| R6-5 | حذف `catalog-sync.ts` اليتيم | حذف الملف |

### يدوي (لا يحتاج كود)
| الرمز | الإجراء |
|---|---|
| R6-6 | `gcloud run services update khadamatak-staging --min-instances=1 --region=us-central1 --project=khadamatk-auth` |
| R6-3 alert | في Cloud Logging → Log-based alerts → Filter: `jsonPayload.alert="outbox.permanently_failed" OR jsonPayload.alert="domain_event.failed"` |

---

## النطاق 7 — التفصيل

### مُغلق ✅
| الرمز | الإصلاح | الملف |
|---|---|---|
| Q7-1 | `EMBEDDINGS_DRY_RUN` + `AI_EMBEDDINGS_DRY_RUN` كلاهما مقبول → Vertex embeddings تعمل فعلاً | `services/embeddings.ts` |

### جيد بلا تعديل ✅
| البند | الحالة |
|---|---|
| عزل workspaceId في كل استعلام معرفة | ✅ |
| `rebuildDocumentChunks` عند الإنشاء/التحديث/إعادة-الفهرسة | ✅ |
| الاسترجاع الهجين (TSV + vector) مسلّك للوكيل | ✅ (Q5-1) |

---

## النطاق 8 — الوسائط (PD-3)

### مُكتمل ✅
| الرمز | الإصلاح | الملف |
|---|---|---|
| M8-1 | بروكسي عرض الوسائط الواردة عبر Graph API | `meta-media.ts` + `conversations.routes.ts` |
| M8-2 | عرض المرفقات في الوارد | `InboxPage.tsx` |
| M8-3 | إرسال صورة يدوياً عبر رابط HTTPS (واتساب) | `conversations.routes.ts` + worker |
| M8-4 | ربط `loadMediaContext` بحلقة الوكيل الحيّة | `agent-reply.ts` |
| M8-5 | إرسال وسائط IG/Messenger عبر رابط HTTPS | `outbox-worker/index.ts` + `conversations.routes.ts` |

### مؤجّل (coming_soon) ⛔
| البند | السبب |
|---|---|
| رفع ملف مباشر (GCS) | يحتاج مسار تخزين + مسح |
| تفريغ الصوت للوكيل | يحتاج Vertex speech API |

### بوابة الإغلاق — مؤجلة بقرار المالك

هذه الخطوة مؤجلة الآن بقرار المالك، ولا تُقترح كخطوة تالية حتى يطلب المالك استئناف النطاق 8.

عند استئنافها لاحقاً:
1. صورة واردة من واتساب تظهر في الوارد
2. إرسال صورة برابط من الوارد → تصل للعميل (WA/IG/Messenger)
3. النص العادي ما زال يعمل

---

## Session update — 2026-06-29 — L3 commerce/payment read

- Owner decision: live Meta/channel test is deferred for now; continue to next launch-readiness step.
- Per section 5, next step is L3 commerce. Checked `ac0b27f` (`fix: restore payment read isolation and dto parity`) and did not cherry-pick it because it depends on commerce route/test files that are not present in this working tree and the commerce branch is large.
- Applied the same minimal read-isolation fix to the current payments route instead:
  - `GET /payments` now validates read query params with Zod.
  - confirmed/pending totals now reuse the same workspace + method/currency/contact/order/date scope as the list query, instead of counting across the whole workspace.
  - response now includes `page` and `limit`.
- Verification:
  - `corepack pnpm -C artifacts/api-server run typecheck` ✅
  - `corepack pnpm -C artifacts/api-server run build` ✅

Changed file for this L3 step:
- `artifacts/api-server/src/modules/payments/payments.routes.ts`

Note: web typecheck/build remains blocked by pre-existing `artifacts/web/src/lib/fileExtract.ts` missing `pdfjs-dist` and `mammoth`; not touched in this step.

---

## النطاق 9 — الأتمتة والمتابعة والبث (فُحص 16 يونيو 2026)

**الخريطة:**
- `automations` + `automation-engine.ts` (worker) — محرّك كامل (شروط+إجراءات: send.template, add.tag, assign.conversation, create.task, create.followup) **لكنه غير مستورد في `index.ts` → لا يعمل أبداً حياً**.
- تعارض كامن: استعلام claimDomainEvents في automation-engine يأخذ كل event_type ما عدا catalog.sync — يتداخل مع agent-runner الذي يأخذ `message.received/echo` بالتحديد. لو وُصِّل بدون تعديل، الاثنان يتنافسان على نفس الصفوف (FOR UPDATE SKIP LOCKED يمنع الازدواج، لكن لو فاز automation-engine يضيع رد الوكيل على تلك الرسالة).
- `schedule_followup` (أداة الوكيل) → يُنشئ صفاً في `followups` فقط؛ لا إرسال تلقائي — تذكير لموظف يعرضه `GET /followups?due=true`. **سلوك سليم ومقصود، ليس عطلاً.**
- `agent-learning.ts` (worker) — **غير مستورد أيضاً** → `learned_answers` فارغة دائماً. أثر منخفض (صمت، لا ضرر).
- `billing-maintenance.ts` (worker) — **غير مستورد أيضاً** → الاشتراكات لا تنتقل active→grace→expired تلقائياً أبداً (يخص الفوترة، اكتشاف عرضي).
- **Q9-1 (عطل مؤكّد):** `broadcasts.service.ts: startBroadcast()` يُدرج outbox event بحمولة `{ contactId, templateId, variableMapping }` بلا حقل `to` وبلا `templateName/language` — والـworker يرفض فوراً أي event بلا `to`. **كل حملة بث ستفشل 100% من رسائلها — الميزة معطّلة كاملاً منذ إنشائها.**
- لا يوجد أيضاً مسار يُحدِّث `broadcast_recipients.status/sentAt` بعد نجاح إرسال الـworker — حتى بعد إصلاح `to`، شاشة تتبّع الحملة ستبقى "queued" دائماً.

**بوابة الإغلاق:** ❌ — البث معطّل كلياً (Q9-1)، الأتمتة لا تعمل حياً (يتيمة).

**قرار المالك:** الحد الأدنى للبث الآن، تأجيل الأتمتة.

### مُغلق ✅
| الرمز | الإصلاح | الملف |
|---|---|---|
| Q9-1 | `startBroadcast()` يبني `to` (هاتف العميل) + `templateName/language` + `components` من `variableMapping` بدل حمولة ناقصة كانت تفشل 100% | `broadcasts.service.ts` |
| — | الأتمتة: فُحصت واجهة `AutomationsPage` — العنوان الفرعي يقول صراحة "بدون تنفيذ تلقائي مباشر في هذه المرحلة" — **صادقة بالفعل، لا تعديل لازم** | — |

### مؤجّل (بقرار المالك) ⛔
| البند | السبب |
|---|---|
| توصيل `automation-engine.ts` بالـworker | يحتاج فصل تعارض مع agent-runner أولاً؛ مؤجّل |
| تتبّع حالة البث (sent/delivered/failed) في `broadcast_recipients` بعد إرسال الـworker | إصلاح لاحق، خارج الحد الأدنى الحالي |
| `agent-learning.ts` / `billing-maintenance.ts` (worker، يتيمتان) | أثر منخفض / يخص الفوترة — تُسجَّل فقط |

**typecheck + build:** api-server ✅

**متبقٍّ للـstaging:**
```
artifacts/api-server/src/modules/broadcasts/broadcasts.service.ts
WESAL_ONE_CHAT_HANDOFF.md
```
3. النص العادي ما زال يعمل
