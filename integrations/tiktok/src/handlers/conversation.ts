import type { ConversationHandlers } from "@chatbotx.io/sdk"
import type { TiktokAuthValue } from "../schema"

export const conversationHandlers: Partial<
  ConversationHandlers<TiktokAuthValue>
> = {}
